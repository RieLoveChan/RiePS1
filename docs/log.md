# Knowledge Bundle Update Log

## 2026-08-09
* **Timing word route initializer reconstructed**: Added `FUN_8002d60c` (36 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It selects between two local initialization paths, writes `0x800e4060`, and clears `0x800e406c`, preserving the original branch and jump delay slots. `Invoke-FunctionMatch.ps1` matched all 36 bytes at executable file offset `0x1360c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `98fe509f9b0b36043480c83611e5b78bb18fef9c8d5085f704b4e0647773ef8a`.
* **Card-format wrapper reconstructed**: Added `FUN_8002d5d4` (56 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It reads the timing byte at `0x800e4068`, scales it by 16, calls `_card_format` at `0x8003b82c`, adjusts the argument based on the result, and returns it. `Invoke-FunctionMatch.ps1` matched all 56 bytes at executable file offset `0x135d4` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `5392ea1cf452cfea03f801e47f2eb8ce6f19ae48085f9a9b3574ecc728bacc23`.
* **Timing byte pair setter reconstructed**: Added `FUN_8002d5c0` (20 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It stores `$a0` at `0x800e4068`, reloads the data base, and clears `0x800e4064` in the branch-delay slot. `Invoke-FunctionMatch.ps1` matched all 20 bytes at executable file offset `0x135c0` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `5508458e429652b74c88d01667d88493c02cc4d7cc94871b8852097ef7e823da`.
* **SPU reinitialization coordinator reconstructed**: Added `FUN_8002fcdc` (56 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It calls `ResetCallback`, `SsUtReverbOff`, `SpuClearReverbWorkArea(7)`, and `_SsInit` in sequence, preserving the return address across the calls. `Invoke-FunctionMatch.ps1` matched all 56 bytes at executable file offset `0x15cdc` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `d00989e2815b2e6ecfed9b778f30f4e4b90e85d57ada9cd0ed56469efc2d6f52`.
* **State-word conditional rewrite reconstructed**: Added `FUN_8002f29c` (36 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It reads `0x800d6b9c`, compares it with `$t0`, and when different stores `$a0` back to the same shared state word before returning. `Invoke-FunctionMatch.ps1` matched all 36 bytes at executable file offset `0x1529c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `e99ec07ed271643e355fc732269585ea19b829cfbc5275e0857481b4108b028f`.
* **SPU interrupt wrapper reconstructed**: Added `FUN_8002ef2c` (36 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It preserves the input in `$a1`, calls `InterruptCallback` at `0x80035b38` with `$a0=9`, restores `$ra`, and returns. `Invoke-FunctionMatch.ps1` matched all 36 bytes at executable file offset `0x14f2c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `68015b4d11fc8699c6b77ac65034ab0925b07d35391b62afe1624e3648dfc6a3`.
* **SPU timing-state bit predicate reconstructed**: Added `FUN_8002ee8c` (32 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It loads the state pointer at `0x800d6b64`, reads halfword offset 426, tests bit `0x40`, and returns a normalized boolean. `Invoke-FunctionMatch.ps1` matched all 32 bytes at executable file offset `0x14e8c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `d3ead6cb24b7ca21fa7e8ec29ea950464ee1080a6b2ac943991ae48b1d4ba19a`.
* **SPU voice-parameter wrapper reconstructed**: Added `FUN_8002e8bc` (36 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It saves `$ra`, sets `$a2=204` and `$a3=205`, calls `FUN_8002e8ec` at `0x8002e8ec`, restores `$ra`, and returns. `Invoke-FunctionMatch.ps1` matched all 36 bytes at executable file offset `0x148bc` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `ca92c009b8f6ba3946a3d5be854b5718372903784bd3822f40d26185f5b09c58`.
* **DMA callback wrapper reconstructed**: Added `FUN_8002e72c` (36 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It preserves the input in `$a1`, calls `DMACallback` at `0x80035b68` with `$a0=4`, restores `$ra`, and returns. `Invoke-FunctionMatch.ps1` matched all 36 bytes at executable file offset `0x1472c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `3119f7b94919133714dd0913c042fce24a78d7de3cab4160eebb7838f515a2a7`.
* **GTE depth/scale setter reconstructed**: Added `FUN_80036668` (12 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It writes `$a0` to GTE data register 26 and returns. `Invoke-FunctionMatch.ps1` matched all 12 bytes at executable file offset `0x1c668` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `d8c79a8c2661cba5b0c79fdf925407b5a4945c383bb0c8c293287878a693cac2`.
* **GTE coordinate shift helper reconstructed**: Added `FUN_80036648` (24 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It shifts both input coordinates left by 16 bits and loads them into GTE data registers 24 and 25 before returning. `Invoke-FunctionMatch.ps1` matched all 24 bytes at executable file offset `0x1c648` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; the GTE transfers are represented by exact `.word` opcodes; reference/built SHA-256 `9aa95b09a0c5f31d49c3f954d1bc4cbe5101b46feb9a7a9a92673d0716fdfcf1`.
* **Timing state word accessor reconstructed**: Added `FUN_8003659c` (16 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It loads the word at `0x800d875c` through the `0x800e` data-base alias and returns it. `Invoke-FunctionMatch.ps1` matched all 16 bytes at executable file offset `0x1c59c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `df272196ccc9788bcc0d331755ffcf1163e28b6195ba563a4850451cd46afc7c`.
* **Timing mode flag predicate reconstructed**: Added `FUN_80033604` (24 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It loads the mode word at `0x800d6b98`, inverts its low bit, normalizes the result to `0` or `1` with `sltu`, and returns. `Invoke-FunctionMatch.ps1` matched all 24 bytes at executable file offset `0x19604` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `eb7851223f63a554d9ed05eb8aa20585505ee777bb95bb1aaa3fc6daee2daae0`.
* **Timing mode flag selector reconstructed**: Added `FUN_800335dc` (40 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It compares `$a0` with `1`, then stores either zero or one at `0x800d6b98` and returns. `Invoke-FunctionMatch.ps1` matched all 40 bytes at executable file offset `0x195dc` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `e3c052f495ecc0a705765261bd3e5081afdcd97b6dc3802211b71b0a090352e5`.
* **SPU voice wrapper reconstructed**: Added `FUN_8003323c` (36 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It saves `$ra`, sets `$a2=202` and `$a3=203`, calls `_SpuSetAnyVoice` at `0x8002e8ec`, restores `$ra`, and returns. `Invoke-FunctionMatch.ps1` matched all 36 bytes at executable file offset `0x1923c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `90f5279f932758c1dc8a11c61802cdb2a9f88bc2ba16b04080017116e2279c6c`.
* **Timing voice-allocation mode setter reconstructed**: Added `FUN_80032bac` (32 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It saves `$ra`, calls `FUN_8002e4fc` with `$a0=1`, restores `$ra`, and returns. `Invoke-FunctionMatch.ps1` matched all 32 bytes at executable file offset `0x18bac` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; linker symbol `FUN_8002e4fc` was resolved at `0x8002e4fc`; reference/built SHA-256 `c97da4eaf320829d757d1e026beb6f30bc54f889bc2d5c2113fecd3d1d8fe915`.
* **Timing table secondary halfword clear reconstructed**: Added `FUN_80032d9c` (12 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It loads the `0x800e` data base and clears the halfword at `0x800e51b8` in the branch-delay slot of `jr $ra`. `Invoke-FunctionMatch.ps1` matched all 12 bytes at executable file offset `0x18d9c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `0b77ce464bda62d1f7f38073b42832e4a763b52b9aa1b8e4967f1f2582250ab1`.
* **Timing table halfword clear reconstructed**: Added `FUN_80034b3c` (12 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It loads the `0x800e` data base and clears the halfword at `0x800e5200` in the branch-delay slot of `jr $ra`. `Invoke-FunctionMatch.ps1` matched all 12 bytes at executable file offset `0x1ab3c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `e25b14cca8383777baeb354d177f11e79276b692d9f73c0e269d74a2a4ddbb5d`.
* **Timing table mode flag setter reconstructed**: Added `FUN_80034b2c` (16 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It returns the constant `1` in `$v0`, loads the `0x800e` data base, and stores the halfword at `0x800e5200` in the branch-delay slot. `Invoke-FunctionMatch.ps1` matched all 16 bytes at executable file offset `0x1ab2c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `f6f49becae6a5b3f974253e0f4c1bc29ad7e2215b6ef7076eb50635fd587caa6`.
* **Timing table byte-offset setter reconstructed**: Added `FUN_80034b1c` (12 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It loads the `0x800e` data base into `$at`, stores `$a0` at `0x800e5250` in the branch-delay slot, and returns. `Invoke-FunctionMatch.ps1` matched all 12 bytes at executable file offset `0x1ab1c` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `0b8faee3f2c8408fd93a83f5a559d0bc9eb4afc9c10d2d2f946c6c5f90483aeb`.
* **Timing state flag setter reconstructed**: Added `FUN_8002d518` (20 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It stores the input byte at `0x800e407c`, reloads the `0x800e` base, and clears the byte at `0x800e408c` in the branch-delay slot. `Invoke-FunctionMatch.ps1` matched all 20 bytes at executable file offset `0x13518` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `a7f620ae7f83de3acc7a0ff40c9d56de9b34d9b8d24fc3d67b96190521debad4`.
* **Timing record offset initializer reconstructed**: Added `FUN_8002b9e0` (212 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It initializes relative pointers, walks timing groups, and accumulates each nested record's offsets into the shared base. `Invoke-FunctionMatch.ps1` matched all 212 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `86d3ad7b5d426e6a26caf3b8b7c0a87d31b7389c4f27aaede59579dbcf3edd7e`.
* **Timing geometry GTE mapper reconstructed**: Added `FUN_8002b8d0` (272 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It transforms timing coordinates through the GTE, packs the result with the lookup table at `0x800e54b4`, and updates the linked output pointers. `Invoke-FunctionMatch.ps1` matched all 272 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `1a4cc16c65e04ef79ed498074ee7b7c0060182577500548d3a3ad11a7458ffaa`.
* **Timing geometry expansion helper reconstructed**: Added `FUN_8002b734` (412 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It runs the GTE transform over source triples, copies indexed geometry records with unaligned loads/stores, and advances the secondary timing buffer. `Invoke-FunctionMatch.ps1` matched all 412 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `5eb6e17399c1ab24a65e22b6457cd05f42ddb641b3fe17925b48aa8338d8c805`.
* **Timing record dispatcher reconstructed**: Added `FUN_8002b634` (256 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It walks timing records, normalizes source pointers, dispatches 52-byte records through `FUN_8002b734`, and forwards generated geometry through `FUN_8002b8d0`. `Invoke-FunctionMatch.ps1` matched all 256 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `562fc447a194b922eef27c2271ccedd5567468a046a4716d6d63f5df6e78b257`.
* **Timing GTE control loader reconstructed**: Added `FUN_8002b1e8` (40 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It loads the five-word matrix/control block into GTE control registers 0 through 4. `Invoke-FunctionMatch.ps1` matched all 40 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `b65afce17a6564daad28edb6a406cb9b20c26d698e5e6f5fadb32f74d9215b27`.
* **Timing stream primitive builder reconstructed**: Added `FUN_8002bbac` (140 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It emits two 32-byte primitives per source record, applies the encoded color/flag word, copies the source coordinates, and advances through the record table. `Invoke-FunctionMatch.ps1` matched all 140 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `2541604b0285643aaf959f7d85583928650e613fe6d65db1ede7390e6e707153`.
* **Timing stream record encoder reconstructed**: Added `FUN_8002bacc` (224 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It walks fixed-size records, emits the source pointer and flag byte into the destination stream, and delegates 52-byte records to `FUN_8002bbac`. `Invoke-FunctionMatch.ps1` matched all 224 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `ba7bf1fa74cbf7d53436674874441aae99ca3abf4aeff7ec79f6daa8ef96d106`.
* **Timing record slot address helper reconstructed**: Added `FUN_8002bab4` (24 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It scales a slot index by three words, advances the base pointer, and stores the resulting address into the record. `Invoke-FunctionMatch.ps1` matched all 24 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `1f5b75e7d77da8abd81b098dcb511e79cd03c5380ca1a9da1aa826355712f5e6`.
* **Timing decoder state initializer reconstructed**: Added `FUN_8002abf0` (72 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It initializes the decoder record at `0x800e3b40`, sets its source and end pointers, clears its counters, and invokes `FUN_8002a80c` to clear the backing range. `Invoke-FunctionMatch.ps1` matched all 72 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `b44ee6a564aa83bfb7ab5d523b702977921c37093db46714db07b584ec131ffd`.
* **Timing bitstream decoder reconstructed**: Added `FUN_8002a8b8` (292 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It decodes literal, short-back-reference, long-back-reference, and run-length records into the destination buffer, preserving the original loop counters and branch delay slots. `Invoke-FunctionMatch.ps1` matched all 292 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `80ce1f7d4b04c5d1d9b5cfde7bb273cda774448bcae0f7ea8a0f383c384d1ef9`.
* **Timing no-op return stub reconstructed**: Added `FUN_8002a8b0` (8 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`, preserving its `jr $ra` and MIPS delay-slot `nop`. `Invoke-FunctionMatch.ps1` matched all 8 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `6d64edf91449c1b17746c1ef18afa2eb25c70bdf1322ab3df5a2630993b7e2f1`.
* **Timing pointer-chain rebuilder reconstructed**: Added `FUN_8002a82c` (132 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It clears each record head, follows linked pointers, rebuilds the masked pointer and flag fields, and advances through the bounded table. `Invoke-FunctionMatch.ps1` matched all 132 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `1d7d9e1fa9c75d1b51e57dc1baf2436488a815790691f8735f3d8f401bdf0995`.
* **Timing record range clearer reconstructed**: Added `FUN_8002a80c` (32 bytes) to `src/ddr5thmix/LibrarySignatureBatch9.s`. It clears the word range beginning at the record's pointer and ending at its stored limit, preserving the original MIPS delay-slot instructions. `Invoke-FunctionMatch.ps1` matched all 32 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; reference/built SHA-256 `08dfe845eb4840e5d36054aaa1715d83049568238f8f2a5419bd2bb71b7b40b7`.
* **Timing helper cluster reconstructed**: Added `FUN_8002a670` (16 bytes), `FUN_8002a680` (24 bytes), `FUN_8002a698` (48 bytes), and `FUN_8002a6c8` (32 bytes) to `src/ddr5thmix/StateTransition.s`. Together they expose the baseline, reset and update the accumulator, and convert it through reciprocal division. `Invoke-FunctionMatch.ps1` matched all four functions (16/16, 24/24, 48/48, and 32/32) against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 values are respectively `e822bca9818d6039440113614e4705827abc1328004886439d947d96bd4b1ebe`, `3e2cc97d6f59bb5a6fae6fa461fa57121977e65c16c7b21aaa4aaeb3d3598319`, `47eea3688d720577bfdbcd826e26ba01dffbbbf5c098fceed00def185149354f`, and `69c11fff7f57a905a7decddabe4fd8c73d5ddc54f4f695d81ac6ef2fd202111f`.
* **Timing scale dispatcher reconstructed**: Added `FUN_8002a5a8` (200 bytes) to `src/ddr5thmix/StateTransition.s`. It stores the input baseline, scales it through reciprocal division and a table byte, clamps the signed result to `127`, and submits it to `FUN_8003164c`. `Invoke-FunctionMatch.ps1` matched all 200 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `7621ac1d907157927e2820aeef301387c8aed480d98e9c9b95467b35cebd7605`.
* **Timing feedback updater reconstructed**: Added `FUN_8002a4c8` (224 bytes) to `src/ddr5thmix/StateTransition.s`. It refreshes shared timing state, computes a signed difference against the current target, dispatches `FUN_8002a5a8` with an adjusted value, marks completion, and stores the refreshed baseline. `Invoke-FunctionMatch.ps1` matched all 224 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `a15abab3059002268da1d4d55d9b126811ff66176936493728732736963d821d`.
* **Timing limit test reconstructed**: Added `FUN_8002a438` (52 bytes) to `src/ddr5thmix/StateTransition.s`. It adds `FUN_8002a6c8`'s result to the shared offset at `0x800e2bbc` and tests whether it exceeds the limit at `0x800e2bc0`. `Invoke-FunctionMatch.ps1` matched all 52 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `3ba1e2b6325a84d6c86680ca0fa1801fcaeb17820b73698c0988d3ec098dcc43`.
* **Timing quotient helper reconstructed**: Added `FUN_8002a3fc` (60 bytes) to `src/ddr5thmix/StateTransition.s`. It converts the `FUN_8002a670` result through a reciprocal multiply and returns the corrected value divided by `128`. `Invoke-FunctionMatch.ps1` matched all 60 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `8930326abac20f44564319b27834950f210f067ccdb00681c3e3ebd53e3d1815`.
* **Timing rate updater reconstructed**: Added `FUN_8002a300` (252 bytes) to `src/ddr5thmix/StateTransition.s`. It derives a scaled target from the input, optionally dispatches `FUN_8002a5a8`, refreshes the record through `FUN_800358f8`/`FUN_8002a670`, computes a divisor-based rate, and clamps the shared timing field. `Invoke-FunctionMatch.ps1` matched all 252 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `55bbd7cc87a65ec57e958aea372e01f1b589af6726527504c589a82ffcd5e673`.
* **Timing hook pair reconstructed**: Added `FUN_8002a2b0` (40 bytes) to `src/ddr5thmix/StateTransition.s`. It invokes `FUN_80032048` for event codes `18` and `17`. `Invoke-FunctionMatch.ps1` matched all 40 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `5cb0d65a0e61efd45c82978b6c31c1b4c03fce4ceaa29c4064559a5522c7b4a3`.
* **Timing identifier validator reconstructed**: Added `FUN_8002a20c` (164 bytes) to `src/ddr5thmix/StateTransition.s`. It selects one of two state-dependent halfword tables, returns `1` when the input identifier is found, and otherwise returns `0`. `Invoke-FunctionMatch.ps1` matched all 164 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `56354d656666ae547184fbfd2e12178370fd3e768b0bdaafb04d0862bf9f76b3`.
* **Timing mode setter reconstructed**: Added `FUN_8002a200` (12 bytes) to `src/ddr5thmix/StateTransition.s`. It writes the supplied value into the halfword state field at `0x800e38c6`. `Invoke-FunctionMatch.ps1` matched all 12 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `9f37f6e83f03c04e6a30e8df8155cde2cd7d2342c806b5c1bc089daa51084ed5`.
* **Timing mode setter reconstructed**: Added `FUN_8002a1f0` (16 bytes) to `src/ddr5thmix/StateTransition.s`. It stores `272` into the halfword state field at `0x800e38c6`. `Invoke-FunctionMatch.ps1` matched all 16 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `c5b1bd1f4f295bdf6949e8b3b199ae002dc84a72643fd51a6af281752b3139c5`.
* **Timing subset delta reconstructed**: Added `FUN_8002a1b0` (64 bytes) to `src/ddr5thmix/StateTransition.s`. It initializes the selected four-byte entry through `FUN_800358f8(-1)` and returns the new result minus its prior value. `Invoke-FunctionMatch.ps1` matched all 64 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `33f20c1be27c0ddf5376455433fc65e1fca4e871c99be142e3559aa72b7d51c3`.
* **Timing subset initializer reconstructed**: Added `FUN_8002a174` (60 bytes) to `src/ddr5thmix/StateTransition.s`. It invokes `FUN_800358f8` with `-1` and stores its result in the four-byte entry selected from `0x800e3a00`. `Invoke-FunctionMatch.ps1` matched all 60 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `b810cca1e574521a14ad2dd60b4cfdf08c347d77a4197dace42bb15a59323774`.
* **Timing subset clear wrapper reconstructed**: Added `FUN_8002a140` (52 bytes) to `src/ddr5thmix/StateTransition.s`. It clears a four-byte record selected by a byte index from `0x800e3a00` through `FUN_8003bac8`. `Invoke-FunctionMatch.ps1` matched all 52 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `97077c086c026ce219cea2441958547421abadc1a5af6e37cba90d2bea4392b2`.
* **Timing subset sweep reconstructed**: Added `FUN_8002a108` (56 bytes) to `src/ddr5thmix/StateTransition.s`. It invokes `FUN_8002a140` for normalized indices `0` through `4`. `Invoke-FunctionMatch.ps1` matched all 56 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `7733b2e0d1720c65a74a0b4be675b6c4fd2a1d341825bb9851d1b3f1dd9539af`.
* **Timing slot writer reconstructed**: Added `FUN_8002a0b8` (56 bytes) to `src/ddr5thmix/StateTransition.s`. For slot indices below `21`, it stores three halfword fields and one stack-supplied byte into the record at `0x800e3818`. `Invoke-FunctionMatch.ps1` matched all 56 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `b30b9dfec3420dcfa209eb3960ea6f6bad88b3ca8092cd72244a9b8d2c4d763a`.
* **Timing slot clear wrapper reconstructed**: Added `FUN_8002a04c` (32 bytes) to `src/ddr5thmix/StateTransition.s`. It clears the pointed-to eight-byte record through `FUN_8003bac8`. `Invoke-FunctionMatch.ps1` matched all 32 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `70015ddb5c1643ee4b96bae57e322cfc4bceee9fb4321360de6ff2f6c6c1a596`.
* **Timing slot sweep reconstructed**: Added `FUN_8002a004` (72 bytes) to `src/ddr5thmix/StateTransition.s`. It invokes `FUN_8002a04c` once for each of 20 eight-byte records beginning at `0x800e3818`. `Invoke-FunctionMatch.ps1` matched all 72 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `29b7bc9e653f3fc945aff347e70c137f183be258a8279457f03fe91457819fd9`.
* **Timing resource lookup reconstructed**: Added `FUN_80029f40` (196 bytes) to `src/ddr5thmix/StateTransition.s`. It searches three category tables for a 16-bit identifier, returning the category and entry index through output pointers or `-1`. `Invoke-FunctionMatch.ps1` matched all 196 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `2eabee04f43a14f8b9eebbde9ad663e49dc0d0877af0dc282e6c52ef4ccc7708`.
* **Timing record initializer reconstructed**: Added `FUN_80029ef8` (56 bytes) to `src/ddr5thmix/StateTransition.s`. It clears the 44-byte shared record at `0x800e3730` and sets its state byte to `6`. `Invoke-FunctionMatch.ps1` matched all 56 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `c3ac575f320b1c55109c7d8c23dd67cfcf96474ae6eb585d0371f5ffa22fa84e`.
* **Timing table reset reconstructed**: Added `FUN_80029ed0` (40 bytes) to `src/ddr5thmix/StateTransition.s`. It clears three words beginning at `0x800e3a18`. `Invoke-FunctionMatch.ps1` matched all 40 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `8ed56828bd499ad79570c7d0664d6f1754935431b69f830395763540d522583f`.
* **Timing state counter reconstructed**: Added `FUN_80029e34` (60 bytes) to `src/ddr5thmix/StateTransition.s`. It counts nonzero halfword entries across 16 slots at `0x800e3988`. `Invoke-FunctionMatch.ps1` matched all 60 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `1b033a5b810863e7ae1bdd869467ee736464b244865f62e9b4dd3e83e0f384b1`.
* **Timing state increment/clamp reconstructed**: Added `FUN_80029e08` (44 bytes) to `src/ddr5thmix/StateTransition.s`. It increments a signed 16-bit state and returns it when below `16`, otherwise returning zero. `Invoke-FunctionMatch.ps1` matched all 44 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `4f0883e980876c6c517eb13370481d77595b53e6ba4b0ba0d27f292ca59f233f`.
* **Timing state cleanup reconstructed**: Added `FUN_80029da0` (104 bytes) to `src/ddr5thmix/StateTransition.s`. It clears the previous indexed state entry when the current and target states differ, updates the current state through `FUN_80029e08`, and returns the cleared value or zero. `Invoke-FunctionMatch.ps1` matched all 104 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `144f6a5c6a376365d8e571a80e0cf4b31bb60064cbfa82a75848934365829292`.
* **Timing state transition reconstructed**: Added `FUN_80029d24` (124 bytes) to `src/ddr5thmix/StateTransition.s`. It compares the current state through `FUN_80029e08`, writes a requested halfword into the indexed state table, updates the current state, and returns success or `-1`. `Invoke-FunctionMatch.ps1` matched all 124 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `52bd5e091989f75d3440f63d77ad709fe7329a53a9203444a4b1db10a0c0af6b`.
* **Timing index mapper reconstructed**: Added `FUN_80029cdc` (72 bytes) to `src/ddr5thmix/StateTransition.s`. It clamps a 16-bit index to `30`, looks up a signed halfword in the table at `0x8002a854`, and forwards it to `FUN_800302bc`. `Invoke-FunctionMatch.ps1` matched all 72 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `9dc151dfd4318d6985eeda941ebdfd2ca924576777014bde01f091d3ed3a3021`.
* **Timing mode getter reconstructed**: Added `FUN_80029ccc` (16 bytes) to `src/ddr5thmix/StateTransition.s`. It returns the mode/state word stored at `0x800e38c8`. `Invoke-FunctionMatch.ps1` matched all 16 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `8cfabba7e7a2cfd8991dad1fa4648430a0b02360ff0d625f876d305dbbcd04ae`.
* **Timing mode color setup reconstructed**: Added `FUN_80029c40` (140 bytes) to `src/ddr5thmix/StateTransition.s`. It selects one of two RGB triplets based on the mode, uploads it through `FUN_8003fca0`, updates `0x800e38c8`, and calls `FUN_80024e00`. `Invoke-FunctionMatch.ps1` matched all 140 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `537fe283f4706b5702359feb249edca33d643e8b44d20348e1ce038a0d813b50`.
* **Timing slot reset reconstructed**: Added `FUN_80029b94` (68 bytes) to `src/ddr5thmix/StateTransition.s`. It normalizes a slot index, performs `FUN_80032048`, then clears the corresponding eight-byte record at `0x800e3818` through `FUN_8003bac8`. `Invoke-FunctionMatch.ps1` matched all 68 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `6053fe59fcdb5a7efb26f909b653e846df6c6d9e8bb7b69952ba9442d9a3e87d`.
* **Timing parameter assembler reconstructed**: Added `FUN_80029ab4` (224 bytes) to `src/ddr5thmix/StateTransition.s`. It indexes timing data at `0x800be2f8`, computes scaled values with `0x51eb851f`, prepares auxiliary parameters and calls `FUN_80031cec`, returning a signed 16-bit result. `Invoke-FunctionMatch.ps1` matched all 224 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `8dbc0bc153e55744252cdf2ab4fe48bc024b77b6d1d54ba4622c954cb48f6aee`.
* **Timing record threshold cleanup reconstructed**: Added `FUN_80029a4c` (104 bytes) to `src/ddr5thmix/StateTransition.s`. It scans 20 slots at `0x800e3818` and invokes `FUN_80029b94` for entries whose 16-bit value is at least `819`. `Invoke-FunctionMatch.ps1` matched all 104 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `82629980646ab8362fa95fb412e80bd3c50885d4c9941413bc6f50bd94252430`.
* **Timing record cleanup dispatcher reconstructed**: Added `FUN_800299dc` (112 bytes) to `src/ddr5thmix/StateTransition.s`. It scans 20 slots at `0x800e3818` for a matching 16-bit identifier and invokes `FUN_80029b94` with the matched slot index. `Invoke-FunctionMatch.ps1` matched all 112 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `713798ca07914f9f5efc72bad05bb464ce77ca850bb64073a4759c5f823c8abb`.
* **Timing resource resolver reconstructed**: Added `FUN_80029920` (188 bytes) to `src/ddr5thmix/StateTransition.s`. It clamps the third input to `100`, queries `FUN_80029f40`, maps signed indices through `0x800e3a18`, submits the resolved ID and parameters to `FUN_80029ab4`, and propagates failure as `-1`. `Invoke-FunctionMatch.ps1` matched all 188 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `1409bdbcf5e0b75975ceb36ed7dbaa53544f3a1a850857953080c49208bb57e8`.
* **Timing slot allocator reconstructed**: Added `FUN_80029868` (152 bytes) to `src/ddr5thmix/StateTransition.s`. It scans 17 eight-byte slots at `0x800e3818`, skips occupied entries, tests availability through `FUN_8002f11c`, records the first valid index at `0x800e2b84`, and returns `-1` on failure. `Invoke-FunctionMatch.ps1` matched all 152 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `90a1108846ef2795bb5e4e60ef0dcc4ce6c80b9937901622772d3dce9493b482`.
* **Timing record address helper reconstructed**: Added `FUN_80029850` (24 bytes) to `src/ddr5thmix/StateTransition.s`. It masks the input to one byte, scales it by eight, and returns `0x800e3818` plus that offset. `Invoke-FunctionMatch.ps1` matched all 24 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `216ae4eb5f0418a1e8b9c8bf105f9e105444af8eceb87c7d6b202f599e9a2409`.
* **Timing subsystem initializer reconstructed**: Added `FUN_800297c4` (104 bytes) to `src/ddr5thmix/StateTransition.s`. It submits two records through `FUN_8002f42c` with flags `0x4000` and `0x2000`, status `3`, then invokes `FUN_8002ef5c(0, 0x300000)`. `Invoke-FunctionMatch.ps1` matched all 104 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `f0ab5a1d55b83a169523485c3e90b686b6bbe5ee05b9369c50df62a0e648ce34`.
* **Timing record subsystem reset reconstructed**: Added `FUN_80029788` (60 bytes) to `src/ddr5thmix/StateTransition.s`. It invokes `FUN_800297c4`, clears fields at `0x800e37fa` and `0x800e380e`, clears record flags at `0x800e37f8` and `0x800e3800`, and clears byte `0x800e39b9`. `Invoke-FunctionMatch.ps1` matched all 60 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `b30967594fc5518e465320cc8a51e941ef556b5cd9210092f56168fc1e5cfcd3`.
* **Timing record submitter reconstructed**: Added `FUN_80029750` (40 bytes) to `src/ddr5thmix/StateTransition.s`. It passes the 28-byte record at `0x800e37f8` to `FUN_8003bac8`. `Invoke-FunctionMatch.ps1` matched all 40 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `06befb48f91eb7c1e8dff16e2f5cf51ba637dfdab1de9f59e0b9fab6d5b16bf8`.
* **Timing record finalizer reconstructed**: Added `FUN_80029644` (104 bytes) to `src/ddr5thmix/StateTransition.s`. It scans records marked `3` at `0x800e3958`, matches the input argument, marks the record with byte value `4`, and returns zero. `Invoke-FunctionMatch.ps1` matched all 104 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `628935d92121f1c22d27f7332ab3f5c5614970819212f37a87fca8d1166575a6`.
* **Timing record matcher reconstructed**: Added `FUN_800295ac` (152 bytes) to `src/ddr5thmix/StateTransition.s`. It scans active records at `0x800e3958`, compares each argument against the input, marks a match with byte value `3`, and returns the corresponding result from `0x800ae480`. `Invoke-FunctionMatch.ps1` matched all 152 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `0d9e6085b92e7f0758f9d1cb2bffe24e0974e55444b01c15630fa283043472cb`.
* **Timing record lookup reconstructed**: Added `FUN_80029520` (140 bytes) to `src/ddr5thmix/StateTransition.s`. It scans up to the active mode count in the four/eight-byte record table at `0x800e3958`, dispatches record kinds through `0x8001aae4`, compares the per-record argument, and returns the matching result from `0x800ae480`. `Invoke-FunctionMatch.ps1` matched all 140 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `033f9b71e7a91f36c63bdf565b6a35fb66787d1e91be1dff8a4ce4259f887c9d`.
* **Timing record cleanup loop reconstructed**: Added `FUN_800294f8` (40 bytes) to `src/ddr5thmix/StateTransition.s`. It clears the first byte of each of four eight-byte slots beginning at `0x800e3958`. `Invoke-FunctionMatch.ps1` matched all 40 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `0320c5728b50b7d4d04e0e978c7f8132d4d538d351bc44e6836f6e7ac441029a`.
* **Timing record reset reconstructed**: Added `FUN_800294d4` (36 bytes) to `src/ddr5thmix/StateTransition.s`. It clears the active byte at `0x800e3978` in the `FUN_800294f8` call delay slot, then delegates record cleanup. `Invoke-FunctionMatch.ps1` matched all 36 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `81564b537e36dabd9adad30dd3275fdcde1edc810b47526671fe369cda61624b`.
* **Timing record initializer reconstructed**: Added `FUN_80029480` (84 bytes) to `src/ddr5thmix/StateTransition.s`. If the active byte at `0x800e3978` is clear, it initializes the 16-byte record at `0x800e3958` with the two arguments; otherwise it delegates to `FUN_800294f8`. `Invoke-FunctionMatch.ps1` matched all 84 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `41b3232a5c7ccc0e4994613c1e6dcb35910a0bdcf584e91104ec944af8de16b9`.
* **Timing mode byte setter reconstructed**: Added `FUN_80029474` (12 bytes) to `src/ddr5thmix/StateTransition.s`. It stores the first argument as a byte at `0x800e397a`; the second argument is unused by the observed implementation. `Invoke-FunctionMatch.ps1` matched all 12 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `e5496676138733641df07bdaf8e90d8bfcd7f6b725151c55e42a326023680146`.
* **Timing buffer coordinator reconstructed**: Added `FUN_80029440` (52 bytes) to `src/ddr5thmix/StateTransition.s`. It submits the 48-byte record at `0x800e3958` through `FUN_8003bac8`, then invokes `FUN_80029474(4, 16)`. `Invoke-FunctionMatch.ps1` matched all 52 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `bebdf92175ffcd4c8ae52e97b16ed5a8e0e872aa1ce81e6f5d678a0488856099`.
* **Configuration index scaler reconstructed**: Added `FUN_80029410` (48 bytes) to `src/ddr5thmix/StateTransition.s`. It evaluates the observed shift/add polynomial, multiplies by `0xd722fb11`, and returns the HI product shifted right by 11 bits. `Invoke-FunctionMatch.ps1` matched all 48 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `b834910c76dd480a6f55cb283ce9671db14d7aa7a86afc9feece88cda855fe58`.
* **Timing state initializer reconstructed**: Added `FUN_80029390` (128 bytes) to `src/ddr5thmix/StateTransition.s`. It clears the completion word at `0x800e39c0`, sets the active flag at `0x800ae478`, mirrors the cleared value into `0x800ac890` and `0x800ac88c`, runs reset calls, invokes `CdControlF(9, 0)`, and stores mode `5` at `0x800e39bc`. `Invoke-FunctionMatch.ps1` matched all 128 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `b4371577fe84a2c724ec00f79b96d0ade665551d13bd4864aa51672f53c102f7`.
* **Timing mode-4 watchdog reconstructed**: Added `FUN_80029324` (108 bytes) to `src/ddr5thmix/StateTransition.s`. It increments the record counter at `0x800e39d4` only when mode is `4` and the completion field at `+0x28` is zero; after `180` increments it calls `CdControlF(9, 0)`. `Invoke-FunctionMatch.ps1` matched all 108 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `cd8c94b7f2f738015a3ee3d612b58daaf7f0a601eaa77ba1dde4d7da721fdaa7`.
* **Timing scale calculation reconstructed**: Added `FUN_800292e8` (60 bytes) to `src/ddr5thmix/StateTransition.s`. It evaluates the observed polynomial in the input and scales it with `0x51eb851f`, returning the HI product shifted right by 5 bits. `Invoke-FunctionMatch.ps1` matched all 60 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `601b8d5baa37d5339c844c85236e97eb34a151d56ea82852ad79304bcd40aea6`.
* **Timing flag handler reconstructed**: Added `FUN_80029268` (128 bytes) to `src/ddr5thmix/StateTransition.s`. It tests `0x800ae46c` and the `0x10` result bit from `FUN_8003f738`, updates `0x800ae474`, and invokes `FUN_80025c1c(3)` on the reset paths. `Invoke-FunctionMatch.ps1` matched all 128 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `caa8cbf4e89051770eed77111d8d450c33defaf395d8ff67990b1bc4ef75c12d`.
* **Timing limit setter reconstructed**: Added `FUN_80029238` (48 bytes) to `src/ddr5thmix/StateTransition.s`. It clamps the argument to `0xff`, stores it at `0x800e38c0`, and invokes `FUN_80024e00`. `Invoke-FunctionMatch.ps1` matched all 48 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `ef9b051954a42684ab2aff56dc01c864faa29f48c16e28422ccbd2050335e986`.
* **Timing byte initializer reconstructed**: Added `FUN_80029228` (16 bytes) to `src/ddr5thmix/StateTransition.s`. It stores `0xff` at `0x800e38c0`. `Invoke-FunctionMatch.ps1` matched all 16 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `f8a1807c87ea9c3b859c555c09e1f5cb3f8f0323a5b70deb30a23d5bd9ee1e9e`.
* **Timing value progression reconstructed**: Added `FUN_8002916c` (188 bytes) to `src/ddr5thmix/StateTransition.s`. Mode `1` advances `0x800e37c0` with saturation, mode `2` decrements it with a lower bound, clears mode byte `0x800e37d4`, and invokes `FUN_80024e00`. `Invoke-FunctionMatch.ps1` matched all 188 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `787cd75d330ae92b52f4dcb824e2c4116b06b04a33a6dcc9a294d1ca6cc94474`.
* **Timing state getter reconstructed**: Added `FUN_8002915c` (16 bytes) to `src/ddr5thmix/StateTransition.s`. It returns the value stored at `0x800e37c0`. `Invoke-FunctionMatch.ps1` matched all 16 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `f6df0c9dd59a3bbedf69aab774844b4de09efe7c5451122befe6ebebaf44ab3d`.
* **Timing configuration updater reconstructed**: Added `FUN_80029084` (216 bytes) to `src/ddr5thmix/StateTransition.s`. It clamps the input to `127`, scales it by `127`, updates `0x800e37c0`, and either resets via `FUN_80024e00` or computes direction/rate fields and timestamps through `FUN_800358f8(-1)`. `Invoke-FunctionMatch.ps1` matched all 216 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `275e63b146ba9ee52e11c8d5dd86d3fa4c85beb85b97ce1633928aef4c8b3093`.
* **Timing state initializer reconstructed**: Added `FUN_8002906c` (24 bytes) to `src/ddr5thmix/StateTransition.s`. It stores `0x3f01` at `0x800e37c0` and clears byte `0x800e37d4`. `Invoke-FunctionMatch.ps1` matched all 24 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `818c1c16989be3017c052b902fa428d86d8249345240999e8c68f07e765c5ce1`.
* **Timing interval coordinator reconstructed**: Added `FUN_80028fc8` (164 bytes) to `src/ddr5thmix/StateTransition.s`. It compares the requested value with `0x800e37a0`, computes the interval and direction mode in the record at `0x800e37ac`, initializes its count to `16` when empty, and updates timestamps through `FUN_800358f8(-1)`. `Invoke-FunctionMatch.ps1` matched all 164 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `29aff723848227c8b69f941cede2139255592642e043052bf32a14d8a01d1526`.
* **Timing request wrapper reconstructed**: Added `FUN_80028f94` (52 bytes) to `src/ddr5thmix/StateTransition.s`. It submits the 20-byte record at `0x800e37ac` through `FUN_8003bac8` and clears its `+0x10` counter. `Invoke-FunctionMatch.ps1` matched all 52 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `c1b607fa297a2fce08827a83bf36de01d90db844af99ffce0f49fc74f08f1bdc`.
* **Timing event predicate reconstructed**: Added `FUN_80028f74` (32 bytes) to `src/ddr5thmix/StateTransition.s`. It returns one when the two control bytes at `0x800e3b38` and `0x800e3b39` differ, and zero when they match. `Invoke-FunctionMatch.ps1` matched all 32 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `1c9cec157b26d45e643826bb549f2678984d42eb514ce58e288ee197ff612835`.
* **Timing event ring-buffer writer reconstructed**: Added `FUN_80028ef0` (132 bytes) to `src/ddr5thmix/StateTransition.s`. It advances the 16-entry index byte at `0x800e3b39`, writes four argument words into the selected 16-byte entry at `0x800e3a28`, and returns `-1` when the ring is full. `Invoke-FunctionMatch.ps1` matched all 132 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `8628c444f148c3642516ed1daa5587c002ee2f6c419c33e8d4f0ef8e08d88faa`.
* **Timing buffer update wrapper reconstructed**: Added `FUN_80028e80` (112 bytes) to `src/ddr5thmix/StateTransition.s`. It submits the 276-byte buffer at `0x800e3a28` through `FUN_8003bac8` while preserving and restoring its four words at offsets `0x100`–`0x10c`. `Invoke-FunctionMatch.ps1` matched all 112 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `44d4410d92dcbfb173a2827067aa2dc3977c13d65ef6c8055bfbac333abf82f9`.
* **Timing buffer submission wrapper reconstructed**: Added `FUN_80028e58` (40 bytes) to `src/ddr5thmix/StateTransition.s`. It passes the 276-byte buffer at `0x800e3a28` to `FUN_8003bac8`. `Invoke-FunctionMatch.ps1` matched all 40 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `ce9886e3888e1b8cd87f57bcf0f0a0ca0ae5d1a38eab1549f8f862dcdd9b5f21`.
* **Timing mode dispatcher reconstructed**: Added `FUN_80028dec` (108 bytes) to `src/ddr5thmix/StateTransition.s`. It stores the mode at `0x800e37d8`, dispatches modes below 12 through `0x8002aab4`, and invokes `FUN_8002a174` with arguments `0`–`3` for the four active table entries. `Invoke-FunctionMatch.ps1` matched all 108 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `c957f5cd867f1914c50799727c9860837bdf3655f2fd312d96f7a642652dff61`.
* **Timing accumulator reset reconstructed**: Added `FUN_80028de0` (12 bytes) to `src/ddr5thmix/RuntimeGraphicsInit.s`. It clears `0x800e37d8` in the return delay slot. `Invoke-FunctionMatch.ps1` matched all 12 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `58961e84a23056aa7ed32541a69843510a57c9f5bafd34575091e84a67066014`.
* **Timing configuration loader reconstructed**: Added `FUN_80028d90` (80 bytes) to `src/ddr5thmix/RuntimeGraphicsInit.s`. It stores the selected index and fields from the table at `0x800ad7e0` into the five-word record at `0x800e3770`, adding the source `+4` field shifted right by 11 to the `+8` field. `Invoke-FunctionMatch.ps1` matched all 80 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `e7e275a832b44894ca8c69901ba3820c78b7d62af659e90224de046bf55cb9b5`.
* **Timing interval helper reconstructed**: Added `FUN_80028d5c` (52 bytes) to `src/ddr5thmix/RuntimeGraphicsInit.s`. It derives a scaled value from `0x800e377c` using `0x88888889`, then multiplies it by `0x800e37d8` and shifts right by 12 bits. `Invoke-FunctionMatch.ps1` matched all 52 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `f5611b2a6f1468c379a3b20a649874f7a28e3a02162f8465e5d7f898b1078a45`.
* **Timing rate helper reconstructed**: Added `FUN_80028d3c` (32 bytes) to `src/ddr5thmix/RuntimeGraphicsInit.s`. It scales the 8-byte timing accumulator at `0x800e38b8` by `0xd1b71759` and returns the HI product shifted right by 13 bits. `Invoke-FunctionMatch.ps1` matched all 32 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `011fa7c5049c82695b0c71528c739c7171f13fa60ff4b642ffad29cef1b6a4e1`.
* **Timing scale helper reconstructed**: Added `FUN_80028ce4` (88 bytes) to `src/ddr5thmix/RuntimeGraphicsInit.s`. It multiplies the fields at `0x800e37a0` and `0x800e37d8`, applies the unsigned factor `0xbe37c63b`, and evaluates the observed shift/add scale. `Invoke-FunctionMatch.ps1` matched all 88 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `6636878d5d3ca33c35dd84a9b0f4feb82237f9a56d0b49bfeb4d1c27c6238008`.
* **Timing accumulator reconstructed**: Added `FUN_80028c74` (112 bytes) to `src/ddr5thmix/StateTransition.s`. It derives a delta from `FUN_800358f8(-1)` and `0x800e37a4`, then updates both words of the 8-byte buffer at `0x800e38b8` using `FUN_80028ce4` and `FUN_80028d5c`. `Invoke-FunctionMatch.ps1` matched all 112 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `5edb698913e45eaea62425418175a4e8d1701d54e18d8f4c68785480c5589c0d`.
* **Transfer descriptor wrapper reconstructed**: Added `FUN_80028c4c` (40 bytes) to `src/ddr5thmix/StateTransition.s`. It passes the 8-byte buffer at `0x800e38b8` to `FUN_8003bac8`, preserving the observed stack frame and delay-slot epilogue. `Invoke-FunctionMatch.ps1` matched all 40 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `361c5660e10433405b3eb7e16c4ff0701f9473c2ecc6638aad95b2038ffe2e3c`.
* **Graphics timing calculator reconstructed**: Added `FUN_80028c14` (56 bytes) to `src/ddr5thmix/RuntimeGraphicsInit.s`. It scales the timing field at `0x800e377c` with the observed unsigned multiply/HI extraction, then multiplies by the descriptor field at `0x800e37a0` and shifts the result. The exact reference/built SHA-256 is recorded after verifier validation.
* **Transfer size initializer reconstructed**: Added `FUN_80028c04` (16 bytes) to `src/ddr5thmix/StateTransition.s`. It stores `0x1000` at `0x800e37a0` in the return delay slot. The exact reference/built SHA-256 is recorded after verifier validation.
* **Completion flag getter reconstructed**: Added `FUN_80028b6c` (16 bytes) to `src/ddr5thmix/StateTransition.s`. It returns the completion flag stored at `0x800ae478`. The exact reference/built SHA-256 is recorded after verifier validation.
* **Queued transfer setup reconstructed**: Added `FUN_80028b00` (108 bytes) to `src/ddr5thmix/StateTransition.s`. It prepares the descriptor at `0x800e39b0`, increments its request counter, resolves the `0x80025d44` resource, invokes the transfer and completion helpers, and returns zero. The exact reference/built SHA-256 is recorded after verifier validation.
* **Transfer buffer wrapper reconstructed**: Added `FUN_80028ad0` (48 bytes) to `src/ddr5thmix/StateTransition.s`. It submits the 48-byte descriptor at `0x800e39b0` through `FUN_8003bac8` and sets the observed completion flag to `1`. The exact reference/built SHA-256 is recorded after verifier validation.
* **Timing value initializer reconstructed**: Added `FUN_80028aac` (36 bytes) to `src/ddr5thmix/StateTransition.s`. It calls `FUN_800358f8(-1)` and stores the result at `0x800e3798`, preserving the observed call and delay-slot layout. The exact reference/built SHA-256 is recorded after verifier validation.
* **Subsystem wrapper reconstructed**: Added `FUN_80028a8c` (32 bytes) to `src/ddr5thmix/StateTransition.s`. It invokes `FUN_80024e00` with no arguments and returns through the observed 24-byte stack frame. The exact reference/built SHA-256 is recorded after verifier validation.
* **Transfer descriptor initializer reconstructed**: Added `FUN_800289f4` (152 bytes) to `src/ddr5thmix/StateTransition.s`. It initializes the descriptor at `0x800e2b40` twice with the observed mode, address, and halfword fields, submitting each configuration through `FUN_8002f42c`. The exact reference/built SHA-256 is recorded after verifier validation.
* **Subsystem activation wrapper reconstructed**: Added `FUN_800289d0` (36 bytes) to `src/ddr5thmix/StateTransition.s`. It calls `FUN_8002ef5c` with arguments `0` and `0x00c00000`, preserving the observed setup and delay-slot layout. The exact reference/built SHA-256 is recorded after verifier validation.
* **Timing comparison helper reconstructed**: Added `FUN_80028894` (72 bytes) to `src/ddr5thmix/StateTransition.s`. It computes the elapsed value from the two state fields at `0x800e3774`/`0x800e3778`, scales it by four bits, compares the result against `FUN_80028d3c`, and returns the unsigned comparison. The exact reference/built SHA-256 is recorded after verifier validation.
* **State progression handler reconstructed**: Added `FUN_800287e0` (180 bytes) to `src/ddr5thmix/StateTransition.s`. It advances the state word at `0x800e376c` through states 0–3, runs the initialization/check sequence for state 1, increments the state after success, and clears the state back to zero for state 3. The exact reference/built SHA-256 is recorded after verifier validation.
* **State dispatch predicate reconstructed**: Added `FUN_80028740` (72 bytes) to `src/ddr5thmix/StateTransition.s`. It reads the state word at `0x800e3768`, dispatches values below `9` through the table at `0x8002aa8c`, and calls `FUN_8002422c` for out-of-range values. The exact reference/built SHA-256 is recorded after verifier validation.
* **Resource class predicate reconstructed**: Added `FUN_800286e4` (28 bytes) to `src/ddr5thmix/StateTransition.s`. It masks the argument to 16 bits, returns `2` for values below `53`, and returns `1` otherwise. The exact reference/built SHA-256 is recorded after verifier validation.
* **Callee-saved-register epilogue reconstructed**: Added \`FUN_800286bc\` (40 bytes) to \`src/ddr5thmix/StateTransition.s\`. The bounded section restores \`s3\`, \`s2\`, \`s1\`, and \`s0\`, then returns while releasing the 104-byte stack frame. The exact reference/built SHA-256 is recorded in the verifier output and symbol map after validation.
* **Three-entry resource lookup reconstructed**: Added `FUN_80028600` (96 bytes) to `src/ddr5thmix/StateTransition.s`. It masks the requested value to 16 bits, scans the three halfword entries at `0x800e3a18`, returns the matching zero-based index, and returns `-1` when no entry matches. `Invoke-FunctionMatch.ps1` matched all 96 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `8d302c9d83256f70af0515e918b4ed59215f3f1e4acc6c3e75afbb305aac2ebd`.
* **State-progress handler reconstructed**: Added `FUN_80028588` (120 bytes) to `src/ddr5thmix/StateTransition.s`. It initializes the state byte to 4, calls `FUN_800356ec(0)`, selects state 2 or 5 from the returned result and counter comparison, and enters state 6 through `FUN_8002f29c` when recovery is required. `Invoke-FunctionMatch.ps1` matched all 120 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `b81ff150c3d3db29f545207483fd987470479b32e31e43f12a8cc799cf0530c2`.
* **Subsystem initializer reconstructed**: Added `FUN_80028398` (268 bytes) to `src/ddr5thmix/StateTransition.s`. It clears the 36-byte workspace at `0x800e3988`, initializes descriptor and resource state, configures the 16-byte and 40-byte blocks, resets shared state, and enables the final services through the observed helper sequence. `Invoke-FunctionMatch.ps1` matched all 268 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `7f8a07397e735c846d090ebd3b96278a6016fff07e4ae3866bcd2c2213419a39`.
* **16-bit resource wrapper reconstructed**: Added `FUN_80028358` (32 bytes) to `src/ddr5thmix/StateTransition.s`. It masks its argument to `0xffff` and delegates the normalized value to `FUN_80026940`. `Invoke-FunctionMatch.ps1` matched all 32 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `62c11ea28de4fa109027a8285b01b5cdbb56f4d03ef2e405e4a17c6c2b307757`.
* **Timing gate predicate reconstructed**: Added `FUN_800282e4` (24 bytes) to `src/ddr5thmix/StateTransition.s`. It loads byte `0x800e37dc`, inverts its low bit, and returns one only when the resulting value is zero. `Invoke-FunctionMatch.ps1` matched all 24 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `8e73805d42e8e7aa7c96833c591a2192944d58e230f74a7c41f54306588314a2`.
* **Timing readiness predicate reconstructed**: Added `FUN_8002829c` (72 bytes) to `src/ddr5thmix/StateTransition.s`. It compares the two bytes at `0x800e3b38`/`0x800e3b39`, requires the shared state at `0x800e37d8` to be zero, then checks `0x800e3768` and returns zero only when all readiness conditions hold. `Invoke-FunctionMatch.ps1` matched all 72 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `268e055d7bb419c3e410b7248d35a55d0ac8f59e474b4eb80f068947aed4a8ed`.
* **Timing gate helper reconstructed**: Added `FUN_80028254` (72 bytes) to `src/ddr5thmix/StateTransition.s`. It calls `FUN_800282e4`, returns zero when the gate is inactive, and otherwise multiplies `0x800e38b8` by `0xd1b71759`, extracts `HI`, and shifts right 13 bits. `Invoke-FunctionMatch.ps1` matched all 72 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `3bed910aa52ee980dcecec187bd8e7b91d89b1ed6f7d3fedd4a3bf16cc6962be`.
* **Timing/rate helper reconstructed**: Added `FUN_8002821c` (56 bytes) to `src/ddr5thmix/StateTransition.s`. It returns zero when `0x800e37f4` is set; otherwise multiplies `0x800e38b8` by `0xd1b71759`, extracts `HI`, and shifts right 13 bits. `Invoke-FunctionMatch.ps1` matched all 56 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `ad735feb5c29d486b1559e481b280dd71fe436d3000b4e99da4391ab1848de29`.
* **Delegating wrapper reconstructed**: Added `FUN_800281f4` (40 bytes) to `src/ddr5thmix/StateTransition.s`. It forwards the two arguments to `FUN_80028fc8` in swapped register order and returns constant status `2`. `Invoke-FunctionMatch.ps1` matched all 40 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `78e29a4b2334a869d577732b3131f19b909fa64733bf5cc169135eaa676d1127`.
* **Three-argument configuration helper reconstructed**: Added the catalog-bounded `FUN_80028150` (148 bytes) to `src/ddr5thmix/StateTransition.s`. It scales the second argument through `FUN_80029410`, conditionally runs `FUN_80028e80`/`FUN_80028ef0`, and submits the retained third argument through the final `FUN_80028ef0` call. `Invoke-FunctionMatch.ps1` matched all 148 cataloged bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `bd396457ddde5ce2c1a4c330b30a3f6c660eff90d0f28e086276eec0f6ff376b`. The adjacent 16-byte `FUN_800281e4` remains separate.
* **Configuration helper reconstructed**: Added `FUN_800280c8` (136 bytes) to `src/ddr5thmix/StateTransition.s`. It scales the second argument by 16 after `FUN_80029410`, records it globally, conditionally runs `FUN_80028e80`/`FUN_80028ef0`, and always submits the final pair through `FUN_80028ef0`. `Invoke-FunctionMatch.ps1` matched all 136 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `a06e504569f3d0219ad1a72c683213e9dfc0037ef754125c3e1618b7914330ab`.
* **Initialization coordinator reconstructed**: Added `FUN_80028034` (148 bytes) to `src/ddr5thmix/StateTransition.s`. It invokes `FUN_800275b4` and the fixed subsystem setup sequence, computes the timestamp delta into `0x800e37a8`, updates `0x800e37a4`, and finishes with `FUN_8002a4c8`. `Invoke-FunctionMatch.ps1` matched all 148 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `29bab29bff515df27d307b784698142b2ba6d02f170f3fb4929dc611d7f4521c`.
* **Startup orchestrator reconstructed**: Added `FUN_80027f7c` (184 bytes) to `src/ddr5thmix/StateTransition.s`. It initializes the primary and secondary structures, resets the common descriptor, invokes the fixed startup routine sequence, and finishes with `FUN_80029238(190)`. `Invoke-FunctionMatch.ps1` matched all 184 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `10d5e7ab32254eadd7673e9cc8b35f92e190f47fae004e004eb10794544e44f5`.
* **Descriptor initializer reconstructed**: Added `FUN_80027ed4` (168 bytes) to `src/ddr5thmix/StateTransition.s`. It prepares `0x800e2b00` with buffer limits, flags, counters, and the shared timing value, submits it through `FUN_8002f42c`, then activates the subsystem through `FUN_8002ef5c`. `Invoke-FunctionMatch.ps1` matched all 168 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `ed80113381a013e83c35458df767499d3b576f254b7c0cc0f2962552303c9e0e`.
* **Runtime initializer reconstructed**: Added `FUN_80027d18` (444 bytes) to `src/ddr5thmix/StateTransition.s`. It configures two transfer buffers, initializes the backend and descriptor at `0x800e39e8`, returns distinct setup errors `-1`/`-2`/`-3`, and computes or waits for the shared timing value stored at `0x800e39e0`. `Invoke-FunctionMatch.ps1` matched all 444 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `0c074093e4f2208809c7528eab693a389c5489699287a6fde6100748e9c92265`.
* **State reset handler reconstructed**: Added `FUN_80027c20` (248 bytes) to `src/ddr5thmix/StateTransition.s`. It clears shared state through `FUN_8002fe7c`, initializes mode 9 through `FUN_8003f87c`/`FUN_8003fa20` for state 0, copies the mode byte for state 1, and resets descriptor fields for state 2. `Invoke-FunctionMatch.ps1` matched all 248 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `509828b1f4c19cb1aa3d7acd401b8140361a65e63de819de4ea3b1fa5f6ef8d5`.
* **State transition handler reconstructed**: Added `FUN_80027ac4` (348 bytes) to `src/ddr5thmix/StateTransition.s`. It dispatches states 0, 1, and 2 through `FUN_8003f87c`, initializes descriptor modes 27 and 16 through `FUN_8003fa20`, and applies the state-1 `0x20` flag/resource transition through `FUN_8002a680` and `FUN_8002fe7c`. `Invoke-FunctionMatch.ps1` matched all 348 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `01ccd5713498be8e377481fdb482cf451ea75a782e714ea8092cc799c80765e0`.
* **Nine-state backend dispatcher reconstructed**: Added `FUN_800277d8` (748 bytes) to `src/ddr5thmix/StateTransition.s`. It selects a handler from `0x8002aa64` using `0x800e376c`, prepares the shared descriptor at `0x800e2bb0`, calls `FUN_8003f87c`/`FUN_8003fa20` or `FUN_8003fd44`, and handles the special state-9 `0x40` flag path. `Invoke-FunctionMatch.ps1` matched all 748 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `2513cefa732656f9deb04fae02289fb35a363d395648e3897f657337349147b9`.
* **Three-resource initializer reconstructed**: Added `FUN_80027688` (336 bytes) to `src/ddr5thmix/StateTransition.s`. It queries resource IDs 815, 816, and 817 through `FUN_80029f40`, maps small indices through `0x800e3a18`, and registers the resulting values with `FUN_80029ab4` using widths 18, 18, and 17. `Invoke-FunctionMatch.ps1` matched all 336 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `1673853479084876814300f727c6ca7e66928b049d4094146090972c96bda6fa`.
* **Descriptor maintenance loop reconstructed**: Added `FUN_800275b4` (212 bytes) to `src/ddr5thmix/StateTransition.s`. It resets the 20-entry descriptor cleanup state, releases entries whose counters exceed 16 through `FUN_80032048`/`FUN_8003bac8`, then runs `FUN_80026a88` and finalizes through `FUN_8002e8bc`. `Invoke-FunctionMatch.ps1` matched all 212 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `b2802121649fa1672e7ba3dfc0391f29e3ea7b13b8559e0c21ee8ecbb774506e`.
* **Mode/resource initializer reconstructed**: Added `FUN_800273cc` (488 bytes) to `src/ddr5thmix/StateTransition.s`. It dispatches 18 mode codes, prepares two graphics records for the buffer mode, initializes pattern buffers for modes 0/1/8, invokes cleanup helpers, and routes modes 16/17 to `FUN_8002a200`. `Invoke-FunctionMatch.ps1` matched all 488 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `2c814e20355ac77bcf924eac33dfcb9ef26b92886dbfa6071cb6671e30c61f7a`.
* **Slot cleanup helper reconstructed**: Added `FUN_800272cc` (256 bytes) to `src/ddr5thmix/StateTransition.s`. It resolves a category table entry, walks its active records, compares each against the 20 shared slot descriptors, and invokes `FUN_80029b94` for every match. `Invoke-FunctionMatch.ps1` matched all 256 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `1b01fa54446c2b91fe58e7c1bad12d4156136132f46d1c174c313f796cb2f983`.
* **Dual-slot allocator reconstructed**: Added `FUN_80027074` (600 bytes) to `src/ddr5thmix/StateTransition.s`. It validates the packed request, scans 17 availability slots twice through `FUN_8002f11c`, constructs type-1 or type-2 request descriptors, and returns failure codes `-1` through `-6` for the observed error paths. `Invoke-FunctionMatch.ps1` matched all 600 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `c808d0e2e76b8acdefd572cad14a70bc4be26d377cd37ae9cac1f835f59ccdbf`.
* **Transfer updater reconstructed**: Added `FUN_80026e70` (516 bytes) to `src/ddr5thmix/StateTransition.s`. It initializes the shared state when needed, loads a unit through `FUN_8003540c`/`FUN_800354cc`, updates the active and saved cursors across normal and rollover paths, and returns distinct negative error codes. `Invoke-FunctionMatch.ps1` matched all 516 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `1e71d0c21fad06037573f94c95152d769c6b1efa8c8860cef5b751be5027d1f2`.
* **State-entry finalizer reconstructed**: Added `FUN_80026d00` (368 bytes) to `src/ddr5thmix/StateTransition.s`. It releases indexed auxiliary entries through `FUN_80034b7c`, copies the prepared buffer, loads the record through `FUN_80034fac`, records successful category metadata, and sets the completion flag and saved length. `Invoke-FunctionMatch.ps1` matched all 368 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `aee498e8f4ceb197463547ce01c4e1c36a834acf2f055fc9e4ceadc211f428e3`.
* **State-entry constructor reconstructed**: Added `FUN_80026bc8` (312 bytes) to `src/ddr5thmix/StateTransition.s`. It clears each active subentry through `FUN_800272cc`, copies table metadata into the shared `0x800e3730` record, derives its bounds and flags, and registers the initialized record through `FUN_8002f29c`. `Invoke-FunctionMatch.ps1` matched all 312 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `c4047517e56f5b44142a8edb980f8c8b90b1d540c6cf212da99c8e66ba9e2673`.
* **Category queue processor reconstructed**: Added `FUN_80026a88` (320 bytes) to `src/ddr5thmix/StateTransition.s`. It consumes the count from `FUN_80029e34`, dispatches 35 category handlers, invokes special resource/audio routines for selected codes, validates ordinary codes through `FUN_8002a20c`, and delegates the remaining record to `FUN_800262ec`. `Invoke-FunctionMatch.ps1` matched all 320 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `4023e24753e28c20e6a0c54684dd9dd3df9041f15619d2379e228ed1f1de27a7`.
* **Resource dispatcher reconstructed**: Added `FUN_80026940` (328 bytes) to `src/ddr5thmix/StateTransition.s`. It routes packed codes by high-byte category, validates and resolves categories 3/4 through metadata and request helpers, updates the resolved record status, and delegates other categories to `FUN_80029d24`. `Invoke-FunctionMatch.ps1` matched all 328 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `9a23a3cf28dc9c10333f8c04ddec634ff7b2d1b4ce440fa296db4cb6e9af4e34`.
* **State-record dispatcher head reconstructed**: Added `FUN_80026538` (116 bytes) to `src/ddr5thmix/StateTransition.s`. It guards an active record, dispatches the ten substate handlers through the table at `0x8001a964`, and increments the selected substate for the first handler. The cataloged interval ends at the branch whose delay slot begins `FUN_800265ac`. `Invoke-FunctionMatch.ps1` matched all 116 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `714ebfb385bdc718e886387470a2ac42debe56f776b71eb4deec80cd0d11fc61`.
* **Resource/state selector reconstructed**: Added `FUN_800262ec` (588 bytes) to `src/ddr5thmix/StateTransition.s`. It derives a timing flag, validates the supplied packed code, chooses one of twelve handlers, and fills the shared record with the selected resource base, size, and offset fields. `Invoke-FunctionMatch.ps1` matched all 588 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `ae3816a8fb50dd538cc0e2088a7162d9831bd74438204d9136ca8890154ee90a`.
* **Queued-record processor reconstructed**: Added `FUN_8002616c` (384 bytes) to `src/ddr5thmix/StateTransition.s`. It scans a bounded five-state record list, starts a 16-byte request through `FUN_80028b00` when the selected record is available, marks successful work, and advances the queue cursor. `Invoke-FunctionMatch.ps1` matched all 384 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `5bca4dcdcee879fa452b467c32676b6b307419cfcc06350b0f34f5c9d3b3cb61`.
* **Transition scaler reconstructed**: Added `FUN_80025c1c` (296 bytes) to `src/ddr5thmix/StateTransition.s`. It filters global states, enters state 11 for the active path, computes fixed-point scale values from the supplied low word and shared timing, updates three graphics fields, and re-enters transition state 3 when the limit is crossed. `Invoke-FunctionMatch.ps1` matched all 296 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `2f48ee4d8a7b04f78956162b47e107409147ce3a77728be75ee703f2582daf12`.
* **State handler reconstructed**: Added `FUN_80025b18` (260 bytes) to `src/ddr5thmix/StateTransition.s`. It selects the internal mode path for global states 3-4, divides the active counter by the supplied argument when needed, initializes the shared graphics record, and notifies the graphics subsystem with either fixed or caller-supplied parameters. `Invoke-FunctionMatch.ps1` matched all 260 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `76c9fb10eade9ea5f74d01d636ca935e179e2acebf2b6408e237aaa47e4fbd66`.
* **Timed graphics updater reconstructed**: Added `FUN_800259e8` (304 bytes) to `src/ddr5thmix/StateTransition.s`. It advances or clamps the shared counter, derives a scaled value, builds two branch-specific primitive records, and submits both through `FUN_8002f42c`. `Invoke-FunctionMatch.ps1` matched all 304 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `d6333eb151ba188529a2013fbd301b0a353a15f5113405b94d2ebd40ce637ae8`.
* **State-record rotator reconstructed**: Added `FUN_8002592c` (188 bytes) to `src/ddr5thmix/StateTransition.s`. It clears the selected slot when the two indices match; otherwise it copies the four-word record to the active state, advances the 16-entry ring index, and returns zero. `Invoke-FunctionMatch.ps1` matched all 188 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `9aed6551e2a0b629d8af95cd32479c701fa62bd7e7e41d727dcdf2a683c8867a`.
* **State-transition gate reconstructed**: Added `FUN_80025850` (220 bytes) to `src/ddr5thmix/StateTransition.s`. It applies index-range and state gates, calls two auxiliary checks, updates the shared transition field for cases 1-3, and invokes `FUN_8002a174`. `Invoke-FunctionMatch.ps1` matched all 220 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `8a17bf0a0fe118603ba2e540a68cc7031cb867e780d86ade62df27de6df64349`.
* **Graphics configuration helper reconstructed**: Added `FUN_80024e00` (352 bytes) to `src/ddr5thmix/RuntimeGraphicsInit.s`, completing the call chain from `FUN_80024ca4`. It derives a scaled timing/size value, selects one of two primitive-record layouts, and submits the records through `FUN_8002f42c`. `Invoke-FunctionMatch.ps1` matched all 352 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `e57f3798ab346f35dd640274be03a6eef348986be94993d2e7233dd7c556b194`.
* **Paired graphics-record initializer reconstructed**: Added `FUN_80024ca4` (348 bytes) to `src/ddr5thmix/RuntimeGraphicsInit.s`. It initializes two 56-byte records at the shared graphics workspace with parallel display/ordering parameters, invokes the graphics subsystems, and finishes through `FUN_80024e00`. `Invoke-FunctionMatch.ps1` matched all 348 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `aa8601d2051676c4f5a1386b1a1e21da6b0de01c8ddc0888a537bb40931449e2`.
* **Indirect state dispatcher reconstructed**: Added `FUN_80024f60` (124 bytes) to `src/ddr5thmix/StateDispatch.s`. It bounds-checks a 12-entry state index, loads a target from `0x8002a8ec`, and dispatches through `jr`; its cataloged interval includes the first target block and ends at `0x80024fdc`. A first assembly pass emitted one instruction beyond that cataloged boundary; removing the post-boundary instruction restored the exact 124-byte interval. `Invoke-FunctionMatch.ps1` matched all 124 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `249ce9862689e4138411d4dbf455cd112890b178de5339bdccb888a408cd9157`.
* **GPU ordering-table linker reconstructed**: Added `FUN_80023f68` (116 bytes) to `src/ddr5thmix/GraphicsPrimitiveBuilder.s`. It allocates an 8-byte primitive, combines a texture command word, and links the record into the ordering table, completing the contiguous `0x80023c00`-`0x80023fd8` graphics-builder cluster. `Invoke-FunctionMatch.ps1` matched all 116 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `065ee1b2156be1a86413facc89878f61bf75f329ba1908dcb3687319202b90b4`.
* **Third GPU primitive builder reconstructed**: Added `FUN_80023e9c` (204 bytes) to `src/ddr5thmix/GraphicsPrimitiveBuilder.s`. It writes a 20-byte primitive record with position, texture coordinates, RGB bytes, flags, and ordering-table links, completing the contiguous three-function builder cluster. `Invoke-FunctionMatch.ps1` matched all 204 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `10c654bdf9122e93afaa26cfc1cb77f1c8a733336e8b10e0c550a07edd394d0a`.
* **Second GPU primitive builder reconstructed**: Added `FUN_80023dbc` (224 bytes) to `src/ddr5thmix/GraphicsPrimitiveBuilder.s`. It writes a 16-byte primitive record with packed color/dimension values, flags, and ordering-table links, complementing `FUN_80023c00`. `Invoke-FunctionMatch.ps1` matched all 224 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `7232fb840a2a636ca7224618a7a31cf9c87d034327431f662f90ddf6f4c605b7`.
* **GPU primitive builder reconstructed**: Added `FUN_80023c00` (444 bytes) to `src/ddr5thmix/GraphicsPrimitiveBuilder.s`. It advances a shared 40-byte primitive record, encodes coordinates, dimensions, colors, flags, and ordering-table links, with no external calls. `Invoke-FunctionMatch.ps1` matched all 444 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `03086e95ccd9e96bec07951936295c5143b4e26c697d5c2a2227e415c8c460d7`.
* **CD request flag getter reconstructed**: Added `FUN_80021010` (16 bytes) to `src/ddr5thmix/GapStartup.s` and `runtime-core`. It returns the active request word at `DAT_800ac88c`, completing the small helper immediately after the CD read worker. `Invoke-FunctionMatch.ps1` matched all 16 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `caa052e9a7e5599b2b64c5fb3286e0813f5b485217b2550f26ceacc17b7f24fe`. Runtime-core now contains 19 functions and 4,420 selected bytes.
* **CD read worker reconstructed**: Added `FUN_80020efc` (216 bytes) to `src/ddr5thmix/GapStartup.s` and `runtime-core`. It marks the active request and sector count, stores the destination, installs the CD data callback, converts the sector position, starts `CdControlF`, and records the zero-length failure flag. `Invoke-FunctionMatch.ps1` matched all 216 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `12b92b492a2160df4972a85bb0d656cefe9b47283e38645f79f3919967eb3de4`. Runtime-core now contains 18 functions and 4,404 selected bytes.
* **CD synchronization poll reconstructed**: Added `FUN_80020e68` (128 bytes) to `src/ddr5thmix/GapStartup.s` and `runtime-core`. It calls `CdSync(1, 0)`, conditionally queries the CD status buffer through `CdControl`, and mirrors the result into `DdrSecondaryState+0x05`. `Invoke-FunctionMatch.ps1` matched all 128 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `d3265c4afeb209052ae5d4ebd70d83021d824350e0422f2b73e19298268dcb07`. Runtime-core now contains 17 functions and 4,188 selected bytes.
* **CD read-request initiator reconstructed**: Added `FUN_80020d24` (176 bytes) to `src/ddr5thmix/GapStartup.s` and `runtime-core`. It guards against an active request, delegates to `FUN_80020efc`, records the destination window in `DdrSecondaryState`, and computes the 0x800-byte sector-rounded end plus 0x100 bytes. `Invoke-FunctionMatch.ps1` matched all 176 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `11eb9ed7721d033da41c1ea7eb13a10b65c0602e385c2fe357c56af2e0b66cd7`. Runtime-core now contains 16 functions and 4,060 selected bytes.
* **Generic image upload helper reconstructed**: Added `FUN_800223a8` (456 bytes) to `src/ddr5thmix/RuntimeCore.s` and `runtime-core`. It decodes the image header, adjusts a RECT, uploads through `LoadImage`, and applies the observed 0x7c1f transparency-key and 0x8000 pixel-bit pass before a conditional second upload. `Invoke-FunctionMatch.ps1` matched all 456 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `af9b66e9a164f21292e523504f5ec606b639b65987ffaba86411a76545de8580`. Runtime-core now contains 15 functions and 3,884 selected bytes.
* **Startup status dispatcher reconstructed**: Added `FUN_8002263c` (104 bytes) to `src/ddr5thmix/RuntimeCore.s` and `runtime-core`. It dispatches `DAT_800e0b18+0x71` to `FUN_80026940` with the observed literals `0x110-0x112`. `Invoke-FunctionMatch.ps1` matched all 104 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; built/reference SHA-256 `f437ccbfce599b08896f8b65ce4f8188599d6ffc10ef747956dc6f6bceadf908`. Runtime-core now contains 14 functions and 3,428 selected bytes.
* **Startup display helpers reconstructed**: Added `FUN_80022570` (116 bytes), `FUN_800225e4` (32 bytes), and `FUN_80022604` (56 bytes) to `src/ddr5thmix/RuntimeCore.s` and `runtime-core`. All three passed `Invoke-FunctionMatch.ps1` with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; built/reference SHA-256s are `aab0a8352a3b63ee70b56ec2420187bac62e4c0af36419f1d8a24f989549e664`, `2bd11e27955409b780ee5216dfa5978a851e90934c0c710c3172b00e58daa35a`, and `f35b20e4d0dfd6a99b0a2c2687d2dd8fe3dadd0f7e46378416d53c21c7116c36`. Runtime-core now contains 13 functions and 3,324 selected bytes.
* **Graphics subsystem helpers reconstructed**: Added `FUN_8002200c` (300 bytes) and `FUN_80022208` (60 bytes) to `src/ddr5thmix/RuntimeCore.s` and `runtime-core`. The pair prepares two display records, dimensions, flags, half-height fields, and the remaining geometry/display setup chain. Both passed `Invoke-FunctionMatch.ps1` at 300/300 and 60/60 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; built/reference SHA-256s are `b40e25a0829aa386e0788fd11f5712f6a5834f7d6f9cd6b972fe06d3bd0ab9f2` and `b617c2849eaabaeaee146e1a524f1e0db2c7c8662d010169e659e77adc08adc3`. Runtime-core now contains 10 functions and 3,120 selected bytes.
* **Graphics startup reconstructed**: Added `FUN_80021f00` (268 bytes) to `src/ddr5thmix/RuntimeCore.s` and `runtime-core`. The function stores the 320x240 display configuration, guards the one-time `GsInitGraph` call with `DdrSecondaryState+0x01`, initializes both display records, and invokes the final startup callback. `Invoke-FunctionMatch.ps1` matched all 268 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; built/reference SHA-256 `47ebd47eb0d02349a11bab9ea0c802812e0df4d3c4dc5c34c8943cba0b9eeb19`. Runtime-core now contains 8 functions and 2,760 selected bytes.
* **Runtime initialization reconstructed**: Added `FUN_80021dfc` (260 bytes) to `src/ddr5thmix/RuntimeCore.s` and the `runtime-core` manifest module. The function initializes the game-owned primary/secondary state, clears the two startup regions, configures PAD buffers through the existing PsyQ entry points, copies the two-word startup snapshot, records the boot callback result, and runs the remaining startup callbacks. `Invoke-FunctionMatch.ps1` matched all 260 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; built/reference SHA-256 `4c32b3e6f85067a54bb9a8295ee12890e3b40b9a03890128fbe13695e374f097`. Runtime-core now contains 7 functions and 2,492 selected bytes; `git diff --check` is clean.

## 2026-08-04 (9)
* **Eighth confirmed PsyQ object boundary (`PADCMD`), a third consecutive object in the PAD cluster**: `PADCMD` (`0x8003d7d8`–`0x8003e3d4`, 3,068 bytes, 42 rows) sits immediately after `PADMAIN`, completing a three-in-a-row confirmed sequence (`PADENTRY` → `PADMAIN` → `PADCMD`). Complete byte account, with the same kind of caveat as `PADENTRY` stated plainly: three 4-byte fragments (`PADCMD_OBJ_2FC`, `PADCMD_OBJ_8B0`, `PADCMD_OBJ_B40`) are `confidence: library_signature`, not yet individually byte-matched. Base occupied directly by the independently byte-matched `_padSetAct`; measured end lands exactly on the independently byte-matched `FUN_8003e3d4`. Leading edge shows a real 12-byte zero-padding run after `_padWaitRXready` (the same shape `PADMAIN`'s own leading edge showed, a separate instance not a re-measurement); trailing edge is dense, directly onto `FUN_8003e3d4`'s own first (real, byte-matched) instruction. All four criteria met: eighth `object_boundary_confirmed` result. Updated `linked-object-evidence.md`'s scope statement and bar-definition conclusion in place, and README's target-2 status.

## 2026-08-04 (8)
* **Seventh confirmed PsyQ object boundary (`PADMAIN`), adjacent to `PADENTRY`**: `PADMAIN` (`0x8003ca08`–`0x8003d714`, 3,340 bytes, 23 rows, all `confidence: verified` with no caveats: `PadEnableCom`, `_padSetVsyncParam`, `_padChkVsync`, `_padStartCom`, `_padStopCom`, `_padInitSioMode`, `_padSioRW`, `_padSioRW2`, the gap-sweep-reconstructed `FUN_8003d154`, and fourteen `PADMAIN_OBJ_*` fragments) sits immediately after `PADENTRY` in the same PAD-driver cluster, letting the shared seam be checked from both sides. Complete byte account; base occupied directly by the independently byte-matched `PadEnableCom` (the `SYS`/`ResetGraph` base-occupancy pattern). The leading edge is not dense: `PadSetAct` ends at `0x8003c9fc`, followed by a real 12-byte zero-padding run up to the base `0x8003ca08` — a distinct gap from `PADENTRY`'s own (different) padding finding, not a duplicate. The trailing edge is dense, landing directly on `_padClrIntSio0`. All four criteria met: seventh `object_boundary_confirmed` result. Simplified README's per-object listing (now summarized by name/count rather than repeating full addresses inline, since the authoritative detail lives in `linked-object-evidence.md`) to keep it from growing unboundedly with each new confirmation. Updated `linked-object-evidence.md`'s scope statement and bar-definition conclusion in place.

## 2026-08-04 (7)
* **Sixth confirmed PsyQ object boundary (`PADENTRY`), from the PAD-driver cluster**: Picked a candidate from a third distinct library region (PAD/controller driver, not SPU or `GS_*` graphics) to keep testing the method against different parts of the executable. `PADENTRY` (`0x8003c548`–`0x8003c934`, 1,004 bytes, 20 rows: `PadChkVsync`, `PadStartCom`, `PadStopCom`, `PadChkMtap`, `PadGetState`, `PadInfoMode`, `PadInfoAct`, `PadInfoComb`, the gap-sweep-reconstructed `FUN_8003c874`, and eleven `PADENTRY_OBJ_*`/named fragments) has a complete byte account — with one honest caveat carried forward rather than smoothed over: one interior row, `PADENTRY_OBJ_164` (4 bytes), is `confidence: library_signature`, not yet individually byte-matched, only signature-identified; its bytes are still accounted for in the span, just not yet exact-matched like every other row here. The object's base is occupied directly by the independently byte-matched `PadChkVsync` (the same base-occupancy pattern as `SYS`/`ResetGraph`), and the measured end lands exactly on the independently byte-matched `PadSetActAlign`'s start. The true leading edge is **not** dense: `_remove_ChgclrPAD` ends at `0x8003c540`, followed by a real 8-byte zero-padding run up to the base at `0x8003c548`; the trailing edge is dense. All four criteria met (padded-leading/dense-trailing, the same shape `FORMAT` showed): sixth `object_boundary_confirmed` result, and the first found outside the SPU-library and `GS_*`-graphics regions. Updated `linked-object-evidence.md`'s scope statement and bar-definition conclusion in place, and README's target-2 status and Completed foundations.

## 2026-08-04 (6)
* **Fifth confirmed PsyQ object boundary (`PRESET`); third dense-edge negative result (`VSYNC`)**: Checked the remaining strongest double/triple-corroborated candidates from the broadened scan. `VSYNC` (`0x800358f8`–`0x80035b08`, 528 bytes, 4 rows) has the strongest criterion-2 signal found yet — a *triple* corroboration (`VSync` at its base, `_SN_read` immediately before, `ResetCallback` immediately after, all independently byte-matched) and a complete byte account, but both true edges turned out dense with zero padding — a third negative result, joining `BIOS_OBJ_*` and `UT_REV`. All three dense-edge negatives sit inside the same tightly packed SPU/sequencer-library region (`0x8002d9bc`–`0x80035xxx`); tentatively (not proven) this whole region may be one dense object rather than many small ones with padding at every internal boundary. Testing that idea, `PRESET` (`0x8007c4e0`–`0x8007ce44`, 2,404 bytes, 23 rows) was picked from a different region — the `GS_*` graphics library area — and found to have a complete byte account, a dense leading edge landing exactly on the independently byte-matched `GsMapModelingData`'s end, and a real 12-byte zero-padding trailing edge. One caveat recorded plainly: the row immediately after that padding (`FUN_8007ce50`) is currently `unverified`, so this edge's criterion-2 corroboration comes from the *opposite* (leading) edge rather than sitting alongside the padding itself, unlike the prior four confirmations. All four criteria still met: fifth `object_boundary_confirmed` result. Updated `linked-object-evidence.md`'s scope statement and bar-definition conclusion in place, and README's target-2 status and Completed foundations.

## 2026-08-04 (5)
* **Fourth confirmed PsyQ object boundary (`S_SCA`); broadened scan corrects an under-strict filter; second negative result (`UT_REV`)**: The first systematic scan (above) only accepted a candidate whose adjacent neighbor was `confidence: verified`, without requiring that neighbor to be a *real, independently identified* function rather than another anonymous `<name>_OBJ_*` fragment — the latter is unremarkable (two mechanically-placed fragments abutting with zero gap proves nothing extra) and doesn't match how `SYS`'s own original `ResetGraph` corroboration actually worked. Re-ran the scan requiring a real (non-`_OBJ_*`) name at the corroborating boundary; this raised the candidate pool from 2 to 57 of the 59 base-consistent multi-row runs. Checked the two strongest double-corroborated candidates end-to-end. `UT_REV` (`0x80032afc`–`0x80032b98`, 156 bytes, 4 rows, interior includes the previously gap-sweep-reconstructed `FUN_80032b84`) has a complete byte account and both its start (on `_spu_setReverbAttr`'s end) and measured end (on `SsUtGetReverbType`'s start) land exactly on independently byte-matched functions — but a direct byte dump found both true edges dense, zero padding anywhere; criterion 3 fails, recorded as a second honest negative result alongside `BIOS_OBJ_*`. `S_SCA` (`0x8002ff3c`–`0x800302b8`, 892 bytes, 23 rows: `SpuSetCommonAttr` plus 22 `S_SCA_OBJ_*` fragments) also has a complete byte account and its start lands exactly on `SsSetSerialAttr`'s end (dense); its trailing edge shows a real 4-byte zero-padding run before `SsSetMVol`'s first instruction — the same dense-leading/padded-trailing shape `SYS` showed, and incidentally the same `SsSetMVol` whose *other* edge already corroborates `SSSTART`. All four criteria met: fourth `object_boundary_confirmed` result. Updated `linked-object-evidence.md`'s scope statement and bar-definition conclusion in place, and README's target-2 status and Completed foundations.

## 2026-08-04 (4)
* **Third confirmed PsyQ object boundary (`SSSTART`); systematic scan of remaining multi-row runs; one honest negative result (`BIOS_OBJ_*`)**: Continued README's linked-object-validation target past `SYS`/`FORMAT`. Mechanically checked all 59 base-consistent multi-row `<name>_OBJ_*` runs in the symbol-map CSV for a run whose start or measured end lands exactly on an already-`verified` neighbor row; only two new candidates passed the filter, `BIOS_OBJ_*` and `SSSTART`. `BIOS_OBJ_*` (`0x8003fec8`–`0x80041628`, 5,984 bytes, 21 rows) has a complete gap-free byte account (criterion 1) and its start lands exactly on the byte-matched `CdPosToInt`'s measured end (criterion 2), but a direct byte dump of both true edges found dense real code with zero padding at either boundary — criterion 3 explicitly fails, and this is recorded as a genuine negative result (2 of 4 criteria), not a confirmed boundary. This `BIOS`-prefixed object is unrelated to the already-documented "PsyQ BIOS/kernel trampoline block" (`_card_read` and friends) in §1 — same auto-analysis label, different object. `SSSTART` (`0x8003030c`–`0x80030610`, 772 bytes, 9 rows, including the real named functions `SsStart`/`SsStart2`) also has a complete byte account and a start boundary landing exactly on the byte-matched `SsSetMVol`'s measured end; its true edges show a dense leading boundary but a real 12-byte zero-padding run on the trailing edge, immediately before the byte-matched `SsSeqCalledTbyT` — the same asymmetric one-dense/one-padded shape as `FORMAT`, mirrored. All four criteria are met, making `SSSTART` this project's third `object_boundary_confirmed` result. Updated `linked-object-evidence.md`'s scope statement and bar-definition conclusion in place, and README's target-2 status and Completed foundations.

## 2026-08-04 (3)
* **First confirmed PsyQ object boundaries: `SYS` and `FORMAT`**: Advanced README's linked-object-validation target. Re-ran `Invoke-PsyqObjectBoundaryCheck.ps1` fresh for both `SYS` (`0x800381e8`–`0x8003b114`) and `FORMAT` (`0x8003b6e8`–`0x8003ba38`) against the current symbol-map CSV after all subsequent reconstruction work (the whole-executable gap-sweep closure and `DdrSecondaryState` field sweep) — both still report `gap_bytes: 0`/`complete_byte_account: true` (12,076/12,076 and 848/848 bytes respectively). `FORMAT` had not previously had its true edges checked (only `SYS` had, in Unit A on 2026-08-03); dumping the lawful executable directly at `FORMAT`'s boundaries found 16 bytes of zero padding immediately before `0x8003b6e8` (exactly bounded by the already-verified `FUN_8003b668`'s own measured end, confirmed by arithmetic) and zero bytes of padding immediately after its measured end `0x8003ba38` (a dense boundary directly onto the already-verified `_card_read`'s first instruction). With this, both objects independently satisfy all four criteria of the falsifiable `object_boundary_confirmed` bar defined in `linked-object-evidence.md`: (1) complete zero-gap byte account, (2) an independently-derived boundary agreeing with a hand-reviewed/byte-matched function entry point — `SYS` against `ResetGraph`, `FORMAT` against `_card_read`, two unrelated objects validating the method as a real pattern rather than one coincidence, (3) a checked (non-uniform, honestly reported: 8/4-byte for `SYS`, 16/0-byte for `FORMAT`) edge-padding convention, and (4) an explicit scope disclaimer (does not establish whole-executable section ordering, PsyQ's own object format, or object-name identity). This is the project's first `object_boundary_confirmed` result; every other `<name>_OBJ_*` run remains `candidate_only`. Updated `linked-object-evidence.md`'s scope statement, the `FORMAT`/Unit B section, and the bar-definition conclusion in place; updated README's target-2 status and Completed foundations.

## 2026-08-04 (2)
* **`DdrSecondaryState` full field-xref sweep — 20 new fields resolved, struct size corrected**: Ran a full Ghidra 12.1.2 headless field/xref sweep over `PTR_DAT_800ac8ec` (`DumpFieldXrefs.java 0x800ac8ec`, the same method Unit E used for `PTR_DAT_800ac8e8`), decompiling and reading all 79 referencing functions in full rather than only the nine offsets the prior table covered. Found the struct's true size is `0xd0` (208 bytes), not the previously asserted `0xbc` (188 bytes): `FUN_80021dfc` (boot init) calls `bzero(PTR_DAT_800ac8ec, 0xd0)`, and real 4-byte accesses reach `+0xc0`–`+0xc3`, both beyond the old bound. Named 20 fields with concrete reader-and/or-writer evidence: `graphics_init_once_flag` (`+0x01`, a run-once `GsInitGraph` latch), `termination_flag` (`+0x04`, set by the shared termination-latch helper alongside `DdrModeState.termination_latch`), `cd_sync_status` (`+0x05`, written by the already-reconstructed `FUN_80020e68`), `mode4_ready_flag` (`+0x06`, already behaviorally described but unnamed), `idle_transition_flag` (`+0x07`, confirmed reader/unconfirmed writer, matching `screen_range_flag`'s evidentiary shape), `glyph_blink_timer` (`+0x08`, pairs with `DdrModeState.glyph_color` in the shared glyph-draw routine), `boot_debug_flag` (`+0x1e`) and `boot_check_value` (`+0xc0`, the raw tested value behind that boolean), `load_step_index` (`+0x40`) and `load_queue_index` (`+0x51`, together driving a 12-step/14-item mode-4 asset-loading queue with parallel stride-`0x1c` tables), and a 5-field CD-read request record (`cd_read_status_flags` `+0x77`, `cd_read_dest_end` `+0x7c`, `cd_read_dest_start` `+0x88`, `cd_read_request_size` `+0x8c`, `cd_read_padded_size` `+0x90`) written by the CD read-request initiator (`FUN_80020d24`) and consumed by the two CD callback handlers reconstructed in the 2026-08-03 startup-gap pass (`FUN_800207bc`, `FUN_80020a44`) — the `cd_read_padded_size` field also feeds directly into the mode-4 loading queue's own VRAM upload step, tying the CD subsystem and the loading queue together as one evidenced pipeline. Also found a new reader for the already-documented `+0xac`–`+0xaf` flags (they gate `FUN_80022f04`'s countdown-driven auto-transition to mode `0x10`) and seven item-specific scratch bytes (`+0x41`–`+0x44`, `+0x46`, `+0x48`, `+0x4a`) tied 1:1 to individual loading-queue items, kept `unknown_*` since no shared or per-item meaning was confirmed. Updated `src/ddr5thmix/mode_control.h`'s `DdrSecondaryState` layout and static assertions accordingly, and fixed the two dependent hand-written C reconstructions (`FUN_8002313c.c`, `FUN_800236bc.c`) that referenced the renamed/restructured fields. `mode-control`, `runtime-core`, and `screen-selector` module regressions all pass. See [DdrSecondaryState partial layout](/docs/games/ddr-5th-mix-jp-globals.md).

## 2026-08-04
* **Whole-executable gap sweep closed — 80 functions reconstructed, remaining bytes fully classified**: Continued the whole-executable gap sweep started 2026-08-03 to its conclusion. First corrected two pre-existing `library_signature` rows that `Invoke-MainExecutableGapSweep.ps1` reported as overlapping an independently verified neighbor: `VS_VH_OBJ_3FC` (declared 140 bytes from `0x800353a8`, overran `VS_VH_OBJ_434` and `SsVabTransBody` by up to 84 bytes; corrected to 56 bytes) and `PadInfoAct` (declared 132 bytes from `0x8003c7b8`, overran `PADENTRY_OBJ_2F0` by 4 bytes; corrected to 128 bytes), both re-verified `byte_match: true` at their corrected size in `src/ddr5thmix/LibrarySignatureBatch10.s`. With `overlap_count: 0`, the sweep reported 255 gaps (3,143 bytes) outside the two already-classified giant regions: 181 gaps / 1,176 bytes fully zero (alignment padding) and 74 gaps / 1,967 bytes with non-zero content. Each non-zero gap is bounded on both sides by an already-verified or already-catalogued neighbor row (unlike the earlier 135-gap candidate pool, no boundary guessing was needed). Disassembling each with `mipsel-none-elf-objdump -D -z -b binary -m mips:3000 -EL -M no-aliases` found: 4 gaps (59 bytes) are the already-documented PsyQ `"Ps"` object-header marker pattern (`0x800365ac`, `0x8003ba64`, `0x8003bb24`, plus the pre-existing `0x800207ad` prefix), not reconstructed; 3 gaps contain a clean function followed by zero-alignment padding (function reconstructed, padding left uncatalogued); and the remaining 70 gaps (1,880 bytes total across all reconstructed functions) decoded as complete, plausible MIPS and were segmented into 80 named functions (`FUN_<address>`), some gaps yielding 2-4 adjacent functions. All 80 were hand-assembled in `src/ddr5thmix/GapSweepBatch1.s` and verified with `Invoke-FunctionMatch.ps1` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43: **80/80 `byte_match: true` on the first full verification pass**, no defects. All 80 rows promoted to `confidence=verified`/`source_status=hand_written_source` with per-row reference/built SHA-256 evidence. `screen-selector`, `mode-control`, and `runtime-core` module regressions and `git diff --check` all pass. Main-executable verified totals move from 1,278/2,427 to **1,358/2,507** functions and from 167,276 to **169,068** exact bytes (169,068 reflects the 80 new functions plus the −88-byte net effect of the two size corrections); overall exact reconstruction moves to **1,428 functions / 178,440 selected bytes**. Re-running the gap sweep confirms full closure: outside the two already-documented giant non-code regions, the main-executable code region has **zero unclassified bytes** — only 1,204 bytes of zero-alignment padding and 59 bytes of already-evidenced PsyQ markers remain, neither of which are reconstruction targets. See [linked-object evidence §5](/docs/foundations/linked-object-evidence.md).

## 2026-08-03
* **Startup-gap reconstruction — three functions verified**: Reconstructed `FUN_800207bc` (648 bytes), `FUN_80020a44` (692 bytes), and `FUN_80020cf8` (44 bytes) as hand-written MIPS in `src/ddr5thmix/GapStartup.s`. `Invoke-FunctionMatch.ps1` matched all 1,384/1,384 bytes against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061ef16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43; manifest and symbol-map rows carry equal per-function reference/built SHA-256 pairs. Verified main-executable totals move to 1,278/2,427 functions and 167,276 exact bytes; overall exact reconstruction is 1,348 functions / 176,648 selected bytes. The 15-byte marker/data prefix at `0x800207ad` remains intentionally unattributed.
* **Unattributed-byte pass — startup gap catalogued**: Reproduced the hash-gated gap sweep and classified `0x800207ad`–`0x80020d24` (1,399 bytes) as 15 bytes of marker/data followed by three complete MIPS functions: `FUN_800207bc` (648), `FUN_80020a44` (692), and `FUN_80020cf8` (44). Added the three boundaries as `manual`/`disassembly_reviewed`; no exact byte match is claimed. Remaining whole-region gap decreases from 4,523 to 3,139 bytes. See [/docs/foundations/linked-object-evidence.md](/docs/foundations/linked-object-evidence.md).
* **Unit E — remaining global-layout audit**: A reproducible Ghidra 12.1.2 headless DumpFieldXrefs.java pass over PTR_DAT_800ac8e8 found 62 referencing functions. Promoted four behaviorally evidenced fields in src/ddr5thmix/mode_control.h: +0xf2 alternate input mapping flag, +0xf3 input mapping enabled, +0x102 mapping variant flag, and +0x103 button-layout variant. No further stable semantics were justified; remaining unknowns stay conservative. See [/docs/games/ddr-5th-mix-jp-globals.md](/docs/games/ddr-5th-mix-jp-globals.md).


* **Unit C audit**: Re-ran the two existing BIOS stub matches (`FUN_8003ba70` and `FUN_8003bb30`): both remain `byte_match: True` at 12 bytes, and a lawful-executable scan found the repeated `50 73 <tag> 00 00 00 44 00` marker form at both prefixes and multiple independent addresses. The former unexplained-prefix wording is superseded; the exact object-file semantics remain unproven. See [linked-object evidence](/docs/foundations/linked-object-evidence.md).
* **Unit A**: Re-ran the SYS/ResetGraph object checker at current HEAD: 68 `SYS_OBJ_*` rows and 98 total intervals cover all 12,076 bytes with one implied base and zero gaps. The historical 116/36/120-byte gaps are now `SYS_OBJ_F80`/`SYS_OBJ_22F4`/`SYS_OBJ_2828`, each reproduced with `byte_match: True`; edge bytes show 8 zero bytes before the object and 4 after it, correcting the prior symmetric-edge wording. See [linked-object evidence](/docs/foundations/linked-object-evidence.md).
* **Audit**: Classified the 14 remaining real-address `library_signature` rows: one `2MBYTE_OBJ_B4` data/object marker, twelve contiguous internal object fragments containing instruction words, and one zero-word alignment fragment; none is an independent reconstruction target. See [short library fragment audit](/docs/foundations/short-library-fragment-audit.md).
* **Library signature reconstruction batch 12 — completed and verified**: Directly reconstructed the final 15 real `library_signature` main-executable rows of 512+ bytes (11,152 selected bytes; inventory SHA-256 `2b9951586ea27c0c26a8080ccd8044041c9dc3156af99cb23b699776e3603d08`) from lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061ef16198c27fd13c950003d94c4aee` using GCC 14.2.0/binutils 2.43. The `objdump -z -M no-aliases` disassembly was assembled as hand-written MIPS with real load-address linking; one `s8` register spelling was normalized after the first assembler pass. All 15/15 functions passed `byte_match: true`; manifest and CSV rows now carry per-function reference/built SHA-256 evidence and `confidence = verified`/`source_status = hand_written_source`. Main-executable verified totals move from 1,260 to **1,275**; `library_signature` decreases from 182 to **167**, exactly the synthetic GTE-macro remainder. Exact selected reconstruction moves from 1,330 to **1,345 functions / 175,264 bytes** overall. `screen-selector`, `mode-control`, and `runtime-core` regressions pass; `git diff --check` passes.
* **Synthetic GTE macro audit**: Audited the 167 remaining `library_signature` rows after Batch 12 and separated the 153 `gte_*` pseudo-functions at `0x20000000`–`0x20000260` (1-byte synthetic entries, 40 aggregate caller edges, 24 with recorded callers and 129 without) from 14 short rows at real `0x800...` addresses. The 153 macros are analysis cross-reference artifacts, not PS-X EXE bytes or reconstructible MIPS functions; the 14 real-address rows remain a separate object-boundary/data-classification task. See [Synthetic GTE macro segment](/docs/tooling/gte-macro-segment.md).

* **Library signature reconstruction batch 11 — executed directly and verified**: Continued directly executing batches per the repository owner's standing direction (no external agent unless explicitly requested). Reused the Batch 10 disassemble-and-reassemble generator unchanged for the fourth sliced sub-range of the 65-byte-and-larger backlog: all 75 owned 256–511-byte `library_signature` rows at fixed base `2b7c9d7` (25,920 bytes; canonical inventory SHA-256 `ea632cae...`). No generator changes were needed at this size tier: no COP0 symbolic registers, no unresolved cross-reference tokens, and the `-z` objdump flag (already required for Batch 10) continued to prevent repeated-instruction elision. 75/75 `byte_match: true` on the first full verification pass, no defects, via `Invoke-FunctionMatch.ps1` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43. All 75 CSV rows promoted to `confidence = verified`/`source_status = hand_written_source` with per-row reference/built SHA-256 evidence. `screen-selector`, `mode-control`, and `runtime-core` module regressions and `git diff --check` all pass. Main-executable verified totals move from 1,185 to **1,260** (48.9% to **52.0%** of 2,424 cataloged rows; 12.3% to **14.7%** of the 1,050,624-byte code region), `library_signature` from 257 to **182** (167 anomalous synthetic GTE-macro rows at `0x2000...` pseudo-addresses and 15 rows of 512+ bytes remain, none yet batched). Overall exact reconstruction moves to **1,330 functions / 164,112 selected bytes**.
* **Library signature reconstruction batch 10 — executed directly and verified**: At the repository owner's explicit direction ("hazlo, el agente externo no se usa a menos que sea explicitamente requerido"), executed the Batch 10 package directly in the integrator's working tree instead of delegating it, from fixed base `a1043e6`. Built a disassemble-and-reassemble generator (raw executable byte slice -> `mipsel-none-elf-objdump -D -b binary -M no-aliases -z --adjust-vma=<address>` -> GNU-`as`-compatible source) covering all 151 owned 129–255-byte rows. The `-z` flag was required to stop objdump eliding repeated identical instruction words (mostly `sll $zero,$zero,0x0` padding runs) as `...`, which had silently dropped 4 instructions from one function's first attempt. Cross-references were resolved against an address->name map built from every already-`verified` CSV row plus this batch's own 151 names, falling back to generic `sym_<address>` placeholders per the established convention; every entry carries a `symbols` property (empty where unused) per Batch 9's linked-placement finding. No COP0 symbolic register names or `.word` opcode transcription were needed at this tier; GTE COP2 register operands and `break` printed in directly assemblable numeric form. First full run: 151/151 `byte_match: true` on the first pass with no defects, verified per-function via `Invoke-FunctionMatch.ps1` against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee` with GCC 14.2.0/binutils 2.43. All 151 CSV rows promoted to `confidence = verified`/`source_status = hand_written_source` with per-row reference/built SHA-256 evidence. `screen-selector`, `mode-control`, and `runtime-core` module regressions and `git diff --check` all pass. Main-executable verified totals move from 1,034 to **1,185** (42.7% to **48.9%** of 2,424 cataloged rows; 9.7% to **12.3%** of the 1,050,624-byte code region), `library_signature` from 408 to **257** (167 anomalous 1-/4-byte rows and 90 rows of 256+ bytes remain, none yet batched). Overall exact reconstruction moves to **1,255 functions / 138,192 selected bytes**.

## 2026-07-29
* **Library signature reconstruction batch 9 — independently audited and accepted for integration**: From a clean detached checkout of candidate `cb2852f`, reproduced the fixed-base inventory (297 functions / 58,952 bytes / SHA-256 `3d929049...`), exact identity-set equality across the base inventory, manifest entries, assembly sections, and promoted CSV rows, and all 297 individual byte matches against lawful `SLPM_868.97_1` SHA-256 `4e0308ca...` with GCC 14.2.0/binutils 2.43. No missing/unequal manifest hashes, `move` pseudo-instructions, raw opcode transcription, symbolic COP0 register operands, `git diff --check` failures, duplicate manifest entries, or prohibited artifacts were found. Confirmed `screen-selector`, `mode-control`, and `runtime-core` module regressions pass. Accepted totals: main-executable verified 1,034/2,424 (42.7% of cataloged rows; 9.7% of the code region), 1,104 functions / 111,236 selected bytes overall.
* **Library signature reconstruction batch 9 — candidate delivered**: Reconstructed, configured, and verified 297 hand-written MIPS assembly function stubs in `src/ddr5thmix/LibrarySignatureBatch9.s` for every `tool_heuristic` row discovered by the gap sweep (fixed base `8f431ff`, canonical inventory SHA-256 `3d929049...`), spanning the full size range (1 to 936+ bytes) rather than one tier. New instruction classes appeared at this scale: `lwc2`/`swc2`/`cfc2` (GTE data transfer) and raw `c2 0xNNNNNN` GTE macro opcodes, both of which GNU `as` accepts directly in the exact form GNU objdump's `-M no-aliases` prints them, needing no special handling. First verification pass: 281/297 passed; the 16 failures all shared one root cause, found and fixed: every failing function contained a `j` to a *local* label (a target within the function's own address range) with no manifest `symbols` entry, so `Invoke-FunctionMatch.ps1` took the unlinked direct-objcopy path, and GNU `as` encodes an absolute J-type jump target as if the section's base address were `0x0` rather than its real load address unless the linker places it — silently producing a wrong-but-plausible-looking absolute target. Adding an empty `"symbols": {}` to each affected manifest entry (sufficient to force the linked-placement code path per `Invoke-FunctionMatch.ps1`'s existing property-presence check) fixed all 16 with no source changes. All 297 functions matched thereafter; none of the 12 rows flagged with a less-certain upper bound needed a boundary correction — their declared sizes were already correct. All 297 manifest entries carry equal reference/built SHA-256 pairs; all 297 CSV rows promoted to `confidence = verified`/`source_status = hand_written_source` with per-row durable evidence. Verified exact identity-set equality across the fixed-base inventory, manifest, assembly sections, and promoted CSV rows (297/297, no duplicates). `screen-selector`, `mode-control`, and `runtime-core` module regressions pass, and `git diff --check` is clean. Main-executable totals move from 737 to 1,034 verified functions (30.4% to 42.7% of 2,424 cataloged rows; 4.1% to 9.7% of the 1,050,624-byte code region), `tool_heuristic` from 297 to 0, and exact main-executable bytes from 42,912 to 101,864 (1,104 functions / 111,236 selected bytes overall). Delivered to `agent/library-signature-reconstruction-batch9`; remains `candidate_for_audit` pending a separate clean-checkout audit per `docs/workflows/external-agent-acceptance.md`.
* **External library-signature reconstruction work package — Batch 9 ready**: Defined a fixed-base, isolated ninth delegation pass for all 297 `tool_heuristic` rows this project itself discovered via the whole-executable gap sweep (58,952 bytes at commit `8f431ff`, canonical inventory SHA-256 `3d929049...`). Unlike Batches 3–8, this package explicitly allows and expects boundary corrections (adjusting a CSV `size`) for a subset of the 12 rows flagged with a less-certain upper bound, mirroring how Batch 4 fixed `SYS_OBJ_181C`'s wrong immediate — any such fix must be explicit and evidenced, never a silent byte absorption. Encodes every prior recovery finding (required `.set` block, manifest-before-verification ordering, `-M no-aliases` disassembly, numeric COP0 register operands, `symbols`-backed cross-section references) as mandatory pre-delivery rejection gates. Delivery remains `candidate_for_audit` until an independent clean-checkout audit accepts it.
* **Gap-sweep cataloguing pass: 297 new tool_heuristic functions from the 65+-byte candidate gaps**: Followed up the whole-executable gap sweep by disassembling all 135 candidate gaps (`-M no-aliases`) and scoring recognized-mnemonic density; 133/135 scored 90%+ on the first pass, and the 2 exceptions were manually confirmed as false negatives (one gap sits at a misaligned address immediately after the existing one-byte `2MBYTE_OBJ_B4` marker; the other is a large COP2/GTE-using function outside the scoring whitelist, visually confirmed as a normal function prologue). Segmented function boundaries within each gap on every `jr` instruction (any target register, to catch the BIOS-vector-call trampoline idiom too) plus its delay slot; testing showed also splitting on `j` over-splits roughly 2x since most `j` instructions in this codebase are internal control flow, not returns. Produced 297 new rows (58,952 bytes) covering 134 of the 135 gaps, added at `confidence = tool_heuristic`/`source_status = decompiler_output_only`, named `FUN_<address>`, each with evidence recording the sweep+segmentation method; 12 rows are flagged as having a less-certain upper bound (the entire remainder after a gap's last detected `jr`, not a confirmed local return). Verified no duplicate/colliding addresses and that per-gap byte sums fully account for all 134 processed gaps. Left open: the misaligned `0x800207ad` gap (1,399 bytes) and 4,523 bytes still fully unaccounted for in the code region. Updated the main-executable inventory to 2,424 rows and the README's coverage figures to state both the function-count percentage (30.4%) and the true code-region byte percentage (4.1% exact-reconstructed of 1,050,624 bytes) side by side, after confirming they diverge enough to be actively misleading if only one is quoted.
* **Whole-executable gap sweep: the unattributed 51% is mostly two known regions, not 540KB of missing functions**: Added `tools/build/Invoke-MainExecutableGapSweep.ps1`, which merges every `overlay==main` symbol-map row into covered intervals across the full code region (`0x8001a800`–`0x8011b000`) and classifies the zero/non-zero byte split of every gap directly from the hash-gated executable. Of 384 gaps / 540,259 unattributed bytes, one gap (`0x800ac888`–`0x8011b000`, 452,472 bytes, the entire final 43.1% of the file) and a second (`0x8001a800`–`0x800206f8`, 24,312 bytes, everything before `start`) together account for 476,784 bytes (88.2%). The trailing gap splits into 199,284 non-zero bytes (sampling suggests embedded resource/asset data — one 24-byte sample decodes as the ASCII string `MENU`; suspected, not confirmed) and a 222,455-byte zero run at `0x800e2931` that closely (within 0.4%) matches this project's already-documented `start`-function BSS-clear range (`DAT_800e2938`..`UNK_80118e28`), meaning roughly half of the giant gap is already-understood zero-initialized storage, not unknown content. The leading gap reads as a debug string table (`NOW LOADING.`, `PLEASE WAIT...`, a `printf`-style format string) plus a pointer/jump table into `0x800aXXXX`, not code. That leaves only 135 gaps / 60,391 bytes of 65+ bytes as the real candidate pool for un-inventoried code — comparable in scale to the 510 functions / 25,112 bytes already covered by Batches 3–8. Also surfaced (and left unfixed, flagged for a future pass): two pre-existing `library_signature` rows (`VS_VH_OBJ_3FC`, `PadInfoAct`) whose auto-analysis-derived sizes overrun a since-verified neighbor by 84 and 4 bytes respectively.
* **Library signature reconstruction batch 8 — independently audited and accepted for integration**: From a clean detached checkout of candidate `b4fe09d`, reproduced the fixed-base inventory (85 functions / 9,612 bytes / SHA-256 `96e24bd4...`), exact identity-set equality across the base inventory, manifest entries, assembly sections, and promoted CSV rows, and all 85 individual byte matches against lawful `SLPM_868.97_1` SHA-256 `4e0308ca...` with GCC 14.2.0/binutils 2.43. No missing/unequal manifest hashes, `move` pseudo-instructions, raw opcode transcription, symbolic COP0 register operands, `git diff --check` failures, duplicate manifest entries, or prohibited artifacts were found. Confirmed `screen-selector`, `mode-control`, and `runtime-core` module regressions pass. Accepted totals: main-executable verified 737/2,127 (34.6%), 807 functions / 52,284 selected bytes overall.
* **Library signature reconstruction batch 8 — candidate delivered**: Reconstructed, configured, and verified 85 hand-written MIPS assembly function stubs in `src/ddr5thmix/LibrarySignatureBatch8.s` for the 97–128-byte `library_signature`/`decompiler_output_only` main-executable tier (fixed base `9f7a62b`, canonical inventory SHA-256 `96e24bd4...`), the second sliced sub-range of the 65-byte-and-larger backlog and the largest/most instruction-varied tier attempted mechanically so far (introducing `mult`/`multu`/`divu`/`mfhi`/`mflo`, `nor`/`sub`/`addi`, and COP0/COP2 register-move instructions `mfc0`/`mtc0`/`mfc2`/`mtc2`/`ctc2`). Found and fixed one new defect before verification: GNU objdump's `-M no-aliases` disassembly rendered `mfc0`/`mtc0`'s COP0 register operand as the symbolic name `c0_sr` rather than a numeric register, which GNU `as` rejects outright (`invalid operands`); substituted the equivalent `$12` and confirmed it assembles and matches. Applied every other rejection gate encoded from prior recoveries (`.set noreorder`/`noat`/`nomacro` header, manifest entries added before verification, `symbols`-backed cross-section references, no trailing blank line). All 85 functions matched on the first verification pass thereafter; all 85 manifest entries carry equal reference/built SHA-256 pairs; all 85 CSV rows promoted to `confidence = verified`/`source_status = hand_written_source` with per-row durable evidence. Verified exact identity-set equality across the fixed-base inventory, manifest, assembly sections, and promoted CSV rows (85/85, no duplicates). `screen-selector`, `mode-control`, and `runtime-core` module regressions pass, and `git diff --check` is clean. Main-executable totals move from 652 to 737 verified functions (30.7% to 34.6%), `library_signature` from 493 to 408, `decompiler_output_only` from 1,373 to 1,288, and exact main-executable bytes from 33,300 to 42,912 (807 functions / 52,284 selected bytes overall). Delivered to `agent/library-signature-reconstruction-batch8`; remains `candidate_for_audit` pending a separate clean-checkout audit per `docs/workflows/external-agent-acceptance.md`.
* **External library-signature reconstruction work package — Batch 8 ready**: Defined a fixed-base, isolated eighth delegation pass for the second sliced sub-range of the 65-byte-and-larger backlog: the 85 unique, named, 97–128-byte main-executable rows still classified `library_signature`/`decompiler_output_only` at commit `9f7a62b` (9,612 bytes; canonical inventory SHA-256 `96e24bd4...`). Encodes all prior recovery findings as mandatory pre-delivery rejection gates. Delivery remains `candidate_for_audit` until an independent clean-checkout audit accepts it.
* **Library signature reconstruction batch 7 — independently audited and accepted for integration**: From a clean detached checkout of candidate `5c54a82`, reproduced the fixed-base inventory (65 functions / 5,300 bytes / SHA-256 `03a34c9d...`), exact identity-set equality across the base inventory, manifest entries, assembly sections, and promoted CSV rows, and all 65 individual byte matches against lawful `SLPM_868.97_1` SHA-256 `4e0308ca...` with GCC 14.2.0/binutils 2.43. No missing/unequal manifest hashes, `move` pseudo-instructions, raw opcode transcription, out-of-range local labels, `git diff --check` failures, duplicate manifest entries, or prohibited artifacts were found — a second consecutive clean audit with no corrections. Confirmed `screen-selector`, `mode-control`, and `runtime-core` module regressions pass. Accepted totals: main-executable verified 652/2,127 (30.7%), 722 functions / 42,672 selected bytes overall.
* **Library signature reconstruction batch 7 — candidate delivered**: Reconstructed, configured, and verified 65 hand-written MIPS assembly function stubs in `src/ddr5thmix/LibrarySignatureBatch7.s` for the 65–96-byte `library_signature`/`decompiler_output_only` main-executable tier (fixed base `bc837fb`, canonical inventory SHA-256 `03a34c9d...`), the first sliced sub-range of the 65-byte-and-larger backlog. Applied every rejection gate encoded into the Batch 7 package from prior recoveries — `.set noreorder`/`noat`/`nomacro` header, manifest entries added before the first verification pass (Batch 6's own first-pass failure mode), `-M no-aliases` disassembly, `symbols`-backed cross-section references, no trailing blank line — plus the new check for this larger size tier: every local-label target re-verified to fall strictly within `[0, size)`. All 65 functions matched on the first verification pass with no defects; all 65 manifest entries carry equal reference/built SHA-256 pairs; all 65 CSV rows promoted to `confidence = verified`/`source_status = hand_written_source` with per-row durable evidence. Verified exact identity-set equality across the fixed-base inventory, manifest, assembly sections, and promoted CSV rows (65/65, no duplicates). `screen-selector`, `mode-control`, and `runtime-core` module regressions pass, and `git diff --check` is clean. Main-executable totals move from 587 to 652 verified functions (27.6% to 30.7%), `library_signature` from 558 to 493, `decompiler_output_only` from 1,438 to 1,373, and exact main-executable bytes from 28,000 to 33,300 (722 functions / 42,672 selected bytes overall). Delivered to `agent/library-signature-reconstruction-batch7`; remains `candidate_for_audit` pending a separate clean-checkout audit per `docs/workflows/external-agent-acceptance.md`.
* **External library-signature reconstruction work package — Batch 7 ready**: Defined a fixed-base, isolated seventh delegation pass for the first sliced sub-range of the 65-byte-and-larger backlog (391 functions total, too large for one package): the 65 unique, named, 65–96-byte main-executable rows still classified `library_signature`/`decompiler_output_only` at commit `bc837fb` (5,300 bytes; canonical inventory SHA-256 `03a34c9d...`). Encodes all prior recovery findings as mandatory pre-delivery rejection gates (required `.set noreorder`/`noat`/`nomacro` block, manifest entries landing before verification rather than after, `-M no-aliases` disassembly, `symbols`-backed cross-section references, no trailing blank line) plus a new check specific to this larger size tier: local-label targets must be re-verified to fall strictly within `[0, size)` now that functions are large enough to plausibly contain internal loops. Delivery remains `candidate_for_audit` until an independent clean-checkout audit accepts it.
* **Library signature reconstruction batch 6 — independently audited and accepted for integration**: From a clean detached checkout of candidate `69a14e5`, reproduced the fixed-base inventory (46 functions / 2,612 bytes / SHA-256 `70c6e2e5...`), exact identity-set equality across the base inventory, manifest entries, assembly sections, and promoted CSV rows, and all 46 individual byte matches against lawful `SLPM_868.97_1` SHA-256 `4e0308ca...` with GCC 14.2.0/binutils 2.43. No missing/unequal manifest hashes, `move` pseudo-instructions, raw opcode transcription, trailing-whitespace `git diff --check` failures, duplicate manifest entries, or prohibited artifacts were found — the first Batch delivery with a clean audit on every gate. Confirmed `screen-selector`, `mode-control`, and `runtime-core` module regressions pass. Accepted totals: main-executable verified 587/2,127 (27.6%), 657 functions / 37,372 selected bytes overall.
* **Library signature reconstruction batch 6 — candidate delivered**: Reconstructed, configured, and verified 46 hand-written MIPS assembly function stubs in `src/ddr5thmix/LibrarySignatureBatch6.s` for the 49–64-byte `library_signature`/`decompiler_output_only` main-executable tier (fixed base `98dc530`, canonical inventory SHA-256 `70c6e2e5...`), applying every rejection gate encoded into the Batch 6 package from Batch 5's recovery: the source opens with `.set noreorder`/`.set noat`/`.set nomacro`, disassembly used `-M no-aliases`, cross-section branch/jump targets resolve through manifest `symbols` entries (real Batch-6 names where the target is another owned function, `sym_XXXXXXXX` placeholders otherwise), and the generator strips the trailing-blank-line defect found auditing Batch 5. All 46 functions matched on the first verification pass with no defects; all 46 manifest entries carry equal reference/built SHA-256 pairs; all 46 CSV rows promoted to `confidence = verified`/`source_status = hand_written_source` with per-row durable evidence. Verified exact identity-set equality across the fixed-base inventory, manifest, assembly sections, and promoted CSV rows (46/46, no duplicates). `screen-selector`, `mode-control`, and `runtime-core` module regressions pass, and `git diff --check` is clean. Main-executable totals move from 541 to 587 verified functions (25.4% to 27.6%), `library_signature` from 604 to 558, `decompiler_output_only` from 1,484 to 1,438, and exact main-executable bytes from 25,388 to 28,000 (657 functions / 37,372 selected bytes overall). Delivered to `agent/library-signature-reconstruction-batch6`; remains `candidate_for_audit` pending a separate clean-checkout audit per `docs/workflows/external-agent-acceptance.md`.
* **External library-signature reconstruction work package — Batch 6 ready**: Defined a fixed-base, isolated sixth delegation pass for the 46 unique, named, 49–64-byte main-executable rows still classified `library_signature`/`decompiler_output_only` at commit `98dc530` (2,612 bytes; canonical inventory SHA-256 `70c6e2e5...`). The package directly encodes Batch 5's recovery findings as mandatory pre-delivery rejection gates: the source must open with `.set noreorder`/`.set noat`/`.set nomacro` (its omission caused 89/92 first-pass mismatches in Batch 5), disassembly must use `-M no-aliases` to avoid the `move`-pseudo ambiguity, and cross-section branch/jump targets must resolve through the manifest's `symbols` mechanism rather than raw numbers. Delivery remains `candidate_for_audit` until an independent clean-checkout audit accepts it.
* **Library signature reconstruction batch 5 — independently audited and accepted for integration**: From a clean detached checkout of candidate `ea24543`, reproduced the fixed-base inventory (92 functions / 3,920 bytes / SHA-256 `e1da8b76...`), exact identity-set equality across the base inventory, manifest entries, assembly sections, and promoted CSV rows, and all 92 individual byte matches against lawful `SLPM_868.97_1` SHA-256 `4e0308ca...` with GCC 14.2.0/binutils 2.43. The only defect found was a stray blank line at the end of `src/ddr5thmix/LibrarySignatureBatch5.s` tripping `git diff --check`; removed it and reconfirmed all 92 byte matches. No missing/unequal manifest hashes, `move` pseudo-instructions, raw opcode transcription, duplicate manifest entries, or prohibited artifacts were found. Confirmed `screen-selector`, `mode-control`, and `runtime-core` module regressions pass. Accepted totals: main-executable verified 541/2,127 (25.4%), 611 functions / 34,760 selected bytes overall.
* **Library signature reconstruction batch 5 — candidate delivered**: Reconstructed, configured, and verified 92 hand-written MIPS assembly function stubs in `src/ddr5thmix/LibrarySignatureBatch5.s` for the 33–48-byte `library_signature`/`decompiler_output_only` main-executable tier (fixed base `6b082cf`, canonical inventory SHA-256 `e1da8b76...`). Extracted raw executable byte slices directly and disassembled them with `-M no-aliases` to avoid the `move`-pseudo ambiguity found while auditing Batch 4; cross-references (branches and jumps landing outside a function's own byte range) were resolved against either another owned Batch-5 function's real name or a generic `sym_XXXXXXXX` placeholder, each backed by a manifest `symbols` entry supplying its runtime address for linking, matching the established `S_STM_OBJ_1C`-style precedent. The first verification pass found 89/92 mismatches, all traced to one omitted directive block: the source lacked `.set noreorder` / `.set noat` / `.set nomacro`, so the assembler's default reorder/macro-expansion mode inserted extra instructions; adding the three directives (present in every prior batch source) fixed all 92 on the next pass with no further defects. All 92 manifest entries carry equal reference/built SHA-256 pairs; all 92 CSV rows promoted to `confidence = verified`/`source_status = hand_written_source` with per-row durable evidence recorded directly in `verification_evidence`. Verified exact identity-set equality across the fixed-base inventory, manifest, assembly sections, and promoted CSV rows (92/92, no duplicates). `screen-selector`, `mode-control`, and `runtime-core` module regressions pass. Main-executable totals move from 449 to 541 verified functions (21.1% to 25.4%), `library_signature` from 696 to 604, `decompiler_output_only` from 1,576 to 1,484, and exact main-executable bytes from 21,468 to 25,388 (611 functions / 34,760 selected bytes overall). Delivered to `agent/library-signature-reconstruction-batch5`; remains `candidate_for_audit` pending a separate clean-checkout audit per `docs/workflows/external-agent-acceptance.md`.
* **External library-signature reconstruction work package — Batch 5 ready**: Defined a fixed-base, isolated fifth delegation pass for the 92 unique, named, 33–48-byte main-executable rows still classified `library_signature`/`decompiler_output_only` at commit `6b082cf` (3,920 bytes; canonical inventory SHA-256 `e1da8b76...`). The package excludes synthetic GTE macro entries, the 14 anomalous under-8-byte entries, and functions outside the range, restricts delivery to one new assembly source plus the existing manifest/inventory/README/log paths, requires 92 individual hash-gated byte matches and identity-set equality, and directly encodes Batch 4's recovery findings as mandatory pre-delivery rejection gates: no `move` pseudo-instruction (GNU binutils 2.43 expands it to `or`, not the lawful binary's `addu $rd,$rs,$zero` form) and no raw `.byte`/`.word` opcode transcription. Delivery remains `candidate_for_audit` until an independent clean-checkout audit accepts it.
* **Library signature reconstruction batch 4 — independently audited and accepted for integration**: From a clean detached checkout of candidate `b4529bb`, reproduced the fixed-base inventory (82 functions / 2,104 bytes / SHA-256 `72bfc470...`), exact identity-set equality across the base inventory, manifest entries, assembly sections, and promoted CSV rows, and all 82 individual byte matches against lawful `SLPM_868.97_1` SHA-256 `4e0308ca...` with GCC 14.2.0/binutils 2.43. No missing or unequal manifest hashes, no duplicate manifest names/addresses, and no unexpected committed path or prohibited artifact were found. Confirmed `screen-selector`, `mode-control`, and `runtime-core` module regressions pass and `git diff --check` is clean. Accepted totals: main-executable verified 449/2,127 (21.1%), 519 functions / 30,840 selected bytes overall.
* **Library signature reconstruction batch 4 — candidate delivered after external delegation failure**: The external agent run delegated to execute `docs/workflows/external-agent-library-signature-batch4.md` (fixed base `05cd68c`) failed mid-task, leaving an isolated worktree with a complete but unverified 82-function assembly source and manifest entries lacking hashes, never committed or pushed. Continued the package directly from that worktree rather than re-delegating. Running `Invoke-FunctionMatch.ps1` on all 82 owned functions found 11 mismatches and traced both root causes: ten functions used the assembler's `move` pseudo-instruction, which GNU binutils 2.43 always expands to `or $rd,$rs,$zero` (funct `0x25`), while the lawful binary requires the explicit `addu $rd,$rs,$zero` (funct `0x21`) form; the eleventh (`SYS_OBJ_181C`) had a transcribed `lw` immediate of `-0x72bc` where raw executable bytes confirmed `-0x727c`. After correcting both defect classes in `src/ddr5thmix/LibrarySignatureBatch4.s`, all 82/82 functions matched with equal reference/built SHA-256 pairs, recorded in the manifest, and all 82 CSV rows were promoted to `confidence = verified`/`source_status = hand_written_source` with per-row durable evidence (command, lawful executable hash, toolchain versions, byte count, matching hash). `screen-selector`, `mode-control`, and `runtime-core` module regressions still pass. Main-executable totals move from 367 to 449 verified functions (17.3% to 21.1%), `library_signature` from 778 to 696, `decompiler_output_only` from 1,658 to 1,576, and exact main-executable bytes from 19,364 to 21,468 (519 functions / 30,840 selected bytes overall). Delivered to `agent/library-signature-reconstruction-batch4`; remains `candidate_for_audit` pending a separate clean-checkout audit per `docs/workflows/external-agent-acceptance.md`.
* **External library-signature reconstruction work package — Batch 4 ready**: Defined a fixed-base, isolated fourth delegation pass for the 82 unique, named, 20–32-byte main-executable rows still classified `library_signature`/`decompiler_output_only` at commit `05cd68c` (2,104 bytes; canonical inventory SHA-256 `72bfc470...`). The package deliberately excludes anomalous 1–4-byte entries and functions of 36 bytes or more, restricts delivery to one new assembly source plus the existing manifest/inventory/README/log paths, requires 82 individual full-boundary byte matches, equal manifest hash pairs, exact identity-set equality, durable per-row evidence, and regressions for three existing modules. It directly encodes the prior audit's missing-hash and omitted-delay-slot failures as mandatory pre-delivery rejection gates. Delivery remains `candidate_for_audit` until an independent clean-checkout audit accepts it.
* **DDR 5th Mix linked music-database semantics**: Closed the 47-versus-50 table discrepancy with hash-gated executable evidence. The 47 `music_info` records map exactly, in menu-code order, to resource slots 0–46; slots 47–48 (`0x0132`/`0x0133`) have loadable two-word descriptors but no playable metadata record, while slot 49 (`0x0134`) is null. Extended `Export-MusicDatabase.ps1` to verify and emit that mapping, descriptor pointers/words, `resource_slot`, `artwork_selector`, and the consumer-confirmed `is_long_song` flag. Consumer sweeps resolved `+0x02` as a signed artwork selector, bounded `+0x04` to a playback/load selector path, proved `+0x06` is only used as a nonnegative sentinel on its observed path, and identified `flags_0c & 0x0100` as the long-format multiplier; `+0x0e` and `+0x10` remain conservative negative results. Added reusable Ghidra caller and scalar-operand reports and regenerated the 47-row CSV (SHA-256 `2e97b84d...`).
* **Library signature reconstruction batch 3 — independently audited and accepted for integration**: From a clean detached checkout of candidate `55d4127`, reproduced the fixed inventory (140 functions / 1,564 bytes / SHA-256 `ede6b69b...`), exact identity equality across inventory, manifest, source sections, generated reports, and promoted rows, and all 140 individual byte matches against lawful `SLPM_868.97_1` SHA-256 `4e0308ca...` with GCC 14.2.0/binutils 2.43. The audit found and corrected two evidence defects before acceptance: all 140 new manifest entries omitted the required reference/built hashes, and a pre-existing 136-byte prefix match for `FUN_80075af8` omitted its `li $v0, 5` return delay slot despite the function's 140-byte CSV boundary. Added the 140 reproduced hash pairs to the manifest and row evidence, restored and matched the complete router (SHA-256 `51814c35...`), and updated `screen-selector` from 2,344 to 2,348 exact bytes. Re-ran `screen-selector`, `mode-control`, and `runtime-core` successfully; the corrected manifest now mechanically sums to the claimed 367 functions / 19,364 main-executable bytes and 437 / 28,736 overall. No binary diff, prohibited source construct, unexpected candidate path, or tracked generated/reference artifact was found.
* **Library signature reconstruction work package — Batch 3 candidate for audit**: Reconstructed, configured, and verified 140 hand-written MIPS assembly function stubs in `src/ddr5thmix/LibrarySignatureBatch3.s` for short `library_signature`/`decompiler_output_only` functions across the main executable (8–16 bytes in size). Configured all 140 manifest entries in `config/ddr5thmix/build.json` and verified each function with `Invoke-FunctionMatch.ps1` against lawful `SLPM_868.97_1` (SHA-256 `4e0308ca...`), yielding 140/140 `byte_match: true` results (1,564 total reconstructed bytes). Promoted all 140 rows in `docs/games/ddr-5th-mix-jp-symbol-map.csv` to `confidence = verified` and `source_status = hand_written_source` with exact reference and built SHA-256 evidence. Verified exact identity set equality between fixed-base inventory, manifest entries, assembly sections, match reports, and promoted CSV rows. Confirmed module regression tests for `mode-control` and `runtime-core` pass cleanly. Updated main executable verified totals from 227 (10.7%) to 367 (17.3%) functions / 19,364 bytes (total 437 functions / 28,736 selected code bytes overall across main executable and HOW TO PLAY overlay).
* **External library-signature reconstruction work package — Batch 3 ready**: Defined a fixed-base, isolated third delegation pass for all 140 real, named, 8–16-byte main-executable rows still classified `library_signature`/`decompiler_output_only` at commit `cafb781` (1,564 bytes; canonical inventory SHA-256 `ede6b69b...`). The package excludes synthetic `0x20000000` GTE macro entries and larger functions, restricts delivery to one new assembly source plus the existing manifest/inventory/README/log paths, requires 140 individual hash-gated byte matches and identity-set equality, preserves conservative object-name claims, and explicitly guards against the confidence-promotion and byte-total defects caught while auditing batches 1 and 2. Delivery remains `candidate_for_audit` until an independent clean-checkout audit accepts it.

## 2026-07-27
* **Library signature reconstruction work package — Batch 2 candidate for audit**: Reconstructed, configured, and verified 31 hand-written MIPS assembly function stubs for `library_signature`/`decompiler_output_only` functions across the main executable (`SysEnqIntRP`, `SysDeqIntRP`, `ChangeClearRCnt`, `SetDQB`, `CdFlush`, `CdSetDebug`, `CdSync`, `CdReady`, `CdSyncCallback`, `CdReadyCallback`, `CdMix`, `CdGetSector`, `CdGetSector2`, `CdDataSync`, `CD_set_test_parmnum`, `EnablePAD`, `DisablePAD`, `SetInitPadFlag`, `ReadInitPadFlag`, `PadChkVsync`, `PadStartCom`, `PadStopCom`, `_padChkVsync`, `_padSetAct`, `_padSetCmd`, `_padCmdParaMode`, `GetIntrMask`, `SetIntrMask`, `GsGetActiveBuff`, `GetGraphDebug`, `SpuGetVoiceEnvelope`). Added stubs to `src/ddr5thmix/` (`PsyqBiosStubs.s`, `GteSetters.s`, `PsyqLibcdStubs.s`, `PsyqLibpadStubs.s`, `PsyqMiscStubs.s`) and `config/ddr5thmix/build.json`, and verified each function with `Invoke-FunctionMatch.ps1` against lawful `SLPM_868.97_1` (SHA-256 `4e0308ca...`), producing 31/31 `byte_match: true` results (696 total reconstructed bytes). Promoted all 31 rows in `docs/games/ddr-5th-mix-jp-symbol-map.csv` to `confidence = verified` and `source_status = hand_written_source` with exact built/reference SHA-256 evidence. Updated main executable verified totals from 196 (9.2%) to 227 (10.7%) functions / 17,800 bytes (total 297 functions / 27,172 bytes across main executable and HOW TO PLAY overlay).
* **Library signature reconstruction work package — Batch 1 candidate for audit**: Reconstructed, configured, and verified 31 hand-written MIPS assembly function stubs for `library_signature`/`decompiler_output_only` functions across the main executable (`ResetEntryInt`, `HookEntryInt`, `open`, `lseek`, `read`, `write`, `nextfile`, `rename`, `erase`, `Krom2RawAdd`, `ChangeClearPAD`, `strcmp`, `firstfile2`, `InitPAD2`, `StartPAD2`, `StopPAD2`, `PAD_init2`, `rand`, `srand`, `memset`, `memchr`, `strncmp`, `bcopy`, `close`, `strncpy`, `CdStatus`, `CdMode`, `CdLastCom`, `CdLastPos`, `GetClut`, `GsLinkObject5`). Each function was added to `src/ddr5thmix/` (`PsyqBiosStubs.s`, `PsyqLibcdStubs.s`, `PsyqLibgsStubs.s`) and `config/ddr5thmix/build.json`, and verified with `Invoke-FunctionMatch.ps1` against lawful `SLPM_868.97_1` (SHA-256 `4e0308ca...`), yielding 31/31 `byte_match: true` results (412 total reconstructed bytes). Promoted all 31 rows in `docs/games/ddr-5th-mix-jp-symbol-map.csv` to `hand_written_source` with exact built/reference SHA-256 evidence. Updated main executable verified totals from 165 (7.8%) to 196 (9.2%) functions / 17,100 bytes (total 266 functions / 26,472 bytes across main executable and HOW TO PLAY overlay).
* **External linked-object-boundary work package — Unit F complete**: Formulated the evidentiary distinction between translation unit byte-account closure (`gap_bytes: 0`) and semantic object-name identity in `docs/foundations/linked-object-evidence.md`. Audited `SYS` (12,076 B) and `FORMAT` (848 B) objects against this rule, detailing supported evidence (100% byte account, edge alignment padding, landing on `_card_read`) and missing SDK object file provenance. Established the falsifiable standard for future object claims: `candidate_for_audit` (100% complete byte account + edge padding) vs `boundary_confirmed` (requires candidate_for_audit + primary SDK object/source/symbol corroboration). Completed all units (A–F) of the external linked-object-boundary work package.
* **External linked-object-boundary work package — Unit E complete**: Surveyed `DdrModeState` offsets in `src/ddr5thmix/mode_control.h` and `docs/games/ddr-5th-mix-jp-globals.md`/`.csv` for observed fields with concrete readers and writers. Named four 16-bit fields at offsets `+0x0c`/`+0x0e`/`+0x10`/`+0x12`: `display_width` (320 / `0x140`), `display_height` (240 / `0xf0`), `display_mode` (4), and `display_dither` (1). Cited writer `FUN_800232cc` (mode `0x10`/submode `0x00`) and reader `FUN_800222fc` (passes all four as parameters to PsyQ `GsInitGraph2`). Added compile-time static assertions (`ddr_display_width_offset`, `ddr_display_height_offset`, `ddr_display_mode_offset`, `ddr_display_dither_offset`) to `mode_control.h` and updated `docs/games/ddr-5th-mix-jp-globals.md`. Verified `mode-control` (20/1,660 bytes) and `runtime-core` (6/2,232 bytes) module matchers pass cleanly.
* **External linked-object-boundary work package — Unit D complete**: Characterized the 1,472-byte gap between `Lzc` (`0x800553ac`) and `SetVertex0` (`0x80055984`). Dumped raw executable bytes across all 11 boundaries and classified the range: 1,444 of 1,472 bytes (98.1%) are dense, valid MIPS code across 10 inventoried functions (`TransposeMatrix`, `gte_rotate_z_matrix`, `GsGetLw`, `GS_133_OBJ_30`, `GS_133_OBJ_21C`, `FUN_80055788`, `FUN_80055870`, `FUN_8005589c`, `FUN_800558e4`, `GsSetFogParam`), while the remaining 4 non-zero gaps (4 bytes at `0x800553c4`, 12 bytes at `0x800554bc`, 8 bytes at `0x80055780`, and 4 bytes at `0x80055980` — 28 bytes total, 1.9%) are 100% zero-alignment padding (`00` bytes only) rounding entry points to 8-byte/16-byte boundaries. Replaced "left unexamined" in `docs/foundations/linked-object-evidence.md` with concrete byte-range evidence.
* **External linked-object-boundary work package — Unit C complete**: Investigated the two uncatalogued `BIOS_STUB`-shaped trampolines (`FUN_8003ba70` vector `0xb0`/service `0x3f`; `FUN_8003bb30` vector `0xa0`/service `0x72`) and their preceding 8-byte word pairs (`50 73 01 00 00 00 44 00` and `50 73 00 00 00 00 44 00`). Confirmed the 8-byte prefix is a PsyQ object header marker (`"Ps"` signature `0x7350` followed by section length `0x00440000`). Reconstructed both stubs in `src/ddr5thmix/PsyqBiosStubs.s` and `config/ddr5thmix/build.json`. GNU assembler/binutils 2.43 reproduces both 12-byte stubs with exact byte matches (`FUN_8003ba70` SHA-256 `859ccf6879a1def400ddcd0fab5063a5b8ee05bd985e429028e3322b6e149fd9`; `FUN_8003bb30` SHA-256 `d3eefd259eb1b5f648ec00610d2defbf4c6e5873f47895dd3d23a322bb0cfd7f`). Promoted both rows to `verified`/`hand_written_source` in the symbol map CSV, updated `docs/workflows/function-byte-match.md` table to 32 trampolines / 384 bytes, and updated README totals to 165 verified main-executable functions / 26,060 selected bytes.
* **External linked-object-boundary work package — Unit B complete**: Audited all 60 multi-row `<name>_OBJ_<offset>` runs across the symbol map CSV and identified a second independently corroborated object instance: the `FORMAT` run (`0x8003b6e8`–`0x8003ba38`, 848 bytes). All 3 `FORMAT_OBJ_*` rows (`FORMAT_OBJ_0`, `FORMAT_OBJ_F0`, `FORMAT_OBJ_320`) resolve to implied base `0x8003b6e8` with 100% complete byte coverage (848 of 848 bytes, `gap_bytes: 0`, `merged_intervals: 1`), and its measured end address (`0x8003ba38`) lands exactly on `_card_read` at `0x8003ba38` — an independently hand-reviewed, exact-byte-matched PsyQ BIOS trampoline function (`confidence = verified`, `source_status = hand_written_source`, built/reference SHA-256 `875fa877...`) in `src/ddr5thmix/PsyqBiosStubs.s`. Updated `docs/foundations/linked-object-evidence.md`.
* **External linked-object-boundary work package — Unit A complete**: Added the three missing `SYS_OBJ_*` rows identified by Ghidra auto-analysis (`SYS_OBJ_F80` @ `0x80039168`, 116 bytes; `SYS_OBJ_22F4` @ `0x8003a4dc`, 36 bytes; `SYS_OBJ_2828` @ `0x8003aa10`, 120 bytes — each having implied base `address − offset = 0x800381e8`) to `docs/games/ddr-5th-mix-jp-symbol-map.csv`. Verified `tools/build/Invoke-PsyqObjectBoundaryCheck.ps1` runs from clean against the tracked CSV and yields 100.0% byte account closure (12,076 of 12,076 bytes, `gap_bytes: 0`, `merged_intervals: 1`, `complete_byte_account: true`). Checked outer edges with `DumpBytes.java` (`0x800381dc`–`0x800381e7` and `0x8003b114`–`0x8003b11b`) and confirmed an 8-byte zero-alignment padding convention at both outer boundaries. Updated `docs/foundations/linked-object-evidence.md` with quoted check output and edge evidence.
* **External linked-object-boundary work package**: Added
  `docs/workflows/external-agent-linked-object-boundary.md`, a bounded
  delegation package that extends `docs/foundations/linked-object-evidence.md`
  toward its own falsifiable "object boundary confirmed" bar (closing the
  `SYS`/`ResetGraph` object's byte account and true-edge padding check, a
  second independent corroborating instance, the two uncatalogued BIOS-stub
  prefixes, the unexamined `Lzc`→`SetVertex0` gap, remaining global-field
  semantics, and object-name identity as a low-priority last item), each unit
  gated by its own reproducible verification, committed/pushed independently
  to a dedicated `agent/linked-object-boundary` branch, and left at
  `candidate_for_audit` per `docs/workflows/external-agent-acceptance.md` —
  no unit may be self-promoted to `completed` or merged into `master`. Linked
  from `docs/workflows/index.md` and README.

## 2026-07-26
* **Contiguous overlay link validation**: Extended `Invoke-OverlayMatch.ps1` with a generated linker script that places every manifest function/data section at its original address, links both reconstructed objects together, emits one contiguous binary, and checks its exact 11,864-byte length and overlay SHA-256. The global link passes with `contiguous_image_length_match: true`, SHA-256 `3dbf4b...`, and `whole_overlay_match: true`; per-section hash gates and the independent tail checker remain enabled.

## 2026-07-26
* **Inst-demo overlay completo byte a byte**: Añadidas representaciones semánticas para la tabla de bitmasks de 32 bytes, los 16 registros de timing de 12 bytes (incluidos los dos slots cero) y las dos matrices de flags de dibujo de 20 bytes. El manifiesto schema 2 alcanza 12 secciones de datos; `Invoke-OverlayMatch.ps1` reproduce los 70 cuerpos y 2.492 bytes de datos, cubriendo 11.864/11.864 bytes (100%) y reportando `whole_overlay_match: true` contra el overlay legal SHA-256 `3dbf4b...`. `Test-InstDemoRecordArray.ps1` continúa pasando; no se incluyen blobs ni binarios de referencia.

## 2026-07-26
* **Inst-demo first semantic data-source pass**: Added source-authored representations for the 72-byte overlay header and the three pre-command tables (264-byte tick/interpolation records, 96-byte interpolation pointer records, and 120-byte callback parameter records). Extended the schema-2 manifest with four hash-gated sections. `Invoke-OverlayMatch.ps1` now reproduces 9 data sections / 2,228 bytes plus all 70 functions: 11,600/11,864 selected bytes (97.8%); the remaining 264 bytes are the already structurally resolved tail tables. Verified with GNU binutils 2.43 and the lawful overlay SHA-256 gate; `whole_overlay_match` remains false pending semantic tail source and contiguous-image linking.

## 2026-07-25
* **Inst-demo semantic data reconstruction begins at 11,048 exact selected bytes**: Added `InstDemoOverlayData.s` without `incbin` or reference-byte blobs and expressed five evidence-backed regions as semantic commands/records: the 97-step command list plus terminator, runtime counter, nine-callback handler table, thirteen typed draw-record initializers plus sentinel, and 460 zero-at-rest runtime bytes. Extended the manifest to schema 2 with five hash-gated data sections and extended `Invoke-OverlayMatch.ps1` to assemble/link/compare them independently alongside all 70 functions. The verifier now reports 1,676 exact data bytes and 11,048/11,864 exact selected bytes (93.1%), while retaining `whole_overlay_match: false`; 816 bytes still need semantic source (72-byte header and 744 bytes of numeric/boolean tables). Also repaired the verifier's local extraction fallback so it no longer invokes the Ghidra importer without its mandatory project parameters.
* **Inst-demo final 48 tail bytes structurally resolved**: Proved that the two formerly unresolved 24-byte zero spans complete adjacent fixed-stride structures rather than forming independent padding. `0x801e6b6c`–`0x801e6b83` is the final 24 bytes of a complete all-zero 28-byte sentinel record at index 13 of the `0x801e69fc` initialization table; raw MIPS verifies that `FUN_801e5040` advances by 28 and terminates on the next record's null first word. `0x801e6c4c`–`0x801e6c63` supplies slots 14–15 of a 16-slot, 12-byte-stride timing table; `FUN_801e5758` computes `base + index*12`, while the hash-gated main executable computes RNG modulo 12 on the verified caller route, leaving slots 12–15 dormant there. Split the former overbroad command-list map row into the 784-byte script, a 4-byte runtime counter, a 36-byte default table, and the 392-byte initialization table. Extended `Test-InstDemoRecordArray.ps1` with overlay/executable hash gates and raw-instruction, stride, sentinel, boundary, and reachability assertions. Tail structural coverage is now 748/748 bytes with zero unresolved; no whole-overlay byte match is claimed.
* **Inst-demo tail reopened; 32-byte bitmask table resolved, 48 zero bytes remain**: Reopened the prior 80-byte evidentiary limit at the repository owner's explicit request and corrected the earlier `FUN_801e5178` bounds reading. Raw MIPS control flow proves rows 0 and 16 bypass the byte load; the lookup uses `base + selector*16 + row` only for rows 1–15. The sole in-overlay caller chain writes selector word zero immediately before forwarding it, so the reachable access set is exactly first-bank indices 1–15 and the previously suspected plane-1/row-16 one-past-end read is impossible. Both 16-byte banks have a zero index-0 sentinel, non-zero indices 1–15, and low-nibble-only values; reclassified all 32 bytes as a two-bank bitmask-selector table without asserting an audio/note semantic. Extended `Test-InstDemoRecordArray.ps1` with a raw instruction decoder, JAL-count/call-chain checks, bounds assertions, and non-literal table-shape checks. Expanded the two remaining 24-byte zero runs' negative evidence to the main executable: Ghidra 12.1.2 returned zero xrefs for all 48 addresses and an unaligned raw scan found zero literal pointers. They remain unresolved rather than guessed padding. Updated the overlay map/concept and README totals from 668/80 to 700/48.
* **Current-status consistency repair**: Synchronized README, symbol-map concept, and function-byte-match workflow with the authoritative 2,124-row CSV and HEAD: 163 verified, 102 manual, 977 library-signature, and 882 unverified functions; 233 exact functions/26,036 selected bytes overall. Updated `mode-control` to 20/1,660 and `runtime-core` to 6/2,232, replaced stale `+0x2c` descriptions with `menu_selection_index`, and removed already-resolved `+0x09`/`+0x17`/`+0x2c`/42-entry-array work from the recommended targets while retaining `+0x2e` as an exhaustive negative result.

## 2026-07-24
* **Four long-standing `DdrModeState`/screen-flow open questions resolved**:
  Re-ran `tools/ghidra/scripts/DumpFieldXrefs.java` against
  `PTR_DAT_800ac8e8` (now 62 referencing functions, up from the 58 catalogued
  2026-07-14) specifically searching for **reads**, not just the
  already-known clears, of offsets `+0x09`, `+0x17`, `+0x2c`, and `+0x2e` —
  discovered that bracket-indexed byte accesses (`PTR_DAT_800ac8e8[N]`) don't
  match a `+ 0xN)`-style textual search, which is why prior sweeps missed
  them. (1) **`+0x09`**: `main` itself is the same-frame consumer —
  its own two nested `do`/`while` loop conditions test this byte directly;
  it is `main`'s loop-restart flag, set to `1` only by the mode-`0xff`
  service-state handler to force a full state/GPU reset. (2) **`+0x17`**:
  the shared draw routine `FUN_80021470` reads it as a glyph/menu-item
  color-brightness byte; two more writers (`FUN_800ab408`, `FUN_8009e4cc`)
  were found beyond the already-known one. (3) **`+0x2c`**: `FUN_80022b30`
  (mode `0x10`/default submode `0x01`'s menu) reads it back as its own
  0-2 current-selection index for a 3-item menu — already described in this
  document's own mode table from 2026-07-14 but never connected to the
  "what reads `+0x2c`" open question until now. **`+0x2e`** remains a
  genuine, now-exhaustive negative result: no reader exists anywhere.
  (4) **The 42-entry screen-name pointer array**: a per-entry
  `DumpDataXrefs.java` sweep of all 42 individual slot addresses (not just
  the base) found `FUN_80049dec` is the *only* function touching any entry,
  and re-reading its full disassembly shows it unconditionally copies the
  *complete* array to a local stack buffer (not a conditional scan stopping
  at `TEST_MODE`, as previously described) — the buffer is then never read
  again and its one remaining call (`FUN_80042e1c`) doesn't consume it
  either, so the copy appears functionally dead. Reconstructed the two
  newly-implicated small/bounded functions: `main` (408 bytes, semantic MIPS
  assembly, added to `runtime-core` — now six functions/2,232 bytes) and
  `FUN_80022b30` (456 bytes, semantic MIPS assembly, added to `mode-control`
  — now twenty functions/1,660 bytes), both byte-matching on the first
  attempt with GNU binutils 2.43. Renamed the corresponding
  `mode_control.h` fields (`unknown_009`→`loop_restart_flag`,
  `unknown_017`→`glyph_color`, `unknown_02c`→`menu_selection_index`) with
  new compile-time offset assertions; `unknown_02e` is left named as-is
  since it remains unresolved. Did not reconstruct `FUN_80021470`,
  `FUN_800ab408`, or `FUN_8009e4cc` — all three are large text/HUD-rendering
  routines outside every currently tracked module's scope; their access
  patterns are recorded as evidence without a byte-match claim, per this
  project's standard for out-of-scope consumers. Updated
  `docs/games/ddr-5th-mix-jp-globals.md`/`.csv`,
  `ddr-5th-mix-jp-screen-flow.md` (moved all four items from "Open" to
  "Resolved structural questions"), `ddr-5th-mix-jp-symbol-map.md`/`.csv`,
  `ddr-5th-mix-jp-mode-control.md`, and `ddr-5th-mix-jp-runtime-core.md`.
* **First linked-object-boundary evidence pass**: Added
  `docs/foundations/linked-object-evidence.md`, this project's first pass at
  evidence toward original PsyQ object boundaries, not just independently
  placed function bodies. Alignment/padding check (angle 1): every already
  non-zero gap between exact-matched functions in `mode-control` and the
  merged four gameplay-session modules was byte-dumped with
  `tools/ghidra/scripts/DumpBytes.java` and found to be dense real code, not
  padding — including a previously unremarked fact that 48 of the 51
  gameplay-session functions (all four logical modules combined, minus three
  distant outer wrappers) occupy one single contiguous 9,328-byte range with
  only two gaps, both fully explained by already-Ghidra-discovered
  not-yet-matched sibling functions. In contrast, the 30-function PsyQ
  BIOS-stub block shows a real, 100%-consistent 4-byte zero-padding
  convention (16-byte rounding) at 24 of its 29 stub-to-stub boundaries, and
  its larger
  gaps contain two previously uncatalogued `BIOS_STUB`-shaped trampolines
  (vector `0xb0`/service `0x3f`, vector `0xa0`/service `0x72`, not added to
  the accepted manifest). Known-library cross-check (angle 2): found and
  characterized a previously unremarked class of 491 symbol-map rows shaped
  `<name>_OBJ_<hex-offset>`, all `ghidra_psx_ldr` PsyQ-signature tool output;
  60 of 60 multi-row consecutive-address prefix runs are perfectly
  consistent with one implied object-base address per run, and the
  strongest single result is that one `SYS` run's implied base lands exactly
  on `ResetGraph` — a function this
  project had already independently hand-reviewed — with the same ~12KB
  implied object also containing `SetDispMask`/`DrawSync`/`ClearImage2`/
  `LoadImage`/`GsInitGraph2`/`GsInit3D`/`InitGeom`, all already-confirmed
  PsyQ library functions, 97.7% byte-accounted with only three small
  (dense-code, non-padding) residual gaps. Duplicate-body check (angle 3):
  confirmed exactly two repeated hashes across the whole corpus — the
  already-known substantive `SetTransMatrix` duplicate, and a separate,
  weaker four-way collision of the trivial 8-byte `jr ra; nop` no-op body
  across `screen-selector`/`mode-control`, explicitly distinguished as
  expected-by-construction rather than informative. Residual-metadata check
  (angle 4): corroborated the existing `2MBYTE_OBJ_B4` marker finding against
  `ghidra_psx_ldr`'s own upstream README (fetched externally, not
  reproduced) confirming its signature database is built from real PsyQ
  `.LIB`/`.OBJ` files named after their origin object. States explicitly what
  remains open (the un-cross-referenced object names' semantic identity, one
  unexamined GTE-block gap, the two new BIOS stubs' unexplained 8-byte
  prefix) and a falsifiable four-part bar for a future "object boundary
  confirmed" claim. Does not claim any object boundary is confirmed and does
  not modify the accepted-function manifest, symbol map, or build config.

## 2026-07-23
* **Game-session endgame module complete — closes the entire 15-state gameplay-session child graph (states 0-14, all exact)**: Reconstructed all eighteen enter/update/exit callbacks for states 8-13 — `RESULT` (`FUN_80070bf8`/`FUN_80070c70`/`FUN_80070d04`, 324 B), `GAME_OVER` (`FUN_80070d3c`/`FUN_8006fba4`/`FUN_80070d6c`, 448 B), `ENDING` (`FUN_80070d9c`/`FUN_80070e2c`/`FUN_80070ebc`, 360 B), `PRE_END` (`FUN_80070f04`/`FUN_80070f54`/`FUN_80071004`, 296 B), `LINK END` (`FUN_8007102c`/`FUN_80071084`/`FUN_800710f8`, 236 B), and `NAME ENTRY` (`FUN_80071118`/`FUN_80071168`/`FUN_80071214`, 296 B) — as the new `game-session-endgame` module. Re-confirmed all 18 boundaries/sizes with `DumpFunctionDetail.java` against the shared Ghidra project's `SLPM_868.97_1` program before writing source, matching the 2026-07-15 static review in the screen-flow document's "Result and end-of-session branches" section exactly (no drift). Semantic MIPS assembly in the new `src/ddr5thmix/GameSessionEndgame.s` matches all 1,960 selected bytes with GCC 14.2.0/binutils 2.43 on the first attempt per function, registered as `game-session-endgame` in `config/ddr5thmix/build.json`; no compiler-compatibility shim was needed. Regressed all six existing modules (`mode-control` 19/1,204, `runtime-core` 5/1,824, `screen-selector` 22/2,344, `game-session-router` 9/1,684, `game-session-opening` 12/1,736, `game-session-gameplay` 12/3,356) — all pass. Together with the router's terminal-state-14 callbacks and the opening/gameplay modules, this completes all 45 enter/update/exit callback-table entries for the 15-state gameplay session (8,736 bytes across four modules) — every gameplay-session child state (0-14) is now exact. Promoted all 18 functions from `manual`/`disassembly_reviewed` to `verified`/`hand_written_source` in the symbol map, each row citing its built/reference SHA-256; project totals move to 161 verified/103 manual (was 143/121). Added OKF concept `docs/games/ddr-5th-mix-jp-game-session-endgame.md`, linked from the games index and README, and updated README's exact-reconstruction totals to 231 functions/25,172 selected bytes (161/15,800 main executable plus 70/9,372 HOW TO PLAY overlay); removed the now-completed RESULT/PRE_END/LINK END/GAME_OVER/ENDING/NAME ENTRY item from README's recommended next targets.
* **Inst-demo-overlay 80-byte range, third pass: dynamic (BizHawk) verification, write-negative confirmed, no classification change**: Static analysis for the remaining 80 unresolved bytes (two 24-byte all-zero runs plus a partially-referenced 32-byte table) was credibly exhausted, so this pass tried running the actual game in BizHawk 2.11 (PSX core `Nymashock`, Waterbox/Nyma-wrapped) with no controller input through the documented attract loop, watching the three ranges while HOW TO PLAY plays. Reaching HOW TO PLAY required dismissing one real, documented boot-time dialog pair (BizHawk mounts a blank virtual memory card; the game detects "card present, no system data" exactly as the screen-flow doc's "Memory Card Auto Load" step describes) with a single `○` press each — not a synthetic skip — after which the title screen's own documented 900-frame inactivity timeout returns to the attract loop's `WARNING` state on its own; every frame from there through HOW TO PLAY is genuinely zero-input. The active window was confirmed both by polling the documented screen-index global `DAT_800f2908` and, independently, by a mid-window screenshot literally showing the "HOW TO PLAY" tutorial screen; its measured length (1,989 frames) is close to but not identical to the 1,910-tick script trace already verified statically. BizHawk's `event.on_bus_read`/`on_bus_write`/`on_bus_exec` bus-hook API was tested and found **non-functional for this core/session** (`event.availableScopes()` returns zero scopes; a control hook on a function documented as firing every active frame never fired once) — a tooling limitation, not a game claim, now recorded in `docs/tooling/bizhawk-harness.md` alongside an empirically-found BizHawk input-timing fact (simulated presses held only 1-2 frames can be silently dropped on a lag frame; hold ~10 frames) and the real (unprefixed, glyph-named) PSX joypad button names this core exposes to Lua. The working fallback, plain per-frame `memory.read_u8` polling/diffing (confirmed functional via the working screen-index poll), found **zero byte-value changes in all three target ranges across the full visually-confirmed HOW TO PLAY window** — a genuine dynamic negative result for writes specifically, on top of the two existing independent static zero-reference sweeps for the two all-zero runs; it cannot detect reads with no side effect, so it neither confirms nor refutes the partially-referenced table's single ambiguous static read. No range map CSV classification changed (all three sub-ranges remain `data_unresolved`/`unverified`): a third independent negative result does not by itself constitute a resolved consumer, per this project's evidentiary standard. Added `tools/bizhawk/probe-inst-demo-watch.lua` and a `-LuaPath` parameter on `tools/bizhawk/run-probe.ps1`; documented method, findings, and reproduction in a new "Third pass" section of `docs/games/ddr-5th-mix-jp-inst-demo-overlay.md` and in `docs/tooling/bizhawk-harness.md`. Also recorded, as a fragility caveat: this fixed-frame boot-dialog sequence diverged into an actual gameplay-session route (instead of returning to the attract loop) in one of six otherwise-identical runs, likely from disc-read timing jitter; only a visually-confirmed successful run's data is reported.
* **Inst-demo-overlay 288-byte range, second pass: full Ghidra auto-analysis resolves 208 more bytes, 80 remain**: Re-audited whether the shared headless Ghidra project's `inst-demo.bin` program had ever had full auto-analysis run, prompted by the prior same-day 288-byte-unresolved result. A new diagnostic (`tools/ghidra/scripts/DumpProgramInfoOptions.java`) found the program's `Analyzed` option was already `true` with a recorded analysis time: `Import-RawOverlay.ps1`'s `-import` step passes neither `-noanalysis` nor an explicit analyze flag, and Ghidra's headless default in that case is to run full analysis after import — so full analysis had already run at initial import, contrary to the prior pass's working assumption that only entry-point functions existed. Explicitly re-running analysis (12.1.2's headless analyzer has no `-analyze` flag; omitting `-noanalysis` is how a full pass is forced) ran the standard analyzer suite including the Basic Constant Reference Analyzer (the `lui`/`addiu` constant-propagation pass needed to resolve split-instruction data references) and ghidra_psx_ldr's PsyQ Signatures analyzer (detected PsyQ Version `470`, informational only); no ghidra_psx_ldr PSX-EXE-specific analyzer engaged, confirmed rather than assumed, since this is a headerless raw `BinaryLoader` image. A byte-granular `DumpDataXrefs.java` sweep of the full 288-byte range then found four addresses with new references that were exactly zero before: decompiling the five newly-implicated functions and tracing their callers through the tracked assembly source resolved `0x801e6ba4`–`0x801e6c4b` (168 bytes) as a 14-entry array of 3×int32 (12-byte-stride) timing/threshold records — read by `FUN_801e5758`/`FUN_801e5d74`, both direct callees of the overlay's own verified Init/Update entry points (`FUN_801e413c`/`FUN_801e41e8`) at caller-struct offset `+1280`, feeding the already-documented external GTE helper `0x80056148` — and `0x801e6c64`–`0x801e6c8b` (40 bytes) as two adjacent 20-byte 0/1 draw-enable flag arrays, read by `FUN_801e5f08`/`FUN_801e58dc` (same Init/Update entry points, caller-struct offset `+1320`) gating up to ~19 per-slot calls to the already-documented external GPU helper `0x80056a08`. This **retracts** the earlier claim that `FUN_801e5d74`/`FUN_801e58dc` have "no reference to any address" in this data range. A fifth new reference — one byte at `0x801e6b84`, from `FUN_801e5178` — is a genuine partial finding for the 32-byte small-byte-value table but not a full resolution (the row/plane addressing arithmetic isn't fully pinned down and its edge case would read one byte past the sub-range boundary), so that sub-range stays `data_unresolved`. Both 24-byte all-zero runs were reconfirmed byte-granular as genuinely zero-reference. Net: `data_unresolved` drops from 288 to 80 bytes (two 24-byte zero runs plus the partially-referenced 32-byte table); updated the range map CSV, the overlay concept's range map table, code/data surface totals, and external-callees section, and extended `tools/build/Test-InstDemoRecordArray.ps1` with not-all-zero structural assertions for the two newly-resolved ranges (no literal byte content embedded or asserted).
* **Inst-demo-overlay 748-byte tail range: 460 bytes resolved, 288 bytes remain unresolved**: Reimported the hash-gated `inst-demo.bin` program into the shared headless Ghidra project and traced every static reference into the overlay's last unmapped range (`0x801e6b6c`–`0x801e6e57`) with `DumpDataXrefs.java`, `DumpFunctionDetail.java`, and a full-overlay raw-byte scan for literal 32-bit pointers. Found that `0x801e6c8c`–`0x801e6e57` (460 bytes, all-zero in the static ROM image) is a runtime-populated array of 13 32-byte GPU FT4 (textured-quad/sprite-tile) draw-parameter records plus one 14th record and a 12-byte preset/selector record: the command-list block's own tail holds a literal 13-entry pointer table (28-byte stride, `0x801e69fc`) whose targets are exactly `0x801e6c8c + i*0x20`, walked by `FUN_801e5040` at init; eight overlay functions (`FUN_801e6270`, `FUN_801e6320`, `FUN_801e634c`, `FUN_801e4e70`, `FUN_801e4f68`, `FUN_801e6408`, `FUN_801e5040`, plus the `FUN_801e6188`/`FUN_801e61b4`/`FUN_801e61e0` preset trio) populate specific record fields and pass the record pointer to `FUN_801e4978`, which decompiles as a PSX GPU FT4 primitive builder reading exactly `x`/`y`/width-delta/height-delta (`+0x00..+0x0c`), an ordering-table index (`+0x10`), and two flag bytes (`+0x14`/`+0x15`). This **retracts** the earlier "3D lighting/model transform parameter tables" guess for the resolved bytes — it is 2D on-screen text/graphic tile draw data (the `btile` system named in the overlay's own header), not 3D matrices; the five external callees previously flagged as plausibly involved (`0x800548e8`, `0x80054968`, `0x80056148`, `0x80056a08`, `0x80056af0`) were checked directly and confirmed unrelated (called by different overlay functions on an unrelated caller-supplied struct). The remaining `0x801e6b6c`–`0x801e6c8b` (288 bytes: two 24-byte all-zero runs, a 32-byte small-byte-value table, a 168-byte signed-32-bit array, and a 40-byte 0/1 flag array) has zero static references found by either method and stays `data_unresolved`, per this project's evidentiary standard rather than a guessed classification. Replaced the single 748-byte CSV row with eight precisely-bounded sub-ranges, added `tools/build/Test-InstDemoRecordArray.ps1` (SHA-256 gate, zero-run assertions, and pointer-table-to-record-address arithmetic, all reproducible from the hash-gated overlay bytes without embedding any literal data-table content), and updated the overlay concept's range map, code/data surface summary, and external-callee notes accordingly.
* **External prior-art check (decomp.me): negative result, not added as guidance**: Searched decomp.me, decomp.wiki, `decomp-permuter` and its PsyQ forks, and other public PS1 matching-decompilation projects (sotn-decomp, mkst/esa, FoxdieTeam/mgs_reversing, sozud/psy-q-decomp) for reusable GCC-vs-PsyQ matching techniques. DDR 5th Mix (Japan) is not itself present on decomp.me. decomp.me does list a `psyq4.4` compiler preset, matching this project's already-identified PsyQ SDK version, and confirms `CC1PSX.EXE` is a patched **GCC 2.8.1** fork rather than an unrelated proprietary compiler -- a useful lineage fact explaining why this project's GCC-14-vs-PsyQ mismatches (register allocation, frame/epilogue shape, sibling-call elimination) are expected. However, every known project in that ecosystem matches PsyQ output with **period-correct GCC 2.x** (or the real PsyQ binary), never a modern GCC generation-14 bootstrap like `/config/ddr5thmix/build.json`'s. No precedent was found for this project's specific approach, so none of decomp.me's cataloged quirks, `maspsx` (an ASPSX-equivalence translator built for GCC 2.x output), or the `decomp-permuter-psyq` forks (noted incomplete: `import.py` reportedly non-functional) are recorded as applicable guidance here -- doing so without independent verification against this project's own GCC 14/binutils 2.43 output would risk misattributing a different compiler generation's behavior. This finding is deliberately not added to `/docs/tooling/toolchain.md`'s recommended stack or citations.
* **Game-session gameplay module complete (PREPARE, INTRO, DANCING, STAGE END)**: Closed out the remaining STAGE END triple — enter `FUN_8006f784` (260 B), the 424-byte switch-driven update `FUN_8006f888` (including its indirect `jr`-through-jump-table dispatch, verified without embedding the external jump-table data itself), and exit `FUN_8006fa30` (372 B) — completing all twelve state 4-7 callbacks as the `game-session-gameplay` module. GCC 14.2.0/binutils 2.43 matches all 3,356 selected bytes across all twelve functions, every one on its first reconstruction attempt; no compiler-compatibility shim was needed since the module is written directly as semantic MIPS assembly, mirroring `GameSessionOpening.s`/`GameSessionRouter.s`. Together with the router (9/1,684) and opening (12/1,736) modules, the entire 15-state gameplay session's control flow is now exact: 33 functions/6,776 bytes. Regressed `mode-control` (19/1,204), `runtime-core` (5/1,824), `screen-selector` (22/2,344), `game-session-router` (9/1,684), and `game-session-opening` (12/1,736) — all pass. Promoted all twelve functions from `manual`/`disassembly_reviewed` to `verified`/`hand_written_source` in the symbol map, each row citing its built/reference SHA-256; project totals move to 143 verified/121 manual (was 131/133). Added OKF concept `docs/games/ddr-5th-mix-jp-game-session-gameplay.md`, linked from the games index and README, and updated README's exact-reconstruction totals to 213 functions/23,212 selected bytes (143/13,840 main executable plus 70/9,372 HOW TO PLAY overlay).
* **Game-session gameplay states 5/4/6 exact progress (PREPARE, INTRO, DANCING)**: Reconstructed and exact-matched nine of the twelve state 4-7 callback-table functions inventoried earlier the same day: PREPARE's full enter/update/exit triple (`FUN_8006efd4`/`FUN_80070ab4`/`FUN_8006f0a0`, 824 bytes), INTRO's full triple (`FUN_8006f1fc`/`FUN_8006f380`/`FUN_80070aac`, 680 bytes), and DANCING's full triple (`FUN_80070bc4`/`FUN_8006f49c`/`FUN_8006f6cc`, 796 bytes) — including the 560-byte `FUN_8006f49c`, the largest function reconstructed for this project to date. Semantic MIPS assembly in the new `src/ddr5thmix/GameSessionGameplay.s` matches all 2,300 selected bytes with GCC 14.2.0/binutils 2.43 on the first attempt per function, registered as the new `game-session-gameplay` module in `config/ddr5thmix/build.json`. Confirms several globals the same-day structural inventory left uncatalogued: `DAT_800f28f8` is a dividend for a `%3` selector into three words at `0x8001cac0`/`0x8001cac4`/`0x8001cac8` (same magic reciprocal-multiply-by-3 sequence used elsewhere in the codebase), `DAT_80105083`/`DAT_80105086` are per-timeline flag/bitmask bytes, and `DAT_80104e30` is a byte array indexed by `DAT_800f2924` shared with the existing `FUN_80070958` pattern from `game-session-opening`. STAGE END (state 7, three functions/1,056 bytes) remains outstanding.
* **PREPARE/INTRO/DANCING/STAGE END structural inventory**: Investigation-only pass mirroring the HOW TO PLAY overlay's structural-inventory step for the twelve enter/update/exit callbacks of gameplay-session states 5 (`PREPARE`), 4 (`INTRO`), 6 (`DANCING`), and 7 (`STAGE END`). Confirmed all twelve function boundaries with `DumpFunctionDetail.java` against the shared Ghidra project's `SLPM_868.97_1` program (3,356 attributed bytes total); every boundary and unique-callee count exactly matches the existing 2026-07-15 symbol-map rows, so no auto-analysis drift was found and no new boundaries needed creating. Enumerated 27 distinct direct callees, none overlapping the exact-matched `mode-control`, `runtime-core`, `screen-selector`, `game-session-router`, or `game-session-opening` modules: three are already `manual`-reviewed (the per-player timeline classifier `FUN_8007fdec`, shared init `FUN_8007fc8c`, and shared cleanup `FUN_80081e90`), one carries the PsyQ library-signature name `VSync`, and the remaining 23 are unreviewed. Also recorded 26 fixed-address globals referenced by these functions that are absent from the global map, without asserting any field types or layout beyond noting two apparent per-player field clusters. No reconstruction source was written and no byte match is claimed. Added the inventory as a new section in `docs/games/ddr-5th-mix-jp-screen-flow.md`.

## 2026-07-20
* **External-agent acceptance workflow**: Added a repository-wide delivery policy that keeps delegated work at `candidate_for_audit` until a clean-checkout independent audit reproduces it. The workflow requires per-function reference/built hashes, identity-based equality across inventory/manifest/source/report/map sets, separate code/data/unresolved coverage claims, deterministic behavioral checkers, explicit Git path allowlists, and auditor-only promotion to `completed`.

## 2026-07-19
* **HOW TO PLAY overlay integration audit**: Re-ran all 70 per-function hashes and 9,372 selected code bytes against the hash-gated overlay with GNU binutils 2.43, confirmed the 76-interval/11,864-byte map has no gaps or overlaps, and reproduced the 97-step/1,910-tick trace with the explicit callback-derived jump `0x801e67f4` → `0x801e67fc`. Regressed all five existing game-owned modules (67 functions/8,792 bytes) after merging the concurrent session and overlay work. Updated the repository overview to distinguish 131 exact main-executable functions/10,484 bytes from 70 exact overlay functions/9,372 bytes, for 201 functions/19,856 selected bytes overall; the overlay's final 748 data bytes remain explicitly unresolved.
* **Game-session opening exact module**: Reconstructed all twelve callback-table functions for child states 0–3: PLAY START, the six-state selector owner, NON STOP I, and NON STOP C. Semantic MIPS assembly matched all 1,736 selected bytes with GCC 14.2.0/binutils 2.43. Together with `game-session-router`, the session now has 21 exact functions/3,420 bytes; project totals are 131 verified functions/10,484 bytes, with 133 manually reviewed rows remaining.
* **Game-session router exact module**: Reproduced all 45 entries in the 15-state child's enter/update/exit tables directly from the hash-gated PS-X EXE while the shared Ghidra project was legitimately locked by concurrent overlay work; all 45 targets are distinct. Reconstructed the three outer session wrappers, child init/tick/cleanup triple, and three terminal callbacks as `game-session-router`. Semantic MIPS assembly preserves R3000A control flow and fixes two recorded toolchain mechanics: `move` had to be expressed as original `addu`, and the call-free tick section required runtime linking for internal J-format targets. GCC 14.2.0/binutils 2.43 matches all nine functions/1,684 bytes. The map now contains 2,124 functions: 119 verified, 145 manual, 977 library-signature, and 883 unverified; exact reconstruction totals 119 functions/8,748 bytes.
* **Inst-demo-overlay structural inventory and per-function match**: Completed full 11,864-byte structural inventory (`0x801e4000`–`0x801e6e57`) for the DDR 5th Mix HOW TO PLAY executable overlay (inst-demo). Reconstructed and exact-matched all 70 MIPS functions (9,372 code bytes) in `InstDemoOverlay.s` with GNU binutils 2.43 (`mipsel-none-elf-as`, `mipsel-none-elf-ld`, `mipsel-none-elf-objcopy`), recording individual reference SHA-256 hashes for each function in `config/ddr5thmix/inst-demo-overlay.json`. Classified the header (72 B), parameter tables (480 B), 97-step command script (1,192 B), and unresolved 3D transform data (748 B). Added reproducible range map auditor `Invoke-OverlayRangeMapCheck.ps1` (validating 11,864 total bytes without gaps/overlaps) and dynamic script trace auditor `Test-InstDemoScriptTrace.ps1` (deriving 97 steps, 96 callbacks, 1 jump from `0x801e67f4` to `0x801e67fc`, 24 60-tick waits, 5 80-tick waits, 2 20-tick interpolations, 6 5-tick interpolations, and 1,910 total ticks). No whole-overlay byte match is claimed while 748 data bytes remain unresolved. Added OKF concept `docs/games/ddr-5th-mix-jp-inst-demo-overlay.md` and CSV map `docs/games/ddr-5th-mix-jp-inst-demo-overlay-map.csv`.
* **External HOW TO PLAY overlay work package**: Closed the prior selector delegation package with result commit `8a6ba1d` and defined a larger isolated package for the complete 11,864-byte `inst demo` overlay. The contract requires a full non-overlapping range map, inventory and reconstruction of every identified in-range function and unique command callback, exact per-function and selected-code aggregate verification, independent reproduction of the 97-step/1,910-tick script, and durable unresolved-range evidence. Its mandatory `agent/inst-demo-overlay` branch protocol stages only explicitly named files, forbids broad adds and direct `master` integration, and reserves the concurrently developed 15-state gameplay-session machine.
* **Concurrent module integration audit**: Revalidated `mode-control` (19/1,204), `runtime-core` (5/1,824), and the externally completed `screen-selector` (22/2,344) together. Added the six indirect selector callbacks absent from the symbol-map CSV, restored all selector rows' raw `symbol_source_type=DEFAULT`, and attached each of the 22 exact hashes. The map now contains 2,124 functions: 110 verified, 154 manual, 977 library-signature, and 883 unverified. Corrected `DdrModeState` input words to preserve verified four-byte width without asserting four-byte alignment; this prevents GCC from merging `SetMode`'s two halfword clears and keeps every module green.
* **Screen-selector exact module**: Reconstructed and verified the DDR 5th Mix game-session screen selector module. Inventoried all 18 enter/update/exit callback table entries at addresses 0x800defe8, 0x800df000, 0x800df018, three substate-3 helpers, and the central next-state router FUN_80075af8 (22 functions / 2,344 bytes total). GCC 14.2.0/binutils 2.43 matches all 22 functions byte-for-byte in ScreenSelector.s. Created OKF concept docs/games/ddr-5th-mix-jp-screen-selector.md, linked it in games index, and registered the screen-selector module in build.json. The later integration audit adds six missing indirect rows and records the final combined totals.
* **Runtime-core exact module**: Reconstructed the per-frame PAD/auxiliary input adapter `FUN_8002112c`, 0x140-byte runtime-state reset `FUN_8002216c`, mode/submode dispatcher `FUN_80022cf8`, shared counter/derived-state updater `FUN_80023744`, and screen-index range flag `FUN_8009971c`. Semantic MIPS assembly preserves the observed PsyQ control flow, repeated pointer loads, absolute jumps, registers, and delay slots; GNU binutils 2.43 matches all five functions/1,824 bytes. Extended the verifier to link assembly entries with declared symbols while discarding unrelated shared-source sections, with mode-control, GTE, and BIOS regressions passing. The manifest now holds 88 functions/4,720 bytes, the global map holds sixteen entries, and the shared state layout records the verified 0x140-byte cleared prefix without claiming allocation size.
* **External selector work package**: Formalized the owner-requested root `README.md` as the second root documentation exception and added a bounded external-agent package for the nested six-state STYLE/CHARACTER/MUSIC selector. The package requires inventorying all enter/update/exit table entries, reconstructing the 13 known callbacks/router plus any unique exit targets, aggregate byte verification, durable recording of findings and negative attempts, persistence until the full definition of done is satisfied, and normal Git commit/push coordination while explicitly reserving the five `runtime-core` functions for concurrent local work.
* **Repository overview and next targets**: Added the owner-requested root `README.md` as a concise project landing page. It distinguishes Ghidra's ~49% attributed function-body inventory from the 83-function/2,896-byte exact reconstruction result, summarizes the completed provenance, PsyQ, mode-control, global-map, screen-flow, overlay, and music-record work, and prioritizes a game-owned `runtime-core` module before selector, gameplay-session, overlay, global-layout, and linked-object reconstruction work. Detailed evidence remains in the OKF bundle under `docs/`.
* **Completed mode-control range**: Reconstructed the seven remaining functions in `0x800230cc–0x800236cc`. Six bounded semantic inline sequences preserve PsyQ reloads, saved-register layout, shared epilogues, relocations, and delay slots; the 16-byte secondary-state byte setter `FUN_800236bc` matches from direct C on the first attempt. GCC 14.2.0/binutils 2.43 matches all 596 new bytes, closing all 18 inventoried in-range functions/1,168 bytes. With external snapshot dependency `FUN_80022148`, `mode-control` now verifies 19 functions/1,204 bytes; the full manifest reaches 83 functions/2,896 bytes. Expanded the global map to ten symbols and the shared partial layouts without assigning semantics beyond observed operations.
* **Mode-control dependency matches and range inventory**: Reconstructed mode-4 handlers `FUN_8002340c`/`FUN_80023474` plus external snapshot helper `FUN_80022148`. Direct C preserved semantics but produced incompatible PsyQ control flow or base/register allocation, so bounded semantic inline sequences retain linked relocations, shared epilogues, and delay slots. GCC 14.2.0/binutils 2.43 matches all 228 new bytes, expanding `mode-control` to 12 functions/608 bytes and the manifest to 76 accepted functions/2,300 bytes. Inventoried all 18 function starts and 1,168 attributed bytes in `0x800230cc–0x800236cc`; 11 are accepted in-range members, six more were already reviewed, and newly reviewed `FUN_800236bc` is a 16-byte setter for secondary-state byte `+0x00` with no identified caller. Expanded the global map from three to six fixed symbols without treating the tool-assigned `PTR_DAT_800ac8f0` name as pointer evidence.
* **First global map and game-logic module**: Defined a global-map CSV schema and created the initial DDR 5th Mix map for `PTR_DAT_800ac8e8`, `PTR_DAT_800ac8ec`, and `DAT_800e2a60`, keeping runtime pointer targets distinct from fixed addresses. Consolidated the observed primary/secondary state offsets in a shared header with compile-time layout assertions; the fuller type exposed one negative result, where GCC merged four byte stores until a zero-instruction constraint removed its alignment inference. Added the named [`mode-control`](/docs/games/ddr-5th-mix-jp-mode-control.md) build module and an aggregate verifier; GCC 14.2.0/binutils 2.43 matched all nine functions and 380 selected bytes. This establishes the first reproducible multi-function unit without claiming whole-object or whole-executable linkage.
* **Remaining exact-name GTE matches**: Reconstructed the 11 remaining real linked functions whose names exactly match the current GTE catalog: two identical `SetTransMatrix` copies, `MulMatrix0`, three matrix setters, two color setters, `AverageZ3`, `AverageZ4`, and `Lzc`. Semantic MIPS assembly retains the GTE register mapping, three MVMVA operations, AVSZ commands, and documented coprocessor latency; only GTE command encodings unsupported as assembler mnemonics use raw words. GNU binutils 2.43 matches all 632 selected bytes, bringing real GTE/COP2 coverage to 33 functions and 1,324 bytes and the manifest to 73 accepted functions.

## 2026-07-18
* **PsyQ BIOS trampoline byte matches**: Reconstructed 30 twelve-byte PsyQ BIOS/kernel wrappers spanning card services, C-runtime helpers, heap/cache/GPU calls, and event services. A project-authored `BIOS_STUB` macro preserves the common `addiu t2` / `jr t2` / delay-slot `addiu t1` shape while emitting a separate section for every function. Manifest-driven GNU binutils 2.43 comparisons match all 360 selected bytes; every wrapper has an individual report and SHA-256. Promoted all 30 rows to `verified`/`hand_written_source`; the manifest now contains 62 accepted functions and 52 PsyQ byte matches.
* **GTE register-setter byte matches**: Reconstructed ten contiguous PsyQ 4.4.0 LIBGTE wrappers from `SetRGBfifo` through `SetDQA` at `0x800559d4`–`0x80055a87`. Direct `lwc2`/`mtc2`/`ctc2` sequences match all 180 executable bytes with GNU binutils 2.43. Promoted the ten map rows to `verified`/`hand_written_source` and recorded per-function hashes; real GTE/COP2 coverage is now 22 functions and 692 bytes. `SetDQB` remains deliberately outside this exact ten-function unit.
* **GTE color/vector byte matches**: Reconstructed eight PsyQ 4.4.0 LIBGTE wrappers: `LightColor`, `DpqColorLight`, `DpqColor3`, `Intpl`, `Square12`, `Square0`, `OuterProduct12`, and `OuterProduct0` at `0x800551b8`–`0x800553ab` (with intervening unrelated functions outside the accepted ranges). Manifest-driven GNU binutils 2.43 comparisons match all 432 selected bytes. The source preserves color/interpolation register traffic, the square pair's return delay slots, and the outer-product pair's GTE control-register save/restore order. Promoted all eight map rows to `verified`/`hand_written_source`; combined with the four `SetVertex` matches, real GTE/COP2 coverage is now 12 functions and 512 bytes.
* **First real GTE byte matches**: Reconstructed the contiguous PsyQ 4.4.0 LIBGTE block `SetVertex0`/`SetVertex1`/`SetVertex2`/`SetVertexTri` at `0x80055984`–`0x800559d3`. Explicit MIPS assembly reproduces all 80 bytes on the first attempt: the wrappers load GTE data-register pairs 0/1, 2/3, and 4/5 with `lwc2`, individually or from three vertex arguments, then return. Promoted all four from unreviewed library signatures to `verified`/`hand_written_source`; these are real linked functions, not the synthetic `0x20000000` GTE macro segment. The pattern is broadly useful for PsyQ-era PS1 research, while byte-identity evidence remains revision/toolchain-specific.
* **Matching mode-4 dispatcher**: Reconstructed `FUN_800230cc`, the signed submode `0/2/default` dispatcher with a shared byte-copy tail, and matched all 112 reference bytes at `0x800230cc` (file offset `0x90cc`, SHA-256 `cb26535e271b9b4a3943fa914b8ce9d231ccef8c092f14964ec68d2865f1fe8a`). Plain GCC duplicated the copy and epilogue across all three paths and retained its incompatible `ra` slot. The accepted bounded inline sequence preserves one shared tail, both calls, four linked relocations, and exact branch/load delay slots; it remains a compatibility shim rather than an original-source claim.
* **Matching conditional reset handler**: Reconstructed `FUN_800234cc`, the mode-`0xff`/submode-`0x00` conditional caller, and matched all 52 reference bytes at `0x800234cc` (file offset `0x94cc`, SHA-256 `f89d1b763810e38465070c5d387121ad700729c11fd2e3f7febbc0b0111a9a8d`). Plain GCC shrink-wrapped the frame and tail-called `NextSubmode`; disabling both optimizations duplicated the epilogue and retained the incompatible `ra` slot. The accepted bounded inline sequence preserves the shared epilogue, PsyQ `sp+0x10` slot, three linked relocations, and the reference's explicit `addu` encoding for `a0=0`.
* **Matching one-call wrapper**: Reconstructed `FUN_8002356c`, the mode-`0xff`/submode-`0x03` wrapper around `NextSubmode`, and matched all 32 reference bytes at `0x8002356c` (file offset `0x956c`, SHA-256 `5b73884804fb5ff3d5b4e2742236b994d91ed57a487b99f89b8481a57da29882`). Plain GCC C collapsed to an 8-byte tail call; disabling sibling-call optimization restored the 24-byte frame but placed `ra` at `sp+0x14` instead of PsyQ's `sp+0x10`. The accepted bounded inline sequence preserves that frame slot and is explicitly a compatibility shim, not a claim about original inline assembly.
* **Matching empty C hook**: Reconstructed `FUN_800236cc` as an empty C function and matched all 8 reference bytes at `0x800236cc` (file offset `0x96cc`, SHA-256 `6d64edf91449c1b17746c1ef18afa2eb25c70bdf1322ab3df5a2630993b7e2f1`) on the first attempt. GCC emits exactly `jr ra; nop`; this is the first C proof of the no-op shape, complementing the earlier assembly-only `FUN_8007eea8` match.
* **Matching termination-latch setter**: Reconstructed `FUN_8002313c` in C and matched all 52 reference bytes at `0x8002313c` (file offset `0x913c`, SHA-256 `849b35e29c1f8ffc60ea0b8d2e8fe8b7cd4d79ef6d70f85bd4fc9d230aea1346`). The function copies the low byte of `DAT_800e2a60` into secondary-state offset `0x66`, sets secondary offset `4`, and sets the outer-session termination latch at primary offset `0xff`. Straightforward/volatile C collapsed or hoisted pointer loads; barriers recovered the 52-byte phase order, while narrowly fixed `v1`/`v0` values recovered the original register allocation and both delay slots without emitting inline instructions. Reconfirmed four direct call sites across `GAME_OVER`, `ENDING`, and two Cross-hold branches; no field meaning beyond the established termination latch is claimed.
* **Matching session-entry reset**: Manually reviewed `FUN_80023170` and its only two callers, then reconstructed it in C. The function clears four still-unknown bytes at `PTR_DAT_800ac8e8+0xf6/+0xf7/+0x100/+0x101` when entering the session child's `PLAY START` or selector states. GCC 14.2.0/binutils 2.43 matched all 32 reference bytes at `0x80023170` on the first attempt (file offset `0x9170`, SHA-256 `101d8bf4ee4e498991ec6a61004b1e4dbf1c8d54133af6a9005f72b63f0c816e`); the field meanings remain explicitly unknown.

## 2026-07-17
* **Matching submode increment**: Reconstructed `FUN_800231b0` (`NextSubmode`) in C and matched all 32 reference bytes at `0x800231b0` (file offset `0x91b0`, SHA-256 `32cf79c2477fc88366732c01727b9c5b965bcced84a4c66612a29a7b546103d4`). Plain C matched the instruction count and semantics but swapped `v0`/`v1`; direct global access did not change it, pointer register constraints added a `move`, and a counter input/output constraint added a load-delay `nop`. The accepted empty `$2` clobber emits no code, keeps the pointer in `v1` and counter in `v0`, and preserves both the `lhu` load-delay clear and `jr $ra` delay-slot store. This is a GCC register-allocation compatibility constraint, not a claim about original inline assembly.
* **Matching mode-transition function**: Reconstructed `FUN_80023210` (`SetMode`) in C and matched all 32 reference bytes at `0x80023210` (file offset `0x9210`, SHA-256 `29be0968527e9aad066ece450b275c22b0de70865a1e09cc4e5ef26db63b2e53`) on the first attempt with GCC 14.2.0/binutils 2.43. The sibling confirms the reusable `FUN_80023230` pattern: a linked non-volatile `PTR_DAT_800ac8e8`, exact-width structure fields, one load-delay `nop`, and the last `sh` scheduled into the `jr $ra` delay slot under `-fdelayed-branch`. Re-ran the earlier assembly and C matches successfully; this remains bounded per-function evidence, not a whole-toolchain or executable match claim.

## 2026-07-16
* **First matching C function**: Extended the per-function build from raw assembly to manifest-driven C compilation, generated linker placement, and external-symbol relocation. `FUN_80023230` now reconstructs the confirmed `SetSubmode` behavior in C and matches all 28 reference bytes at `0x80023230` (SHA-256 `e5a72934d2e749864691d7b5e9a704e755dd6ed1fe89d1e6dbf95cba18a6390b`). Recorded the negative attempts: a literal pointer cast generated an extra `lui`/`ori`, while incorrectly marking ordinary RAM `volatile` prevented filling the return delay slot. The accepted source uses a linked `PTR_DAT_800ac8e8` symbol and keeps offsets `0x2c`/`0x2e` explicitly unknown. The original assembly match remains green.
* **First matching build**: Added a machine-readable DDR 5th Mix build manifest, a pinned GCC 14.2.0/binutils 2.43 `mipsel-none-elf` assembly path, and a hash-gated per-function comparison script. The first reconstructed source, `FUN_8007eea8`, assembles to 8 bytes and matches the lawful `SLPM_868.97` reference exactly at file offset `0x64ea8` (`jr $ra; nop`, bytes `08 00 e0 03 00 00 00 00`). Generated objects, reference slices, disassembly, and reports stay ignored under `/build/`; this closes backlog item 6 for one bounded function without claiming GNU C or whole-executable equivalence to PsyQ 4.4.0.
* **Raw overlay import + structural cleanup**: Added `Import-RawOverlay.ps1`, which extracts a bounded range from lawful local input, enforces its expected SHA-256, imports it with Ghidra 12.1.2 `BinaryLoader` at the runtime address, and delimits supplied entry points. Reproduced the 11,864-byte `inst demo` overlay from `READ_DT.BIN+0x1630000` (SHA-256 `3dbf4bfa55caf2eb9e8e2db8cef4286441fc9e36850b1dca72515ef89060b0bb`) at `0x801e4000`. Its init/update/exit entries are `0x801e413c`/`0x801e41e8`/`0x801e4284`; update executes 96 non-jump callbacks plus one explicit jump, with 24 60-tick waits, five 80-tick waits, two 20-tick and six 5-tick interpolations, totaling exactly 1,910 scripted ticks before the null callback at `0x801e69cc`. This closes the state-5 boundary: the overlay owns HOW TO PLAY, then attract states 3/4 own DEMONSTRATION. Moved the already-settled `DAT_80105120`, `FUN_80021374`, and `+0x2a` questions out of the open list, corrected the remaining 42-name-table question, and kept still-unknown fields explicit.
* **Local runtime references**: Added root-only `/*.png` ignore coverage so owner-supplied game captures remain available locally without appearing as Git candidates or risking publication; no capture or extracted overlay bytes are tracked.

## 2026-07-15
* **Attract presentation internals**: Parallel callback reviews resolved the visible changes inside the seven-state attract child. State 0 remains diagnostically `WARNING` while `FUN_8004c27c` sequences six resource slots for WARNING, KONAMI, BEMANI, the TOSHIBA EMI sponsor card, Dancemania montage, and a short bridge; exact starts are counters 1/301/601/901/1161/1421, with TITLE first reachable at 1506. State 2 is one 481-tick TITLE presentation, so captures 06/07 are visual phases rather than substates. State 5's executable `inst demo` overlay is now strongly associated with HOW TO PLAY by runtime order, while exact overlay timing remains pending import/dynamic trace. Crucially, states 3–4 `CATCH DEMO` implement the actual gameplay DEMONSTRATION and literally share `FUN_8007fc8c`, `FUN_8007fdec`, and `FUN_80081e90` with DANCING; state 6 alone renders BEST RANKING and returns to WARNING. Promoted seven reviewed helpers; totals remain 2,118 functions / 190 manual rows / 509,608 function-body bytes.
* **Attract-loop runtime order correction**: Reviewed 11 consecutively named owner-supplied captures and made their temporal order authoritative: `WARNING -> KONAMI -> BEMANI -> Dancemania/intercord japan/TOSHIBA EMI promotion -> DDR 5thMIX title -> HOW TO PLAY -> DEMONSTRATION -> BEST RANKING -> WARNING`. The last capture repeats the first and proves that WARNING, not RANKING or Company, is the visible loop boundary. Rotated the already-confirmed seven-state cyclic graph to `WARNING(0) -> TITLE(2) -> PLAY DEMO(5) -> CATCH DEMO(3) -> CATCH DEMO(4) -> RANKING(6) -> WARNING(0)` and separated `FUN_80053ed8`'s conditional constructor choice from observed presentation order. The images remain untracked copyrighted output; exact substate boundaries for intermediate logo, promotional, title, and HOW TO PLAY changes remain open.
* **PLAY DEMO/terminal correction**: Exhaustively audited all 15 session-child updates and the seven-state attract child. Only GAME_OVER state 9 and ENDING state 10 can naturally return terminal 14; their exits set `0x800100ff` before outer dispatch, while the force path requires it already set. Thus a valid player session always follows outer 10/11 to the menu and cannot reach outer 0. Corrected the earlier index error: outer 0 enters `0x25` `RANKING`, not `PLAY DEMO`; true `PLAY DEMO` is attract state 5/index `0x24`, inside the confirmed `RANKING -> WARNING -> TITLE -> PLAY DEMO -> CATCH DEMO -> RANKING` cycle. TITLE loads a `0x2e58`-byte `inst demo` executable overlay from `READ_DT.BIN` LBA `0x7a80` to `0x801e4000`; its callbacks use 3D lighting, proving automated rendered gameplay rather than video, while literal reuse of DANCING judgment/scoring remains pending overlay import. Also corrected child state 10 from inherited LINK END to direct screen index `0x15` `ENDING`. Added 26 exact callback boundaries; map totals are 2,118 functions / 183 manual rows / 509,608 bytes.
* **Outer session closure (corrected above)**: Traced the child-terminal handoff and every xref to effective byte `0x800100ff` (`PTR_DAT_800ac8e8[0xff]`). The byte is operationally a force-terminal/destination latch: zero permits outer state 0, while nonzero forces child state 14 and selects outer 10. Outer 10 runs a 42-update radial 4x3-cell transition, outer 11 restores a 512x480/640x480 pipeline, then returns to outer 1's title/PUSH START main menu. GAME_OVER/ENDING exits set the latch before outer dispatch and therefore take the menu branch; a long-Cross-hold detector can also set it, while main-menu entry or whole-block initialization clears it. Added six exact indirect callback boundaries, promoted eight fully reviewed helpers, and updated the map to 2,092 functions / 157 manual rows / 506,364 covered bytes. The newer entry corrects the old `0x25=PLAY DEMO` label to `RANKING` and proves outer 0 unreachable from a valid session.
* **Result/end-session graph**: Used parallel, non-overlapping reviews of the 15-state game-session child to close every branch after `STAGE END`. State 8 is `RESULT` and returns either to selector-owner state 1 or `PRE_END`; state 11 is `PRE_END` and returns to state 1 or advances to state 12 `LINK END`; `LINK END` branches to state 9 `GAME_OVER`, state 13 `NAME ENTRY`, or a null-entry wait state 10. `GAME_OVER` and state 10 converge on terminal 14; `NAME ENTRY` first passes through state 10. The selector return enters `MUSIC SEL` only under the exact `FUN_80075580` condition, otherwise it restarts at `STYLE SEL`. Also reviewed `FUN_8007fdec` as a per-player timeline classifier (`-1` prestart, `0` active, `1`/`2` normal terminal, `-2` special terminal without overclaiming "failure"). Added 17 authentic indirect boundaries, promoted the classifier, verified removal of three exploratory internal boundaries and restoration of `FUN_8006fa30`/`FUN_8006fba4`; map totals are now 2,086 functions, 143 manual rows, and 506,160 covered bytes.
* **Gameplay-state route**: Continued the confirmed `GAME MODE` path past `MUSIC SEL` through the 15-state child. The selector terminates into state 5, whose screen index 1 is `PREPARE` (correcting an earlier off-by-one `INTRO` label), then state 4 writes index 2 `INTRO`. `INTRO` can route to state 6 or 7; state 6's entry writes index 4 `DANCING`, its update remains there during play and exits toward state 7 on completion, and its exit computes per-player result values. State 7 writes index 5 `STAGE END`, performs timed transition work, and accumulates result/stat fields before branching onward. Added and manually reviewed the nine indirect callbacks for states 4, 6, and 7; the map now has 2,069 functions / 125 manual rows and 504,344 bytes of function-body coverage.
* **State-2 selector map**: Followed the main menu's confirmed `GAME MODE`/`EVENT MODE -> DAT_80105124 state 2` target through three layers of enter/update/exit tables. Outer state 2 is `FUN_8004bdec`/`FUN_8004b800`/`FUN_8004be30`; it owns a 15-state game-session child, whose state 1 owns a six-state screen selector. Entry callbacks provide exact screen-index identities: substate 0 `STYLE SEL` (`FUN_800756f0`), 1 `MODE SEL` (`FUN_800757e0`), 2 `CHARA SEL` (`FUN_80075840`), 3 `MUSIC SEL` (`FUN_800754b4`), 4 `LINK START` (`FUN_80075a40`), and 5 terminal. `FUN_80075af8` confirms `STYLE -> LINK START -> CHARA -> MUSIC -> terminal`, with `LINK START -> MUSIC` as the character-select skip path controlled by `PTR_DAT_800e0b18[0x98]`; no normal incoming route to `MODE SEL` was found. Added 26 indirect callback boundaries, promoted 8 existing wrappers/routers, and updated the symbol map to 2,060 functions / 116 manual rows.
* **Runtime screen corroboration**: Reviewed the owner's expanded, untracked screenshot set. `MainMenu.png` is the renamed original main-menu capture. Following its rows produces screens visibly headed `ARCADE LINK`, `RECORDS` (including a `NO MUSIC` empty state), `OPTION`, `GALLERY MODE`, and `INFORMATION`, confirming the already-derived outer-state identities 6, 7, 8, 13, and 9. Three consecutive `GAME MODE` captures independently confirm the visible setup order `SELECT STYLE -> SELECT CHARACTER -> SELECT MUSIC`, matching the internal `STYLE SEL`/`CHARA SEL`/`MUSIC SEL` strings; this identifies state 2 as entry to that flow but does not yet assign numeric values to its nested screen states.
* **Music-database correction + export**: The literal `data/mdb/mdb.bin` is not an ISO 9660 entry, and reviewing its call target refutes the earlier loader interpretation: `FUN_8007eea8` is exactly `jr ra; nop`, so the path is a retained source/build marker with no runtime file-open behavior. Located the runtime metadata already linked into `SLPM_868.97`: 47 records at `DAT_800df3d8` (stride `0x1c`), a separate 50-ID table at `DAT_800df900`, and title strings. Added a hash-gated exporter plus a 47-row CSV with music IDs, titles, SP/DP difficulty nibbles, unlock selectors, BPM pairs, and preserved unknown fields. Also added a recursive ISO extractor, recorded the negative `READ_DT.BIN` container result, and corrected the screen-name string start from the marker's `0x8001bb10` to `0x8001bb24`.
* **Main-menu identification**: Reviewed `DAT_80105124` state 1's enter/update/exit callbacks and its selector/render helpers, then matched them against the repository owner's runtime screenshot (kept untracked as copyrighted game output). State 1 is the 11-row main menu on the title/`PUSH START` screen: it loads `title_25`/`hbota_25`, navigates with Up/Down, confirms with Start/Circle, and returns to state 0 after 900 inactive frames; the owner independently confirms that reaching this timeout returns to the Attract Loop. Recorded the exact destination-state mapping for all 11 visible options; `GAME MODE` and `EVENT MODE` converge on state 2 with different setup flags. Added 3 indirect callback boundaries and promoted 12 existing helpers after manual review (2,034 total functions, 71 manual).
* **Input tracing + tooling**: Traced every reference to `DAT_800e3b60` and `DAT_800e3b70` with new `DumpDataXrefs.java`, which prints exact reference type/function/instruction: `FUN_8002112c` is the sole writer of both. It runs once per frame, validates two PsyQ pad receive buffers, converts their active-low button bytes, and publishes previous/current/newly-pressed/newly-released words; these two globals are specifically newly pressed for ports 1/2. Their buffers are registered during `FUN_80021dfc` through real PsyQ `PadInitDirect(0x800e2a68,0x800e2a9c)` + `PadStartCom`, so the low-level driver is PsyQ/BIOS and `FUN_8002112c` is the game-side adapter. Packet order confirms `0x800=Start`, `0x100=Select`, `0x20=Circle`: `FUN_8004b554` advances on Start or Circle newly pressed, and `FUN_80021374` is now statically confirmed as Start-held + Select-newly-pressed return-to-attract, not merely a structural match.
* **Review**: Reviewed `DAT_80105124`'s state-0 enter/update/exit triple, starting at requested target `FUN_8004bcc8`. It does not recurse into a third copy of the same generic 14-state machine; instead it owns a distinct subordinate 7-state enter/update/exit dispatcher at tables `0x800ddc68`/`0x800ddc84`/`0x800ddca0`, normally initialized to state 6 (state 0 only for `DAT_800f2908` values `0x1c`/`0x1d`). The outer update ticks that child until mask `0x820` appears in either of two input words, then returns `1`, confirming an outer transition `0 -> 1` without yet naming the buttons or screen. `DumpFunctionDetail.java` created three missing indirect callback boundaries; the symbol map now has 2,031 functions and 54 manually reviewed rows.
* **Correction**: The repository owner describes a real Consumer-build feature -- pressing Start+Select during the game loop immediately returns to the `KONAMI`/`BEMANI`/`TOSHIBA` attract screens. This is a strong structural match for `FUN_80021374`'s button-combo-triggered transition to mode `0xff` (a reset/service state performing a GPU reset and re-arming `main`'s loop-restart flag), identified 2026-07-14. This withdraws `FUN_80021374` as evidence for the "arcade-only leftover" quirk entry -- it's better explained as a legitimate, intentional feature, not an inaccessible arcade leftover. The quirk entry, symbol map, and screen-flow doc all corrected; the entry's other evidence (the `TEST_MODE` string and mode `0x10`'s still-unreachable menu) stands on its own.
* **Screen flow**: Recorded the repository owner's domain-knowledge account of the actual gameplay session flow on Consumer BEMANI releases (distinct from the attract loop already recorded): Caution -> Style Select -> Select Character -> Select Music -> Gameplay -> Result screen -> (Select Music / Summary / Game Over depending on settings). Maps directly onto more of the 42-name string table: `WARNING`=Caution, `STYLE SEL`=Style Select, `CHARA SEL`=Select Character, `MUSIC SEL`=Select Music, `DANCING`=Gameplay, `RESULT`=Result, `GAME_OVER`=Game Over. No string obviously matches "Summary" yet (`ENDING`/`PRE_END`/`SELECT END` are candidates, unconfirmed).
* **Correction**: Read one level deeper into `DAT_80105120` state 0 (`FUN_8004ba34`/`FUN_8004bbb4`/`FUN_8004bc54`), correcting the same-day "45 entries = 15 states" reading. `FUN_8004ba34` bootstraps a *second, independent, nested* state machine on a different global (`DAT_80105120+4`), and `FUN_8004bbb4`/`FUN_8004bc54` turn out to be a generic, reusable, parameterized version of `FUN_80049dec`'s own tick logic -- confirming the engine has one reusable "tick a state machine" primitive used both at the top level and recursively for nested children. Reading the nested child's own 3 tables directly (`tools/ghidra/scripts/DumpJumpTable.java` against `0x800d9ac8`) confirmed it has **14 states** (3 flat 14-entry arrays, not interleaved triples) -- so the 45 raw entries are actually `DAT_80105120`'s own 1-state (3-word) table immediately followed by this separate 14-state (42-word) child table, not one 15-state table. `DAT_80105120` itself only ever holds state 0 in every call site found so far; the real branching is one level down. Neither 15 nor 14 matches the 42-name string table's count, so that correction stands regardless.
* **Tooling + review**: Extended `tools/ghidra/scripts/DumpFunctionDetail.java` to create a function boundary (`CreateFunctionCmd`) when asked to dump an address Ghidra's auto-analysis never turned into a function -- needed for `DAT_80105120`'s update/exit callbacks, which are only reached indirectly. Reviewed state 0's enter/update/exit triple (`FUN_80049c24`/`FUN_80049f7c`/`FUN_80049fa4`): `FUN_80049c24` (enter) is called directly by `FUN_80049d3c` as part of mode `0x04`'s boot chain, confirming the state machine is explicitly bootstrapped there; it shares inline player-record init code with `FUN_80049d3c`. `FUN_80049f7c` (update) always returns a literal 0, meaning it never itself triggers a transition. Neither reveals a screen identity yet -- the real logic is one level deeper, in unreviewed `FUN_8004ba34`/`FUN_8004bbb4`/`FUN_8004bc54`. Added 2 new symbol-map rows for the two previously-function-less addresses (2,028 total functions now, up from 2,026).
* **Quirks**: Added the DDR 5th Mix quirk log's first entry, "Arcade-only functionality left in the Consumer build, unreachable but not removed" (`status: suspected_shared`), per the repository owner's domain knowledge that BEMANI PS1 ports routinely leave arcade-specific service/test/debug code compiled in without a reachable retail-control trigger, rather than stripped. This gives a coherent explanation for two open puzzles: `FUN_80021374`'s arcade-test-button-shaped service/reset-combo watcher, and why no path has been found into mode `0x10`'s interactive menu (`FUN_80022b30`) despite an exhaustive search -- "no path found" may be the correct final answer, not an unsolved gap.
* **Correction**: Read the raw contents of `DAT_80105120`'s three function-pointer tables (`tools/ghidra/scripts/DumpJumpTable.java` against `0x800d9abc`) and found exactly 45 valid code-address entries (`45 = 15 x 3`, an enter/update/exit triple for 15 states), terminated by `0xFFFFFFFF` -- not 42. This refutes the earlier same-day claim that `DAT_80105120` directly indexes the 42-entry screen-name string table (15 != 42); the two are more likely separate structures. The string table's own contents remain valid evidence for the screen names it lists -- only the "what indexes it" claim is withdrawn. A more conservative reading is that the 42-name table is a debug/test-mode menu listing rather than the engine's live state enum. Bonus: found a further, unexplored table of option/settings-name strings ("Game Mode", "Diet Mode") immediately after the 45-entry table's terminator.
* **Discovery**: Found a 42-entry screen-name string table at `0x8001bb10` (`BOOT`, `PREPARE`, `INTRO`, `HOW TO`, `DANCING`, `STAGE END`, `RESULT`, `NON STOP I/C`, `STYLE SEL`, `MODE SEL`, `CHARA SEL`, `MUSIC SEL`, `SEQKIND SEL`, `LINK SEL`, `COURSE SEL`, `INRAN SEL`, `EDSEQ_SEL`, `SELECT END`, `NAME ENTRY`, `PRE_END`, `ENDING`, `URL&PASS`, `LINK START/END`, `GAME_OVER`, `PLAY START`, `GAME ??`, `PUSH START`, `WARNING`, `KONAMI`, `BEMANI`, `TOSHIBA`, `TMOVIE`, `TITLE`, `CATCH DEMO`, `PLAY DEMO`, `RANKING`, `LETS LINK`, `DEMO ??`, `TEST_MODE`, `OTHER`) while chasing `FUN_80049dec`'s `"TEST_MODE"` string, using a new tool, `tools/ghidra/scripts/DumpBytes.java` (raw hex+ASCII dump). This is almost certainly the game's real top-level screen enum, indexed by `DAT_80105120` through three parallel function-pointer tables (update/exit/enter) -- a much richer state machine than the `PTR_DAT_800ac8e8` mode/submode system this document has focused on so far. Directly confirms the repository owner's domain-knowledge account with literal string evidence: `WARNING`=Caution, `KONAMI`/`BEMANI`/`TOSHIBA`=three separate Company logo screens, `HOW TO`=How To Play, `PLAY DEMO`=Gameplay Demonstration, `RANKING`=Ranking. One confirmed link to the existing mode system: mode `0x04` initializes `DAT_80105120` to state 0 (`BOOT`); mode `0x02`/submode `0x02` pumps it every frame. How the two systems fully relate is not yet established -- likely the next major thread, possibly meriting its own `DAT_80105120`-keyed screen-flow document.
* **Review**: Reviewed mode `0x02`'s three submode handlers, prompted by the repository owner recalling (from prior BizHawk observation) that DDR 5th Mix switches display resolutions during play. Confirmed directly: submodes `0x00`/`0x01` (`FUN_8009f820`/`FUN_8009f390`) are a display-off/resize-to-640x480/delay/display-on resolution-change sequence -- matching the owner's runtime observation exactly. Submode `0x02` (`FUN_80049dec`) turned out unrelated -- a separate enter/exit/update state machine keyed on `DAT_80105120` (shared with mode `0x04`'s music-database loader) that copies a table ending at an entry containing a pointer to the literal string `"TEST_MODE"`, this project's first human-readable string. Every mode reviewed in the `0x00 -> 0x10 -> 0x04 -> 0x02` chain now reads as infrastructure (memory card I/O, GPU reset, asset/database loading, resolution switching), not a visible screen -- the first real attract-loop screen identity is still further down the chain, not yet located.
* **Review**: Reviewed mode `0x04`'s remaining 6 callees, completing its call tree. Found this project's first literal filename string, `"data/mdb/mdb.bin"` (loaded by `FUN_80049d3c`, called from both of mode `0x04`'s handlers) -- almost certainly the song/chart metadata database. Also found real PsyQ SPU/reverb setup calls (`FUN_8002a7a4`) and a settings-clamp routine sanitizing ~25 option bytes of a third struct (`FUN_8009b0a8`, `PTR_DAT_800e0b18`). This reframes the whole `0x00 -> 0x10 -> 0x04` chain as boot/loading infrastructure (memory card load -> GPU reset + asset load -> settings validation + music DB load + audio setup) rather than a sequence of visible screens, pushing the search for the first real screen identity to mode `2`'s own submodes. Also corrected an ambiguous reading of `FUN_8002340c`'s two branches: only one (`PTR_DAT_800ac8ec[6] != 0`) is an actual mode transition; the other advances submode within mode `0x04`.
* **Review**: Reviewed `FUN_800232cc`'s (mode `0x10`/submode `0x00`) two callees, `FUN_800222fc` and `FUN_8002a9dc`. Neither touches submode (the "does the menu get armed during boot" question stays open), but both clarify what this submode is for: `FUN_800222fc` is pure GPU reinitialization built from real PsyQ calls (`ResetGraph`/`GsInitGraph2`/`GsInit3D`/`InitGeom`/`ClearImage2`/`DrawSync`); `FUN_8002a9dc` (called from 22 places project-wide, confirming it's generic) is an asset loader that draws primitives then uploads images to VRAM via the real `LoadImage` call. This reframes mode `0x10`/submode `0x00` as generic screen-entry boilerplate -- reset GPU, load assets, hand off to mode `4` -- rather than a screen in its own right, nudging the "title/mode-select" guess toward mode `4` instead of mode `0x10` itself.

## 2026-07-14
* **Review**: Tested the repository owner's hypothesis that mode `0x10`'s menu might be armed during Memory Card Auto Load (mode `0x00`), explaining why it's ready the instant Start is pressed. Not confirmed: enumerating every write to `+0x2a` (submode) across all 58 functions referencing `PTR_DAT_800ac8e8` found only three writers -- `FUN_80023210` (`SetMode`, resets submode to 0), newly-identified `FUN_800231b0` (`NextSubmode`, increments submode -- explains a function called from nearly every handler reviewed so far), and `FUN_80023230` (`SetSubmode`, sets a literal value, only called once to advance mode `0x00`'s own local sequence). No path was found that leaves mode `0x10`'s submode at `1` across a frame boundary -- a genuine, well-supported open puzzle now, not a review gap. Side-discovery: `FUN_80021374` (previously an unexplained second mode-field reader) is a service/reset-combo watcher that transitions to mode `0xff` on a specific controller-button combo, explaining that mode's elaborate 5-way dispatch as a real reset/service state.
* **Review**: Reviewed mode `0x10`'s two submode handlers to test the "is mode 0x10 the Company screen?" hypothesis from earlier the same day. Refuted it: `FUN_80022b30` (submode `0x01`) is a real d-pad-cycle-and-confirm menu with a 3-entry destination-mode lookup table (`DAT_800ac8e0 = {4, 0x32, 0x20}`, read directly with a new tool, `tools/ghidra/scripts/DumpShorts.java`), not passive splash-screen code -- a non-interactive attract-loop screen wouldn't need selection logic across 2 controller ports. New hypothesis: mode `0x10` is a title/mode-select hub, `suspected`. This also closes the loop on the confirmed transition graph: mode `0x10` <-> modes `0x20`/`0x32` (menu choices 1/2 send you there, their handlers send you back), while submode `0x00` (`FUN_800232cc`) always unconditionally forwards to mode `4` (which itself flows to mode `2`). `FUN_800232cc` also reaches a second real PsyQ kernel call, `SetDispMask(1)`. New open question: since `SetMode` always resets submode to 0, and submode 0 always forwards away immediately, it's unclear how submode ever advances to 1 to show the menu at all.
* **Tooling + review**: Added `tools/ghidra/scripts/DumpFieldXrefs.java` (dumps decompiled C for every function referencing a given global) and used it on `PTR_DAT_800ac8e8` (58 referencing functions) to chase the long-open "where is the mode field (+0x28) ever written" question. Found `FUN_80023210` is the game's sole `SetMode(newMode)` primitive: writes its argument to +0x28 and clears submode (+0x2a) plus two previously-unseen fields (+0x2c/+0x2e). This turns every already-reviewed call site to it into a confirmed mode transition (modes 0x00/0x20/0x32 -> 0x10; mode 0x04 -> 2; any mode -> 0x80 via the dispatcher's own epilogue flag), resolves that open question, and refutes the earlier +0x17-as-queued-next-mode hypothesis (superseded, not deleted) since the function that writes +0x17=0x80 separately calls SetMode(0x10), not 0x80. New hypothesis: mode 0x10, reached by 3 independent transitions and the default fallback, is a structural match for **Company** (the attract loop's anchor screen) -- recorded at `suspected`. Also surfaced `FUN_80021374`, a second +0x28 read site outside the mapped dispatcher tree, not yet reviewed.
* **Review**: Manually reviewed the remaining 10 call targets from the mode dispatcher tree (mode `0x00`'s 3 submode handlers, mode `0x04`'s 2, mode `0xff`'s 5 confirmed jump-table targets), promoting all to `confidence = manual`. Found mode `0x00` is a 3-step countdown state machine whose last step writes `PTR_DAT_800ac8e8+0x17 = 0x80` (a literal top-level mode constant) — the strongest lead yet on how mode transitions actually happen, though the missing link (something copying `+0x17` into the real mode field `+0x28`) hasn't been found. Also found mode `0xff`'s submode `0x04` (`FUN_8002358c`) calls the real PsyQ kernel function `ResetGraph(1)` and writes `PTR_DAT_800ac8e8[9] = 1`, the same byte `main`'s own reset routine unconditionally re-zeroes every pass — flagged as an open question about which of `main`'s remaining unreviewed per-frame calls actually reads it. 12 new call targets discovered, none yet reviewed.
* **Screen flow**: Recorded the repository owner's domain-knowledge account (former StepMania Team developer, BEMANI genre convention) of the expected boot-to-attract sequence: PS BIOS logo -> Memory Card Auto Load -> Caution -> Attract Loop (Company -> How To Play -> Gameplay Demonstration -> Ranking -> back to Company). Paired mode `0x00` with **Memory Card Auto Load** at `confidence: suspected`, backed by a real structural fact: `FUN_8002216c`'s boot-time state reset zeroes the mode field itself, so mode `0x00` is guaranteed to be the first value the dispatcher ever sees. The remaining five screens are not yet paired to specific mode values — flagged as an open gap pending string/asset evidence in the unreviewed handlers or a full map of `+0x28`'s write sites.
* **Review**: Manually reviewed the 5 smallest handlers discovered by `FUN_80022cf8`'s mode dispatcher (modes `0x00`, `0x04`, `0x20`, `0x32`, `0xff`), promoting them to `confidence = manual`. Corrected two "single call, no args observed" notes: modes `0x00` and `0x04` are themselves submode dispatchers on the same `PTR_DAT_800ac8e8+0x2a` field mode `0x02`/default already use — meaning `+0x2a` is likely a general per-screen phase counter, not scoped to one screen. Also found modes `0x20` and `0x32` have byte-for-byte identical handler bodies, echoing the open "why do `0x10` and default share a dispatch" question. 10 new call targets discovered (3 for mode `0x00`, 2 for mode `0x04`, 5 jump-table targets for mode `0xff`), none yet reviewed.
* **Tooling**: Added `tools/ghidra/scripts/DumpJumpTable.java`, a reusable headless script that reads N consecutive 32-bit words at an address and resolves each to a function name. Used it to read the raw bytes of mode `0xff`'s jump table at `0x8001a840`, confirming the submode-0..4-to-callee mapping inferred earlier the same day from code-layout convention alone.

## 2026-07-13
* **Target selection**: Selected Dance Dance Revolution 5th Mix (Japan, serial `SLPM-86897`) as the project's first target revision with a `byte_match` success contract, decided by the repository owner.
* **Schema**: Added a revision manifest schema covering identity, input provenance, boot-executable, and tool-version fields.
* **Provenance**: Extracted the supplied CHD to a raw data track, hashed it, and recorded the result in the first filled revision manifest under `docs/games/`.
* **Tooling**: Added a PowerShell ISO 9660 reader that resolves `SYSTEM.CNF`, extracts the boot executable, and parses its PS-X EXE header.
* **Analysis**: Installed a portable, git-ignored Ghidra 12.1.2 + JDK 25 + `ghidra_psx_ldr` and imported the boot executable headlessly; its PsyQ Signatures analyzer identified PsyQ SDK `4.4.0` as the original toolchain.
* **Schema**: Added a symbol map schema (CSV columns, confidence tiers) and exported the first tool-generated symbol map for the boot executable (2,026 functions; 1,045 PsyQ-signature-matched, 981 unverified).
* **Review**: Manually reviewed the PsyQ crt0 startup chain (`start`→`stup1`→`InitHeap`→`stup0`→`main`→`trap`), promoting 4 functions to `confidence = manual` and locating `main` at `0x800226a4` as the crt0-to-game-code boundary.
* **Coordination**: Removed the file-based handoff claim/coordination system (`docs/handoffs/`) at the repository owner's direction. Agents now commit and push completed work directly instead of waiting for user review; see `AGENTS.md`'s "Commit and push" section. Configured `origin` to `https://github.com/RieLoveChan/RiePS1`.
* **Review**: Manually reviewed `main` (`0x800226a4`), confirming it is a standard PS1 main loop (subsystem init, RCNT2 vsync/timer IRQ setup via correctly-named PsyQ kernel calls, then an infinite frame loop). All 12 of its unnamed callees matched no PsyQ signature, confirming genuinely DDR-5th-Mix-specific code has now been located and is ready for review.
* **Schema**: Added a quirk-entry schema for recording non-standard engineering practices, with a two-tier structure (per-game log, promoted to a cross-game vendor-quirks log only once a pattern is independently confirmed in a second game). Created the (currently empty) DDR 5th Mix quirk log and the cross-game log.
* **Review**: Manually reviewed `FUN_8002216c`, correcting an earlier hypothesis in `main`'s notes — it is a one-shot state-reset call, not a wait/spin loop condition, because it unconditionally zeroes the exact flag byte its caller's loop tests. Local disk cleanup: removed ~3.08GB of duplicate disc copies across `runtime/bizhawk/runs/*/disc/` (git-ignored, fully reproducible from `input/`), keeping only the small evidence files.
* **Review**: Manually reviewed `FUN_80022cf8` and identified it as the game's mode/screen dispatcher — reads a 16-bit field at `PTR_DAT_800ac8e8+0x28` and branches to ~9 distinct handlers by sparse enum-like values, with a second-level submode dispatch at `+0x2a`. Discovered 14 new unreviewed call targets (mode handlers + a shared per-frame epilogue); this is now the best entry point for mapping the game's top-level screen flow.
* **Schema**: Added a screen/mode flow schema and the DDR 5th Mix screen-flow document, pre-populated with the mode dispatcher's structural facts (handler addresses, sizes, arguments) and empty `proposed screen`/`confidence` columns. The repository owner is a former StepMania Team developer and will fill in domain-informed screen hypotheses for verification against the actual handler code.

## 2026-07-12
* **Validation**: Added a pinned BizHawk 2.11 launcher and Lua probe for deterministic PS1 execution evidence.
* **Tooling**: Adopted a locally supplied, Git-ignored `chdman` as the default CHD conversion tool for the BizHawk harness.
* **Safety**: Added a local CHD intake layout and Git exclusions for copyrighted inputs, proprietary tools, generated game content, and analysis caches.
* **Initialization**: Created the OKF 0.1 knowledge bundle and progressive-disclosure indexes.
* **Research**: Added initial PlayStation architecture, toolchain, and decompile/recompile workflow guidance.
* **Coordination**: Added a file-based concurrent-agent claim and handoff system.
* **Resource setup wrapper reconstructed**: Added `FUN_800286bc` (40 bytes) to `src/ddr5thmix/StateTransition.s`. It passes the 8-byte destination at `0x800e37e0` to `FUN_8003bac8`, preserving the observed call and delay-slot layout. `Invoke-FunctionMatch.ps1` matched all 40 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `084fdf4cfcf3fc5e69d3a9e8bc5a73fff5ae6a6744ba4ed924fceb1262be8a5c`.
* **Resource class predicate reconstructed**: Added `FUN_800286e4` (28 bytes) to `src/ddr5thmix/StateTransition.s`. It masks the argument to 16 bits, returns `2` for values below `53`, and returns `1` otherwise. `Invoke-FunctionMatch.ps1` matched all 28 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `efe4561fac1208ad49676d97eb1b75d637237d476e363b78927eaa58ab61b020`.
* **State dispatch predicate reconstructed**: Added `FUN_80028740` (72 bytes) to `src/ddr5thmix/StateTransition.s`. It reads the state word at `0x800e3768`, dispatches values below `9` through the table at `0x8002aa8c`, and calls `FUN_8002422c` for out-of-range values. `Invoke-FunctionMatch.ps1` matched all 72 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `02495eb1cb7fee57b830ea0c68247e592c208f9eb03039a0ee3f23fdbdcfb32d`.
* **State progression handler reconstructed**: Added `FUN_800287e0` (180 bytes) to `src/ddr5thmix/StateTransition.s`. It advances the state word at `0x800e376c` through states 0–3, runs the initialization/check sequence for state 1, increments the state after success, and clears the state back to zero for state 3. `Invoke-FunctionMatch.ps1` matched all 180 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `b9c2660b518431709bfdf7392f790cf236b7f6738e4623d659318ac5591af2df`.
* **Timing comparison helper reconstructed**: Added `FUN_80028894` (72 bytes) to `src/ddr5thmix/StateTransition.s`. It computes the elapsed value from the two state fields at `0x800e3774`/`0x800e3778`, scales it by four bits, compares the result against `FUN_80028d3c`, and returns the unsigned comparison. `Invoke-FunctionMatch.ps1` matched all 72 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `99ba85f8cd17c53d0beeeda5ff073259958a5c1c862804db97b9eade7119ffeb`.
* **Subsystem activation wrapper reconstructed**: Added `FUN_800289d0` (36 bytes) to `src/ddr5thmix/StateTransition.s`. It calls `FUN_8002ef5c` with arguments `0` and `0x00c00000`, preserving the observed setup and delay-slot layout. `Invoke-FunctionMatch.ps1` matched all 36 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `5ae9f7d9ce424e93d01f811ef8dd7d12bc0f8a61253d52f7ecf2bcb0f6f1ce58`.
* **Transfer descriptor initializer reconstructed**: Added `FUN_800289f4` (152 bytes) to `src/ddr5thmix/StateTransition.s`. It initializes the descriptor at `0x800e2b40` twice with the observed mode, address, and halfword fields, submitting each configuration through `FUN_8002f42c`. `Invoke-FunctionMatch.ps1` matched all 152 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `ac9e5f6c1bf1e17ca09e4b512da8d05a9931823ba5d0d6e2e98823a334bc8bdd`.
* **Subsystem wrapper reconstructed**: Added `FUN_80028a8c` (32 bytes) to `src/ddr5thmix/StateTransition.s`. It invokes `FUN_80024e00` with no arguments and returns through the observed 24-byte stack frame. `Invoke-FunctionMatch.ps1` matched all 32 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `2fcf37804a7a80d559b721d2e3c3efb98df5aa50ce7ed83ade1a471dfa1a1fe7`.
* **Timing value initializer reconstructed**: Added `FUN_80028aac` (36 bytes) to `src/ddr5thmix/StateTransition.s`. It calls `FUN_800358f8(-1)` and stores the result at `0x800e3798`, preserving the observed call and delay-slot layout. `Invoke-FunctionMatch.ps1` matched all 36 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `d2b846e2f46e72de7493cfbe475013abba75d16480c13580469644c4987acd00`.
* **Transfer buffer wrapper reconstructed**: Added `FUN_80028ad0` (48 bytes) to `src/ddr5thmix/StateTransition.s`. It submits the 48-byte descriptor at `0x800e39b0` through `FUN_8003bac8` and sets the observed completion flag to `1`. `Invoke-FunctionMatch.ps1` matched all 48 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `6bed795af37927b996e22a8ccbcfa732af41aecbda0c6cc733bc445721f3f33a`.
* **Queued transfer setup reconstructed**: Added `FUN_80028b00` (108 bytes) to `src/ddr5thmix/StateTransition.s`. It prepares the descriptor at `0x800e39b0`, increments its request counter, resolves the `0x80025d44` resource, invokes the transfer and completion helpers, and returns zero. `Invoke-FunctionMatch.ps1` matched all 108 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `f0a74b6fb541d2561bada1a7fb9668837ef622c9436fe3f00816c05873532a8a`.
* **Completion flag getter reconstructed**: Added `FUN_80028b6c` (16 bytes) to `src/ddr5thmix/StateTransition.s`. It returns the completion flag stored at `0x800ae478`. `Invoke-FunctionMatch.ps1` matched all 16 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `f7be237b7ad8ad272afb5af429f4dfd2854cf0184f3b1fc8c65a61c5cc41439f`.
* **Transfer size initializer reconstructed**: Added `FUN_80028c04` (16 bytes) to `src/ddr5thmix/StateTransition.s`. It stores `0x1000` at `0x800e37a0` in the return delay slot. `Invoke-FunctionMatch.ps1` matched all 16 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `9916caea0d8dbf1bf2b98bd4f0e35570e2590045da10e997ab151dcf2e33319b`.
* **Graphics timing calculator reconstructed**: Added `FUN_80028c14` (56 bytes) to `src/ddr5thmix/RuntimeGraphicsInit.s`. It scales the timing field at `0x800e377c` with the observed unsigned multiply/HI extraction, then multiplies by the descriptor field at `0x800e37a0` and shifts the result. `Invoke-FunctionMatch.ps1` matched all 56 bytes with GCC 14.2.0/binutils 2.43 against lawful `SLPM_868.97_1` SHA-256 `4e0308ca35000fe91bf0b468297125061efeb16198c27fd13c950003d94c4aee`; reference/built SHA-256 `7b6d1025ba12bad69bd7204925319f48d68eb69068e1ebaf9a2157ff84ed5d39`.
